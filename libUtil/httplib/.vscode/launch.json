{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to NotebookFlow",
            "type": "cppvsdbg",
            "request": "attach",
            "processId": "${command:pickProcess}"
        }
    ]
}