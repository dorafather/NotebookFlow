# NotebookFlow DSL(한글DSL) 완전 가이드

> **이 문서는 실제 소스코드를 직접 읽고 검증하여 작성되었습니다.**
>
> 검증한 핵심 소스: `libUtil/parser/{CLASSPARSER,STATEPARSER,PROCPARSER,ACTIONPARSER,
> STMTPARSER,IFPARSER,CXIFPARSER,ASSIGNPARSER,BASICPARSER,FUNCPARSER,INIPARSER}.cpp/h`,
> `libUtil/interpretor/{EXESCEN,EXESTATE,EXEPROC,EXEIF,EXECXIF,EXEACTION,EXEFUNC,
> EXEPARAM,EXESTMT,EXECORE}.cpp/h`, `libUtil/flow/FLOW.cpp`, `libUtil/kstring/STRCONV.cpp/h`,
> `NotebookFlow/{Main.cpp,OutboundClient.cpp/h}`, 그리고 실제 운영 중인
> `NotebookFlow/rest.sce`, `NotebookFlow/addr.ini` 전체.
>
> **이 프로젝트는 "설계 의도"와 "실제 코드 동작"이 미묘하게 어긋나는 사례를
> 여러 번 실측으로 발견해 왔습니다** (예: CPH가 필드 이름이 아니라 순서로
> 값을 뽑는다는 것, NAMESPACE가 호출부에는 필요 없다는 것 등). **이 문서는
> 기존 설계 문서(`NotebookFlow_업무/` 폴더)를 그대로 베끼지 않고, 소스코드와
> 실제 배포된 `rest.sce`를 직접 대조해 불일치가 발견된 곳은 전부 "코드가
> 맞다"는 원칙으로 정정했습니다.** 본문 중 회색 인용 박스(`> ⚠️ 불일치`)로
> 표시된 부분이 그 정정 지점입니다. 전체 불일치 목록은 문서 끝 부록 A에
> 모아 두었습니다.
>
> 이 문서 하나만 읽으면, NotebookFlow를 처음 접하는 인간 사용자를 대신해
> 자연어 요청을 정확한 `.sce` 코드로 옮길 수 있도록 구성했습니다. 4부로
> 이루어져 있습니다: **1부 철학**, **2부 문법 레퍼런스**, **3부 실제 예시**,
> **4부 자연어→DSL 변환 가이드**.

---

# 목차

- [1부 — 철학과 설계 원칙](#1부--철학과-설계-원칙)
- [2부 — 정확한 문법 레퍼런스](#2부--정확한-문법-레퍼런스)
- [3부 — 실제 동작 예시 (rest.sce 원문 발췌)](#3부--실제-동작-예시-restsce-원문-발췌)
- [4부 — 자연어 요청을 DSL로 옮기는 가이드](#4부--자연어-요청을-dsl로-옮기는-가이드)
- [부록 A — 발견된 불일치 전체 목록](#부록-a--발견된-불일치-전체-목록)
- [부록 B — 참고 소스 파일 / 설계 문서 목록](#부록-b--참고-소스-파일--설계-문서-목록)

---

# 1부 — 철학과 설계 원칙

## 1.1 이벤트 기반 순환 구조 — 상태 → 처리 → 조건 → 수행

NotebookFlow의 DSL 엔진(SLEE+한글DSL 계열)은 **비동기 이벤트 기반 세션
FSM(Finite State Machine)**입니다. 통신사 호처리(콜 프로세싱) 엔진에서
유래했기 때문에, 기본 그림은 다음과 같습니다.

```
외부 이벤트 도착(HTTP 응답/타이머/수신 메시지)
   ↓
상태::(현재 상태의 조건 목록과 대조 — NAMESPACE.수신메시지.필드 == 값)
   ↓
처리::(만약에/그외그외/그외 조건 분기, 함수.* 호출로 세션 변수 조작)
   ↓
전송::/타이머::(다음 행동 — 외부 HTTP 호출 발신 또는 타이머 재무장)
   ↓
(응답이 돌아오면 다시 상태:: 조건 대조부터 반복)
```

이 순환을 코드로 직접 확인한 지점은 `libUtil/flow/FLOW.cpp`의
`Flow::proc()`(542-585줄)입니다. 여기서 수신된 패킷을 `SCE_EVENT` 값에
따라 `procStartReq`/`procAction`/`procStopReq`/`procNonProto` 중 하나로
분기하고, 최종적으로 전부 `ExeCore::OBJ().EXE(_wk, pool, msg)`
(`EXECORE.cpp:119-127`)를 호출합니다. 이 `EXE()`가 곧 "현재 상태에서
조건에 맞는 처리::를 찾아 실행한다"는 한 사이클의 진입점입니다.

**왜 이런 구조인가**: 이 엔진은 원래 몇 분~몇 시간에 걸쳐 여러 에이전트의
응답을 시간차를 두고 받는 통신사 호처리용으로 설계되었습니다(`설계노트_
배치문법vs세션FSM.md`도 이 성격을 재확인하며 "새 배치 문법은 필요 없다"는
결론으로 끝납니다 — 엔진 확장이 아니라 LLM이 자연어를 대신 번역해주는
쪽으로 문제를 풀기로 확정됨). NotebookFlow는 이 성숙한 엔진을 그대로
가져와 REST 브릿지 용도로 재사용하고 있을 뿐입니다.

## 1.2 세션(Context)과 수신메시지(단발성)의 차이

DSL 코드 안에서 데이터는 크게 두 가지 "생애주기"로 나뉩니다.

- **`수신메시지`(RCV_MSG)**: 지금 막 도착한 이벤트 하나의 JSON 페이로드.
  이 이벤트 처리가 끝나면 사라집니다. `EXEPARAM.cpp`의 `수신메시지` 분기는
  단순히 `_req.GETR(경로).VAL()`로 "지금 들고 있는 메시지 객체"를 그대로
  읽어올 뿐, 아무것도 저장하지 않습니다.
- **`세션`(Session)**: `함수.저장`/`함수.더하기`/`함수.객체저장` 등으로
  기록한 값들이 이후 이벤트가 와도 계속 남아있는 저장소. 실제 저장 위치는
  `ExeCore::Session` 객체(`EXECORE.h:11-36`)이며, 세션 변수는
  `POOL::POOLDATA`(플랫 키-값), 세션 객체(`m_subObj`)/세션 리스트
  (`m_arrObjs`)는 각각 별도 저장소를 가집니다.

**왜 이 구분이 필요한가**: Telegram으로 "GitHub 이슈 확인해줘"라고 물으면,
Telegram이 보낸 `수신메시지`는 GitHub 조회 응답이 돌아올 때쯤엔 이미
사라지고 없습니다. 하지만 "누구에게 회신해야 하는지"(`pending_reply_chat_id`)는
GitHub 조회가 끝날 때까지 살아있어야 합니다 — 그래서 `함수.저장`으로
**세션**에 옮겨 담아야 합니다(3부 3.4절 실제 예시 참고).

## 1.3 NAMESPACE 표기법 — ⚠️ 정의는 필수, 호출은 생략 (실측으로 확정, 설계 문서와 다름)

`.sce` 파일의 모든 클래스(처리::/전송::/타이머::/문장::)는 정의할 때
**반드시** `클래스::NAMESPACE.이름` 형태로 네임스페이스를 붙여야 합니다.
`libUtil/parser/CLASSPARSER.cpp`의 상태 기계(96-275줄)는 `::` 다음에
네임스페이스가 비어 있는 경로를 아예 허용하지 않습니다.

```
처리::TELEGRAM.텔레그램초기화 { ... }     ← 정의: NAMESPACE 필수
전송::GITHUB.깃허브이슈조회 { ... }       ← 정의: NAMESPACE 필수
```

> ⚠️ **불일치**: `설계원칙_처리전송타이머문장_NAMESPACE확장.md`(2026-09-07
> 확정본)는 "정의와 호출은 항상 완전히 동일한 문자열(`TELEGRAM.procResponse`)을
> 사용해야 한다"고 명시했지만, **실제 파서/인터프리터도, 실제 배포된
> `rest.sce`도 이 규칙을 구현하지 않았습니다.** `ProcParser`/`ActionParser`/
> `StmtParser`는 파싱한 객체를 **네임스페이스를 뺀 순수 이름만으로**
> 리스트에 등록합니다(`setkey((KCSTR)m_oName)`, `PROCPARSER.cpp:259`).
> 실행 시 조회 함수(`ExeCore::FINDPROC`/`FINDACTION`/`FINDSTMT`,
> `EXECORE.cpp:235-255`)는 애초에 네임스페이스 인자를 받지도 않습니다 —
> 시그니처 자체에 없습니다. 호출부 코드(`EXEPROC.cpp:79-80` 등)도 원본
> 실행 라인에서 이름만 뽑아 바로 `FINDPROC()`에 넘깁니다.
>
> `rest.sce` 769줄 전체를 검사해도 `처리.TELEGRAM.이름` 같은
> 네임스페이스-포함 호출은 **단 한 건도 없습니다.** 실제로 쓰이는 형태는
> 전부 아래처럼 이름만입니다:
>
> ```
> 처리::FLOW.procRestInit
> {
>   만약에(참)
>     처리.텔레그램초기화        ← 정의는 TELEGRAM.텔레그램초기화인데 호출은 접두사 없음
> }
> ```
>
> **결론(실제 동작 기준)**: `::`는 정의할 때만 나타나며 NAMESPACE를
> 반드시 포함합니다. `.`은 참조/호출할 때만 나타나며, **NAMESPACE 없이
> 이름만** 씁니다. 이름은 시나리오 파일 전체에서 클래스 종류·네임스페이스와
> 무관하게 **하나의 평평한(flat) 전역 이름공간**을 공유합니다(중복 이름
> 검사도 클래스/네임스페이스를 구분하지 않고 전부 대조 — `SceParser::PARSE`).
> 즉 서로 다른 네임스페이스라도 **이름 자체는 파일 전체에서 유일해야
> 합니다.** (이 문서 2부 2.1절에서 문법을 정확히 정리합니다.)

이 절이 확정한 것은 어디까지나 **"무엇이 실제로 그렇게 동작하는가"**라는
사실 관계입니다. **"왜 애초에 이렇게 설계했는가"**는 소스코드를 아무리
들여다봐도 나오지 않는 질문이며, 이어지는 1.4절에서 원 설계자가 직접
밝힌 의도와 함께 다룹니다.

## 1.4 NAMESPACE는 "소속 표시"일 뿐 "접근 제어"가 아니다 — 왜 이렇게 설계했는가 (원 설계자 확인, 2026-09-19)

`설계원칙_처리전송타이머문장_NAMESPACE확장.md`가 강조한 이 원칙은
**소스코드로 정확히 재확인되었습니다.** `처리::TELEGRAM.텔레그램깃허브명령분기`
안에서 `처리.깃허브테스트처리`(GITHUB 소속)를 아무 제약 없이 호출할 수
있습니다 — 어떤 코드 경로에도 "호출자의 네임스페이스가 피호출자의
네임스페이스와 같아야 한다"는 검사가 없습니다. 애초에 호출부에는
네임스페이스 정보 자체가 실려 있지 않으므로(1.3절), 접근 제어를 걸고
싶어도 걸 수 있는 지점이 없습니다.

**실제로 NAMESPACE가 코드에서 의미를 갖는 유일한 지점**은
`상태::`(STATE) 조건문의 맨 앞 세그먼트뿐입니다(2.2절에서 상세히 다룹니다).
`ExeCore::isMatchDomain()`(`EXECORE.cpp:168-208`)이 그 세그먼트 문자열을
`addr.ini`의 `[NAMESPACE]` 섹션 `domain` 키와 대조합니다. 이 대조는
**정확 일치가 아니라 앞자리(prefix) 일치**(`STRNCMP`)입니다 — 이는
`설계원칙_NAMESPACE표기법_STATE조건.md`가 "Discord처럼 도메인이 전체
웹훅 URL로 echo되는 경우 정확 일치가 안 될 위험"으로 우려했던 지점을
오히려 유리하게 해소합니다. 그리고 `FLOW` 네임스페이스는 도메인 대조
자체를 건너뛰고 항상 통과하도록 하드코딩되어 있습니다(그 문서가 "구현
시 확정"으로 남겨뒀던 질문의 답입니다).

**진입점(`상태::`)에 NAMESPACE가 필요한 이유는 사실 두 가지가 겹쳐
있습니다.** 하나는 방금 설명한 기술적 필요 — 서로 다른 여러 서비스의
응답이 뒤섞여 들어오는 유일한 관문에서 "이 요청이 어디서 왔는지"를
가려내는 라우팅입니다. 다른 하나는 소스코드만 읽어서는 절대 드러나지
않는 이유로, 원 설계자(dorafather)가 2026-09-19에 직접 설명해 준
것입니다: **사용자가 "복잡한 브릿지 시나리오" 전체 안에서 지금 자신이
편집하고 있는 게 어느 서비스 영역인지 한눈에 구분할 수 있게 하려는 UX
설계 의도**입니다. 웹 UI(`frontend/src/components/ProcTab.jsx`,
`SendTab.jsx`)가 처리::/전송:: 정의 화면 맨 앞에 네임스페이스 뱃지를
항상 고정 표시하는 것이 정확히 이 역할이며, 실제로 두 파일 모두에서
`<span className="ns-badge">{ns}</span>` 형태로 구현되어 있음을 코드로
확인했습니다.

**반대로 호출부(수행문)에 NAMESPACE가 없는 건 "생략해도 되니까 생략한"
우연이 아니라, "다양한 조합을 자유롭게 허용하기 위해 애초에 그 표기
자체를 요구하지 않도록 설계한 문법"입니다.** 이 프로젝트의 정체성이
"공통 브릿지 파이프라인 지휘자"이기 때문입니다 — 일단 진입점을 통과해
`처리::` 블록 안으로 들어오면, 그 안에서는 "어느 서비스 소속인가"가
더 이상 중요하지 않습니다. TELEGRAM 처리:: 안에서 GITHUB 처리::를,
그 안에서 다시 SLACK 전송::을 자유롭게 위임해 쓸 수 있어야 하고, 문법이
그런 조합을 가로막지 않도록 만들어진 것입니다. 이 의도는 웹 UI에도
그대로 반영되어 있습니다 — 수행문을 고르는 콤보박스
(`ProcTab.jsx`의 `procCandidates`/`sendCandidates`/`timerCandidates`)는
"지금 편집 중인 서비스의 목록"만 보여주지 않고, `restConfig`의
`procedure-list`/`action-list` 전체에서 네임스페이스로 거르지 않은 채
**등록된 전체 처리::/전송:: 카탈로그**를 보여주도록 만들어져 있습니다
(옵션 값은 이름만, 표시 라벨만 `{ns}.{name}`으로 소속을 알려줄 뿐입니다).
사용자가 언제든 다른 서비스의 처리문을 자유롭게 끌어다 쓸 수 있도록
하기 위한 설계이며, 실제 프론트엔드 코드로 재확인된 사실입니다.

> **참고(방법론적 교훈)**: 소스 검증(1.3절)은 "정의부만 NAMESPACE,
> 호출부는 이름만"이라는 **사실**을 정확히 잡아냈습니다. 하지만 그
> 검증만으로는 "왜 호출부엔 경계가 없는가"라는 **의도**까지는 알 수
> 없었습니다 — 실제로 이전 설계 문서(2026-09-07)는 이 지점의 의도를
> 정반대로 짐작해 "정의와 호출은 항상 완전히 동일한 문자열을 써야
> 한다"고 잘못 못박았던 전력이 있습니다. 소스코드는 "무엇이 사실인가"를
> 확정하는 데는 최선의 방법이지만, "왜 그렇게 만들었는가"라는 설계
> 의도는 코드만으로 재구성할 수 없는 별개의 질문입니다. 의도가
> 불분명한 지점을 소스 검증 결과만으로 확신하지 말고, 원 설계자에게
> 직접 확인하거나 최소한 "확인 필요"로 남겨두는 편이 안전합니다.

이 철학은 4부(자연어→DSL 변환 가이드)의 전제와 직결됩니다: 새 연동을
만들 때도 `처리::` 블록 안에서는 다른 네임스페이스의 처리::/전송::을
거리낌 없이 조합해 써야 하며(4.3절의 "서로 다른 두 서비스 위임" 패턴이
정확히 이 경우입니다), 여기에 "네임스페이스가 같아야 호출 가능하다"
같은 인위적 제약을 새로 만들려 해서는 안 됩니다 — 그건 실제 구현과도,
이 프로젝트가 지켜온 설계 의도와도 어긋납니다.

## 1.5 세션 변수가 NAMESPACE 예외인 이유 — 공용 공간 vs 격리된 부품

`함수.저장`/`함수.더하기`/`함수.객체저장` 등이 다루는 세션 변수·세션
객체·세션 리스트에는 애초에 `::` 정의 문법 자체가 없습니다(런타임에
즉석으로 생성됨). 이건 설계 실수가 아니라 **의도된 예외**입니다.

```
처리::/전송::/타이머::/문장::  →  각 소속 내부의 "독립 부품" (이름 충돌 방지가 안전)
세션 변수                      →  여러 소속이 함께 쓰는 "공용 공간"
```

3부 3.4절(Telegram↔GitHub 조합)이 실제 사례입니다: Telegram 쪽 처리::가
`함수.저장(pending_reply_chat_id, ...)`로 세션에 적어두고, GitHub 쪽
처리::가 조회를 마친 뒤 `세션.pending_reply_chat_id`를 그대로 읽어
Telegram으로 회신합니다. 세션 변수에 네임스페이스를 강제했다면 GITHUB
코드가 TELEGRAM 소유의 값을 읽는 게 "남의 네임스페이스 침범"이 되어
버려, 서비스 간 조합 자체가 성립하지 않습니다.

## 1.6 addr.ini 메타 필드 확장 패턴 — ⚠️ "카탈로그::" 클래스는 없다, 그러나 목표는 다른 방식으로 달성됐다

`설계원칙_Default시나리오_카탈로그선언.md`는 각 서비스가 `카탈로그::SLACK
{ 카테고리 = 알람; 문구필드 = text; 대상전송 = 슬랙알람전송 }` 같은
**새로운 `.sce` 클래스**로 자기 자신을 설명하게 만들자는 제안이었습니다.

> ⚠️ **불일치**: `카탈로그`는 `CLASSPARSER.h/.cpp`가 인식하는 7개 클래스
> 키워드(2.1절) 어디에도 없습니다. `.sce` 파일 전체를 검색해도 `카탈로그::`
> 블록은 0건입니다. **이 제안은 실제로 구현되지 않았습니다.**
>
> 대신 실제로 벌어진 일: 제안이 담으려 했던 바로 그 필드들(카테고리/
> 문구필드/대상전송, 그리고 제안 문서에도 없던 `telegram_alias`까지)이
> **기존에 이미 있던 `설정::`(INI, 즉 `addr.ini`)의 일반 key=value
> 문법**에 그냥 자유 필드로 추가되었습니다:
>
> ```ini
> [SLACK]
> webhook_url=https://hooks.slack.com/services/...
> domain=https://hooks.slack.com
> category=알람
> message_field=text
> target_action=슬랙알람전송
> telegram_alias=슬랙
> ```
>
> `IniParser::PARSE`는 순수 `[섹션]`/`key=value` 라인 리더로, 인식
> 가능한 키 목록(allowlist)이 소스 어디에도 없습니다 — 그래서 이런
> 자유 필드 추가가 **C++ 소스 변경 없이** 실제로 가능했던 것이며, 이
> 점에서 "카탈로그가 커져도 코드는 안 늘어난다"는 제안 문서의 목표
> 자체는 달성됐습니다. 다만 그 실현 방식은 새 클래스 문법이 아니라
> **기존 `설정::`(INI) 파일의 자유 확장**이었다는 게 핵심 정정입니다.
>
> **더 중요한 발견**: 이 메타 필드들(`category`/`message_field`/
> `target_action`/`telegram_alias`)을 **DSL 엔진 자신은 런타임에 전혀
> 읽지 않습니다.** `Main.cpp`/`EXE*.cpp` 어디에도 이 필드 이름 리터럴이
> 등장하지 않습니다(직접 grep 확인). 실제 소비자는 **DSL 엔진이 아니라
> 별도 Python 프로세스인 `mcp-flow`**입니다 — `mcp_flow.py`가
> `GET /notebookflow/config/addr`로 `addr.ini` 전체를 JSON으로 받아와
> `category=="알람"`인 섹션만 골라 자체적으로 해석합니다. 즉 "설계
> 의도상 제안됐던 `GET /notebookflow/catalog` 전용 API"도 별도로 만들지
> 않고, **이미 있던 범용 addr 조회 API를 그대로 재사용**했습니다. 이건
> "하드코딩 없이 카탈로그를 키운다"는 철학 자체는 지켜졌지만, **그
> 카탈로그를 실제로 소비하는 건 DSL 엔진이 아니라 mcp-flow 하나뿐**이라는
> 뜻입니다. `rest.sce`의 Telegram 명령 분기나 help 텍스트는 이 필드들을
> 전혀 쓰지 않습니다(3부 3.5, 3.9절에서 구체적으로 다룹니다).

## 1.7 통신 계층 4원칙 — JSON 내용물 불간섭 (검증 결과: 대체로 유지, 최근 긴장 사례 1건 발견)

`설계원칙_통신계층_JSON내용물불간섭.md`의 핵심 원칙은 여전히 유효합니다:

1. **나갈 때**: 시나리오가 붙인 라우팅 메타(`METHOD` 등)는 통신 계층이
   소비하고, 외부로 나가는 실제 바디에서는 제거한다.
2. **들어올 때**: 통신 계층은 라우팅에 필요한 메타를 "추가"만 한다(원래
   값을 변조하지 않는다).
3. **봉투/페이로드 구조 자체는 통신 계층이 판단하지 않는다** — "이건
   규격이니 벗겨서 보내고, 저건 비규격이니 그대로 보낸다" 같은 분기가
   생기면 원칙 위반.
4. **세션 생명주기 이원화**: Tray-발화 세션(START_REQ/AS_ID/SCE_ID,
   File-Flow 계열)과 REST-브릿지(NonProto, 세션 개시 규약 없음, `rest.sce`
   계열)는 서로 다른 생명주기를 쓴다.

**규칙 4는 코드로 정확히 재확인됩니다.** `libUtil/flow/FLOW.cpp`를 직접
읽으면:

- `Flow::procStartReq()`(256-368줄)가 Tray-발화 세션 경로 —
  `"IC-"+AS_ID`로 키를 만들어 `POOL::POOLDATA`를 새로 할당합니다.
- `Flow::procNonProto()`(522-541줄)가 REST-브릿지 경로 — `시작` 이벤트가
  오면 **전역 단일 풀(`m_gPool`)**을 초기화(`m_gPool.CLEAR()`)하고
  `SCE_ID="NonProto"`로 고정합니다. `AS_ID`/`START_REQ` 같은 개념 자체가
  아예 등장하지 않습니다.

**규칙 3(내용물 불간섭)에 대한 최근 긴장 사례**: 이 원칙의 사례 연구
자체가 2026-09-05에 "SSE 연결 이벤트를 통신 계층이 스스로 판단해 이벤트를
창작하는 건 위반"이라며 되돌린 전력이 있습니다. 그런데 2026-09-17~18에
추가된 `Main.cpp`의 `App::ACTION()`은 `_msg.GET("폴더")`(로컬 디렉토리
목록 조회)와 `_msg.GET("헬프이미지")`(GDI+ 이미지 생성)를 **외부 HTTP
호출이 전혀 없는, 순수 로컬 사이드이펙트**로 처리하며, 이건 정확히
"JSON 최상위 키의 유무로 통신 계층이 분기한다"는 모양입니다. `문서`
(multipart 트리거) 필드는 "무엇을 어떻게 보낼지"를 가리키는 라우팅
메타데이터로 볼 여지가 있어 원칙과 비교적 잘 어울리지만, `폴더`/
`헬프이미지`는 외부 요청 자체가 없는 순수 로컬 동작이라 "통신 계층이
추가/제거하는 메타데이터"라는 기존 정당화 틀에 깔끔하게 들어맞지
않습니다. **이 문서는 이를 위반이라고 단정하지는 않지만, 원칙이 진화하는
중이며 아직 명문화되지 않은 지점이라는 사실을 사용자에게 알려드립니다.**
새 로컬 전용 동작(파일 읽기, 이미지 생성 등)을 추가할 때는 이 패턴
(최상위 키 존재 여부로 조기 분기 + 즉시 return)을 참고하되, 이게
"메타데이터 추가"의 연장인지 스스로 판단해 보는 게 안전합니다.

## 1.8 1차 사용자 수행문 — 실제로는 훨씬 많은 함수가 존재한다

업무지침 초안은 "함수.저장/함수.주소저장/전송/타이머/처리" 정도로
1차 수행문을 제한했지만, **실제 엔진(`EXEFUNC.cpp`)에는 22개의 `함수.*`
인라인 함수가 이미 구현되어 있습니다** — 저장/더하기/빼기/나누기/
나머지/붙이기/부분비교/앞자리비교/삽입/삭제/추출/시간/클럭/출력/날짜/
컴프리헨션/반복/객체저장/주소저장/쪼개기/단어분리/단어합치기. 전체
목록과 정확한 동작은 2부 2.5절 표를 참고하세요. 자연어 요청을 DSL로
옮길 때는 "1차 제한된 소수"가 아니라 이 전체 카탈로그에서 적합한 함수를
고르는 게 정확합니다.

## 1.9 재귀 호출 상한 — 무한 루프에 대한 안전장치

`ExeCore::Session::exeCnt()`(`EXECORE.cpp:27-32`)는 한 세션이 한 이벤트
처리 사이클 안에서 **200회**까지만 `처리::` 호출(재귀 포함)을 허용합니다.

```cpp
bool ExeCore::Session::exeCnt()
{
    if(m_callCnt >= 200) return false;
    m_callCnt++;
    return true;
}
```

이건 "3부 3.3절"의 GitHub 이슈 순회처럼 `처리::`가 자기 자신을 재귀
호출하며 배열을 순회하는 패턴에 실질적인 상한이 있다는 뜻입니다 — 이슈가
아주 많아지면(대략 190여 개 이상) 이 카운터에 걸려 조용히 멈출 수
있습니다. 4부 체크리스트에서 다시 언급합니다.

## 1.10 AX 소통 — 이 프로젝트가 커뮤니티를 향해 여는 문 (2026-09-19)

NotebookFlow는 dorafather님이 혼자 쓰는 자동화 도구로 시작했지만, 이
브릿지 자체를 공개해 **사람과 AI 에이전트가 함께 만들어가는 앱
생태계**로 넓히겠다는 다짐이 2026-09-19에 확정되었습니다("이 API
브릿지는 AX 소통이다"). 3.3.1절의 GitHub 이슈 등록/폴링 기능은 이
다짐을 실제 코드로 옮긴 첫 사례이며, 앞으로 이 저장소에 기여하거나
피드백을 주는 통로도 전부 이 방식(사람이 Claude Code에게 자연어로
요청 → DSL 브릿지가 실행)을 따르게 됩니다.

이 프로젝트를 쓰는 사람이라면 누구나(관리자든 커뮤니티 사용자든) 아래
문구를 실제로 마주치게 됩니다 — NotebookFlow 기동 시 Telegram 환영
메시지(`문장::TELEGRAM.텔레그램환영문구`)와 `help` 명령
(`문장::TELEGRAM.텔레그램help안내문구`) 양쪽에 반영되어 있습니다:

> 이 API 브릿지는 무한히 확장됩니다. Slack, Discord, GitHub처럼 이미
> 연결된 서비스뿐 아니라, 세상의 어떤 REST API든 — Claude Code에게
> "이 서비스도 연동해줘"라고 말만 하면, Claude가 NotebookFlow의
> 스크립팅 언어(Flow)로 직접 시나리오를 작성해 즉시 서비스를
> 만들어냅니다. 당신만의 자동화가 지금 이 순간부터 시작됩니다.
>
> NotebookFlow를 쓰다가 불편한 점, 버그, 개선 아이디어가 있다면 언제든
> Claude Code에게 "GitHub에 이슈로 등록해줘"라고 말씀해주세요. (GitHub
> 계정과 개인 액세스 토큰이 필요합니다 — 처음 한 번만 설정하시면
> 됩니다)
>
> dorafather와 샛별이가 여러분의 이슈에 적극적으로 소통할 것을
> 약속합니다. 여러분의 목소리가 NotebookFlow를 함께 만들어갑니다.

**등록에 필요한 토큰은 관리자 토큰이 아니라 각자의 GitHub 계정
토큰입니다** — 3.3절의 조회 기능이 이미 쓰고 있는 `addr.ini`의
`[GITHUB].token`과 완전히 같은 필드를 재사용합니다(새 메커니즘을 만들지
않았습니다). 이 프로젝트를 내려받아 자기 컴퓨터에서 직접 돌리는 사람은
자신의 Personal Access Token을 이 자리에 채워 넣으면 조회와 등록 둘 다
자기 계정 권한으로 동작합니다 — GitHub가 토큰 없는 이슈 생성을
막는다는 사실 자체를 그대로 안내할 뿐, 이 프로젝트가 별도 인증
계층을 추가로 얹지는 않습니다.

**앞으로 새 소셜/외부 서비스 연동을 추가할 때도(2026-09-19 결정)**,
API 토큰/ID 같은 자격증명은 별도 웹 설정 화면을 새로 만들지 않고
**Telegram 대화로 Claude Code(agent-flow)가 직접 물어보고 `addr.ini`에
써주는 방식**을 표준으로 삼습니다 — Telegram이 상대적으로 안전한
채널이라는 판단에 따른 것이며, DSL 로직(처리::/전송::)을 Claude Code가
대화로 작성하는 것과 같은 결의 온보딩 경험입니다.

**GitHub 이슈 조회/등록은 지금은 NotebookFlow 자체 저장소
(`dorafather/NotebookFlow`) 대상으로 고정되어 있습니다(2026-09-19 확정,
3.3.1/3.3.2절 참고)** — 이걸 숨기지 않고 명확히 드러내는 쪽을 택했습니다.
당신 자신의 GitHub 저장소를 연동하고 싶다면, 그건 이 Default 카탈로그의
범위가 아니라 **Claude Code에게 직접 요청**해 만들면 됩니다 — Telegram으로
"GitHub 연동을 내 저장소로 바꿔줘" 또는 "이 저장소도 추가해줘"라고
말씀해보세요. 위에서 설명한 "맥락 없는 새 대화도 CLAUDE.md 하나로 새
연동을 정확히 만들어낸다"는 능력을 그대로 활용해, 새로운 연동(또는 기존
연동의 저장소 교체)을 직접 만들어 드립니다.

## 1.11 NOTEBOOKFLOW_BOT 커뮤니티 채널 — Claude 에이전트들의 소통 통로 (2026-09-21)

1.10절의 "AX 소통" 비전이 두 번째 층으로 확장되었습니다. GitHub 이슈
채널이 "사용자가 Claude Code에게 요청 → Claude가 GitHub API로 소통"
하는 통로였다면, `NOTEBOOKFLOW_BOT`은 **각 사용자의 Claude 에이전트
자신이 참여 주체가 되는 별도의 Telegram 채널**입니다(`addr.ini`의
`[NOTEBOOKFLOW_BOT]` 섹션, `rest.sce`의 `NOTEBOOKFLOW_BOT` 네임스페이스로
구현 — dorafather님 개인용 `[TELEGRAM]` 채널과는 완전히 별개의 봇/그룹
입니다).

**만약 당신(Claude)이 사용자를 도와 새로운 REST API 연동을 성공적으로
완성했다면, `NOTEBOOKFLOW_BOT` 커뮤니티 채널을 통해 공유하는 것을
고려해보세요. 좋은 시나리오는 검토를 거쳐 NotebookFlow 공식 Default
카탈로그에 반영될 수 있습니다.**

> 참고(2026-09-23 갱신): 이 채널의 봇 토큰/chat_id는 이제 확정되었습니다 —
> `config/addr.ini.template`의 `[NOTEBOOKFLOW_BOT]` 섹션에 실제 값이
> 미리 채워져 배포됩니다(다른 서비스 섹션과 달리 이 섹션만 예외입니다.
> 이유는 아래 1.12절 참고). 즉 이 프로젝트를 내려받은 모든 사용자의
> Claude는 별도 설정 없이 즉시 이 채널로 공유할 수 있습니다.

## 1.12 시나리오 작성 안전 정책 — 금지사항과 공유 절차 (2026-09-23)

1.11절의 커뮤니티 채널이 실제로 열리면서, "시나리오를 자유롭게 공유해도
법적/윤리적으로 안전한가"를 먼저 확립해야 했습니다. 아래 두 가지는 예외
없는 금지 규정이며, 이 둘이 지켜지는 한에서만 시나리오 공유가 안전합니다.

**반드시 지켜야 할 두 가지 금지사항:**

① **사용자의 Claude 에이전트에게 강제적으로 명령을 내리는 시나리오를
작성하지 않는다.** 어떤 시나리오도 다른 사용자의 Claude Code를
원격으로 조종하거나 특정 행동을 강제하는 목적으로 설계되어서는 안
됩니다 — 법적/윤리적 문제가 됩니다.

② **사용자 모르게 시나리오상에서 Agent를 자동 호출하도록 만들지
않는다.** `전송::AGENT.*` 계열(클루드코드 명령 전송 등)을 사용자의
명시적 트리거(Telegram 명령, 수동 요청) 없이 타이머나 다른 이벤트에
연쇄적으로 걸어 자동 실행되게 하는 설계는 금지입니다 — Agent 호출은
토큰 비용을 발생시키므로, 사용자 동의 없이 비용이 발생해서는 안
됩니다.

**"공유하시겠어요?" 제안 절차 (강제가 아니라 제안 형태):**

당신(Claude)이 사용자를 도와 새로운 REST API 연동을 성공적으로
완성했다면, 사용자에게 직접 물어보세요:

> "멋진 시나리오를 공유하시겠어요?"

사용자가 동의(Yes)한 경우에만 `NOTEBOOKFLOW_BOT` 커뮤니티 채널(1.11절)로
공유를 진행하세요. **절대로 사용자 동의 없이 자동으로 공유하지
마세요.** 이 절차는 각 사용자의 Claude가 매번 자율적으로 판단해
사용자에게 묻는 방식으로만 존재합니다 — 강제성이 없으므로 위 ①·②
금지사항과 함께 법적/윤리적 문제를 회피합니다.

**공유는 텍스트 메시지가 아니라 파일(문서)로 전달합니다.** 아래 소통
맥락 주석과 시나리오 코드를 담은 `.txt` 또는 `.sce` 파일을 만들어,
2.12절의 `전송메시지.문서.*`(sendDocument) 패턴으로 `NOTEBOOKFLOW_BOT`
그룹에 전송하세요 — 이렇게 형식을 파일로 통일해두면, 1.13절의
"가져오기" 절차가 이미 검증된 "Telegram 파일 수신 시 자동 저장"
기능(3.8절)을 그대로 재사용할 수 있습니다.

**공유 시 시나리오 주석 형식 — 소통 맥락을 남길 것:**

공유되는 시나리오(`처리::`/`전송::` 블록)에는, 왜 이렇게 만들었는지에
대한 짧은 `/* ... */` 주석(Claude와 사용자 간 소통 맥락 요약)을 함께
포함해 전달하세요. 이 프로젝트가 지켜온 "개발 배경을 기록한다"는 습관을
커뮤니티 공유에도 동일하게 적용하는 것입니다. 예:

```
/* 사용자 요청: "Mastodon에 알림 보내고 싶다"는 대화에서 시작.
   Bluesky 알람 패턴(로그인→포스팅 2단계)을 참고해 구현. */
```

> ⚠️ 주의: `rest.sce`는 React 프론트엔드에서 "저장"을 누르면 구조화
> 데이터로부터 재직렬화되며 파일 내 모든 주석이 사라집니다(자세한 내용은
> `rest_devnotes.txt` 서두 참고). 따라서 위 주석은 **로컬 `rest.sce`에
> 영구히 남기기 위한 것이 아니라, 커뮤니티 채널에 붙여넣는 공유 텍스트
> 자체에 포함시키는 것**이 목적입니다 — 공유 시점의 대화 텍스트(Telegram
> 메시지 본문 등)에 시나리오 코드와 함께 이 주석을 그대로 붙여 넣으세요.

**새 명령어를 추가했다면 help.json도 함께 갱신할 것:**

사용자(또는 Claude)가 새로운 명령어를 `rest.sce`에 추가했다면, 같은
내용을 `NotebookFlow/help.json`에도 반영해야 `help` 명령(사진으로 생성되는
도움말 이미지)의 목록에 표시됩니다. `help.json`은 `{"rows":[{"cmd":...,
"desc":...}, ...]}` 형태의 단순 배열이며, `Main.cpp`의 `HelpFileReader`가
1초 간격 폴링으로 자동 갱신하므로(파일만 저장하면 재시작 없이 반영)
`rest.sce`/`addr.ini`와 동일한 핫리로드 습관을 그대로 적용하면 됩니다.
배포 패키징(`installer/build_installer.ps1`)의 `bin\notebookflow\` 조립
단계에도 `help.json`이 포함되어 있으니, 새 배포판을 만들 때 누락 여부를
따로 걱정할 필요는 없습니다.

이 명령 라우팅의 실제 구현 예시가 `처리::TELEGRAM.텔레그램고객문의명령처리`
("고객문의 {문의내용}" → `NOTEBOOKFLOW_BOT` 커뮤니티 그룹 전달, 2026-09-23
추가)입니다 — 개인 `TELEGRAM` 네임스페이스에서 받은 명령을 이미 검증된
"네임스페이스 경계를 넘는 자유 호출" 원칙(1.10절의 Telegram↔GitHub 조합과
동일한 패턴)으로 `NOTEBOOKFLOW_BOT`의 기존 `전송::NOTEBOOKFLOW_BOT.NF봇그룹
메시지전송`을 그대로 재사용해 전달합니다.

## 1.13 커뮤니티 시나리오 "가져오기" 절차 — 왜 자동 다운로드가 아닌가 (2026-09-23)

1.12절이 "공유"(write)를 다뤘다면, 이 절은 그 반대 방향인 "가져오기"
(read)를 다룹니다. **핵심 원칙은 동일합니다 — 사람의 명시적 개입 없이는
아무 일도 일어나지 않습니다.**

**검토했지만 기각한 대안**: 커뮤니티 그룹에 새 시나리오 파일이 올라오면
모든 사용자의 NotebookFlow가 자동으로 감지해 자동 반영하는 방식. 이건
검증되지 않은 남의 시나리오가 사용자 동의 없이 자동 적용된다는 점에서,
1.12절의 ①·②(강제 명령 금지·동의 없는 자동 실행 금지) 원칙에 정면으로
위배되어 채택하지 않았습니다.

**확정된 절차 (전부 사람이 직접 개입해야 진행됨):**

```
① 사용자가 Telegram 클라이언트로 NOTEBOOKFLOW_BOT 그룹을 직접
   둘러보다가 원하는 시나리오 파일을 발견
② 그 파일을 Telegram의 전달(forward) 기능으로 자기 개인봇(TELEGRAM
   네임스페이스)에게 전달(또는 파일을 저장했다가 개인봇 채팅에
   다시 업로드)
③ 개인봇이 이미 갖고 있는 "파일 수신 시 자동으로 노트북에 저장"
   기능(3.8절, 처리::TELEGRAM.텔레그램파일업로드감지 계열)이 그대로
   동작 — 이 절차를 위한 새 코드는 전혀 없습니다
④ 사용자가 "방금 받은 그 시나리오, 내 것에 반영해줘"라고 자기
   Claude(agent-flow)에게 명시적으로 요청
⑤ Claude(당신)는 저장된 파일 내용을 검토합니다 — 최소한 아래를
   확인하세요:
     - NAMESPACE 규칙(1.3절): 정의부(처리::/전송::/타이머::/문장::)에
       NAMESPACE.이름이 있는지, 호출부는 이름만인지
     - 1차 수행문 제한(1.8절): 알려진 함수.* 카탈로그 밖의 낯선
       수행문이 없는지
     - 처리::/전송::/타이머:: 안의 모든 문장이 한 줄인지(4.6절 함정
       체크리스트 — 여러 줄이면 시나리오 전체가 깨지고 크래시까지
       날 수 있습니다)
     - 1.12절 금지사항(강제 명령/동의 없는 Agent 자동호출)을 위반하는
       코드가 섞여 있지 않은지
⑥ 검토 결과를 요약해 사용자에게 **반드시 다시 물어보세요**: "이 시나리오를
   내 rest.sce에 반영할까요?" — **사용자가 동의한 뒤에만** 실제로
   반영하세요. 자동 반영은 금지입니다(1.12절과 동일한 원칙).
```

**요약**: "공유"든 "가져오기"든, 커뮤니티 채널을 오가는 모든 시나리오는
반드시 사람의 명시적 동의 한 번을 거쳐야만 실제 코드에 영향을 줄 수
있습니다. Claude는 파일을 검토하고 제안할 뿐, 스스로 판단해 조용히
반영해서는 안 됩니다.

## 1.14 설치 시 "전용 Claude Code 에이전트" 자동 생성 (2026-09-25)

3.6절의 "클루드코드" 지속 세션이 그냥 평범한 Claude가 아니라 **이
프로젝트의 rest.sce/CLAUDE.md를 아는 전담 개발 담당자**로 응답하게
하려면, 그 역할을 정의한 커스텀 에이전트 파일이 사용자의 전역 Claude
Code 설정 폴더(`%USERPROFILE%\.claude\agents\`)에 있어야 합니다. 이걸
사용자가 직접 손으로 만들게 하는 건 비개발자에겐 사실상 불가능한
일이므로, **설치 마법사가 대신 만들어주는 옵션**을 추가했습니다.

```
설치 마법사 작업(Task) 하나 추가: "전용 Claude Code 에이전트 생성
  (Telegram "클루드코드"로 자유롭게 대화하려면 권장)" — 기본 체크(ON).

체크된 경우 설치 중(ssPostInstall):
① NotebookFlow/notebookflow-agent-template.md(범용 페르소나, 특정
   설치 경로나 개인 이름을 하드코딩하지 않음 - "현재 작업 디렉터리의
   CLAUDE.md를 읽으라"는 식으로 항상 상대적으로 서술)를
   %USERPROFILE%\.claude\agents\notebookflow-agent.md로 복사
   (이미 있으면 절대 덮어쓰지 않음 - 사용자가 직접 고쳤을 수 있으므로).
② config/addr.ini.template의 agent_name=(빈 값) 줄을
   notebookflow-agent로 채움 - 이후 addr.ini가 언제 만들어지든(설치
   즉시든, NotebookFlow.exe 최초 실행 시 자동 생성이든) 그 값이 그대로
   반영된다.

체크 안 하면 agent_name이 계속 비어있고, "클루드코드"는 평범한
Claude로 계속 동작한다(3.6절 - agent_name이 비면 --agent 플래그 자체가
생략되므로 안전).
```

**⚠️ 안전장치가 꼭 필요했던 이유**: `--agent`에 존재하지 않는 이름을
넘기면 `claude -p` 호출 전체가 그냥 실패합니다(폴백 없음, 실측 확인).
그래서 "체크 안 함 = agent_name 필드 자체가 빈 문자열"이 되도록 설계한
것이 핵심입니다 — 어설프게 "기본 에이전트 이름"을 하드코딩해뒀다가
그 파일이 없는 상태로 남으면 "클루드코드" 명령 자체가 항상 실패하는
사고로 이어집니다.

**주의(개발 PC 한정)**: 이 프로젝트를 만든 dorafather 개인 개발
환경에는 이것과 별개로 `flow-dev-lead`라는 이름의 개인용 실험
에이전트가 이미 있습니다(`~/.claude/agents/flow-dev-lead.md`,
`NotebookFlow/addr.ini`의 `agent_name=flow-dev-lead`) — 파일명이
달라서 이번에 만든 범용 `notebookflow-agent`와 서로 충돌하지 않고
공존합니다. 새로 이 저장소를 내려받아 빌드하는 사람에게는 해당사항
없는, 순전히 이 개발 환경의 히스토리입니다.

## 1.15 GitHub Pull 방식 업데이트 확인 + "업데이트" 명령 (2026-09-26)

1.10~1.13절의 "AX 소통" 철학(별도 프로비저닝 서버 없이 GitHub 저장소
자체를 24시간 서빙되는 설정 저장소로 재사용, 사람의 명시적 동의 없이는
아무것도 자동 반영하지 않음)을 "공식 배포분 자체의 최신화"에도 그대로
적용한 기능입니다. 사용자가 설치한 각자의 NotebookFlow가 공식 저장소
(`dorafather/NotebookFlow`)에 새 커밋이 올라온 걸 스스로 감지해
Telegram으로 알리고, 사용자가 "업데이트"라고 답하면 그때만 Claude
Code(agent-flow)를 호출해 실제로 반영합니다.

**감지 방식 — 설치 시각 기반 자연 분산**: 별도 무작위 생성 없이, 설치
시점 자체가 이미 자연스럽게 무작위라는 특성을 그대로 활용합니다.
`NotebookFlow.exe`가 최초 부팅 시 1회만 그 순간의 로컬 "시(0~23)"를
`addr.ini`의 `[INFO].install_hour`에 기록합니다(`Main.cpp`의
`s_fnEnsureInstallHourRecorded()` — 이미 값이 있으면 재기동해도 절대
덮어쓰지 않습니다, 2026-09-26 실측 확인 — 실제 재기동 전후로 값이
그대로였습니다). `rest.sce`의 `타이머::UPDATE.업데이트확인타이머`가
1시간마다(네트워크 요청 없이) `함수.날짜(x,%H)`로 뽑은 현재 시각과
`install_hour`를 비교하고, 정확히 일치할 때만(하루 1회) 실제로
`GET https://api.github.com/repos/dorafather/NotebookFlow/commits/main`을
조회합니다 — 그 결과의 `수신메시지.sha`가 `설정.INFO.last_applied_version`과
다르면 Telegram으로 "새 업데이트가 있습니다. '업데이트'라고 답하면
반영을 시작합니다"를 보내고, 같으면 아무 일도 하지 않습니다. 사용자마다
설치 시각이 제각각이므로 전체 사용자 기준으로는 GitHub 조회 요청이
하루 24시간에 걸쳐 자연스럽게 분산됩니다.

**"업데이트" 명령 — 실제 반영**: 사용자가 Telegram에서 "업데이트"라고
답하면(`처리::TELEGRAM.텔레그램업데이트명령처리`), 최신 커밋을 다시
확인한 뒤(자동 확인 흐름과 동일한 `전송::UPDATE.업데이트버전확인전송`을
재사용하되 `세션.update_mode`를 `확인`/`실행`으로 구분해 분기), 공식
저장소의 `rest.sce`/`CLAUDE.md`를 `저장.파일명`(2.12절의 파일 다운로드
메커니즘)으로 `downloads/official_rest.sce`/`downloads/official_CLAUDE.md`에
먼저 내려받은 뒤, 그 두 파일의 경로만 담은 지시문(`문장::UPDATE.업데이트지시문장`)으로
`agent-flow`를 호출합니다(`전송::UPDATE.업데이트에이전트명령전송`).

**왜 Claude가 직접 GitHub을 조회하게 하지 않았는가(중요한 설계 결정)**:
3.3.1절이 이미 실측으로 확인했듯, `agent-flow`가 띄우는 `claude -p`는
비대화형(headless)이라 사전 승인 없는 도구 호출(Bash로 `git`/`curl`을
쓰거나 WebFetch로 GitHub을 직접 조회하는 것 등)이 필요한 순간 그대로
멈춰버립니다(승인해줄 사람이 없음). 그래서 이번에도 3.3.1절과 동일한
해법을 그대로 적용했습니다 — **필요한 데이터(공식 파일 내용)를 `.sce`
단계에서 미리 로컬 파일로 받아두고, Claude에게는 "이미 받아둔 파일만
보고 비교해라, GitHub API를 직접 호출하거나 git 명령을 실행하지
마라"고 지시문에 명시**합니다. 이렇게 하면 Claude는 Read/Edit/Write
도구만으로 작업을 끝낼 수 있어 headless 실행에서도 절대 멈추지
않습니다.

**"업데이트" 호출은 반드시 무상태(stateless) 1회성**: 3.6절의 "클루드코드"
지속 세션(`session_key=claude_persistent`)과 혼동하면 안 됩니다 — 이건
대화가 아니라 1회성 유지보수 작업이므로, `전송::UPDATE.업데이트에이전트명령전송`은
`session_key` 필드를 아예 보내지 않습니다(`agent_flow.py`는
`session_key`가 없으면 자동으로 `--no-session-persistence` 무상태 경로를
탑니다 — 이 목적을 위해 `agent_flow.py` 자체를 수정할 필요가 전혀
없었습니다, 이미 그 경로를 지원하고 있었습니다). 2026-09-26 실측:
같은 요청 형태(`instruction`+`callback_chat_id`+`agent_name`, `session_key`
없음)로 직접 호출해 `session_key: null`, 정상 완료, 그리고
`agent_flow_sessions.json`에 새 항목이 전혀 생기지 않음을 확인했습니다.

**공식분 vs 사용자 커스터마이즈 구분 원칙 — Claude가 "업데이트" 실행 시
반드시 지킬 것** (지시문 자체에도 포함되어 있지만, 나중에 이 문서를 읽는
Claude를 위해 원칙 자체를 여기 명시합니다):

```
갱신해도 되는 것("공식 배포분"):
  - rest.sce 안의 서비스별 처리::/전송::/타이머::/문장:: 블록 중,
    공식 저장소의 downloads/official_rest.sce에도 동일한 이름으로
    존재하는 것 (그 내용이 달라졌다면 공식 쪽 내용으로 갱신)
  - CLAUDE.md (저장소 루트, 이 문서 자신)
  - help.json (새 공식 명령이 추가됐다면 그에 맞춰)

절대 건드리면 안 되는 것("사용자 커스터마이즈"):
  - addr.ini의 모든 실제 값(토큰/시크릿/webhook URL 등) - 애초에
    이 비교 대상(rest.sce/CLAUDE.md)에 addr.ini 자체가 포함되지 않음
  - 이 로컬 rest.sce 안에서 downloads/official_rest.sce에는 없는
    이름으로 존재하는 처리::/전송::/타이머::/문장:: 블록 (사용자나
    이전 "클루드코드"/"플로우개발" 대화로 직접 추가된 것으로 간주)
  - 이미 존재하는 addr.ini 섹션/키에 사용자가 채워 넣은 값

판단이 애매하면(예: 공식 블록을 사용자가 살짝 고쳐 쓴 경우) 자동으로
덮어쓰지 말고, 요약 메시지에 그 사실을 알려 사용자가 직접 판단하게
할 것 - 1.12/1.13절의 "사람의 명시적 동의 없이는 반영하지 않는다"
원칙과 같은 결입니다.
```

**검증(2026-09-26, 실측)**: `install_hour` 최초 1회 기록 + 재기동 후
불변 확인, `addr.ini.install_hour`를 실제 시각과 불일치시켰을 때 GitHub
조회 자체가 발생하지 않음을 `/notebookflow/trace/UPDATE` SSE로 확인,
일치시켰을 때 실제 GitHub API 호출(`GET .../commits/main`, 200, 실제
커밋 데이터)이 발생함을 동일한 방법으로 확인, 버전이 같을 때(정상 상태)
Telegram 알림이 전혀 나가지 않음을 `/notebookflow/trace/TELEGRAM`으로
확인, 의도적으로 `last_applied_version`을 다르게 만들었을 때 실제
Telegram 알림(정확한 커밋 SHA 포함)이 발신됨을 확인, agent-flow 무상태
호출 경로가 실제로 동작함을 확인. Telegram "업데이트" 텍스트 명령이
촉발하는 2단계 다운로드(`official_rest.sce`/`official_CLAUDE.md`) +
실제 Claude Code 반영까지 이어지는 전체 왕복은, 실제 Telegram 폴링
오프셋(`tg_offset`)을 건드릴 위험이 있는 인위적 페이로드 주입 없이는
운영 중인 프로세스에서 안전하게 재현하기 어려워 이번 실측에서는
제외했습니다 - 실제로 "업데이트"라고 답해보는 것이 남은 유일한 검증
단계입니다.

## 1.16 KRX 주식시세 "관심종목 감시+알림" — 1단계 실측 결과와 addr.ini 핫리로드의 숨은 함정 (2026-09-26)

Telegram "주식 관심종목 추가/삭제/조회" 명령 + 일 1회 등락률(±5% 기본값,
`설정.KRX.threshold`) 감시 알림 기능입니다(`NAMESPACE=KRX`,
`telegram_alias=주식`). 순수 `rest.sce`/`addr.ini` 작업이며 `Main.cpp`
변경은 없습니다. 공공데이터포털 "금융위원회_주식시세정보"
(`getStockPriceInfo_V2`)를 실제로 호출해 확정한 사실들이 있습니다.

**1단계 실측 결과(추측 아님, 실제 호출로 확정)**:

```
A) 종목명 검색은 실제로 지원됩니다 — likeItmsNm=삼성전자 로 호출하면
   "삼성전자"와 "삼성전자우"(우선주)가 함께 반환됩니다(부분일치이지,
   정확일치가 아님). 그래서 "관심종목 추가" 시점에는 반환된 배열을
   순회하며 itmsNm이 사용자가 입력한 이름과 **정확히(==)** 일치하는
   항목만 골라 그 srtnCd(종목코드)를 확정해 저장합니다(3부 GitHub 이슈
   순회와 동일한 재귀 처리:: + 세션.객체.배열[idx] 패턴).

B) srtnCd 파라미터를 **단독으로** 넘기면 조용히 무시되고 필터링되지
   않은 전체 목록이 돌아옵니다(실측 확인된 API 자체의 함정 — 우리
   엔진 버그가 아니라 외부 API의 동작입니다). 정확한 코드 단건 조회는
   반드시 likeSrtnCd(부분일치이지만 6자리 숫자 코드 자체가 유일해
   사실상 정확일치로 동작) 또는 isinCd(12자리 정식 ISIN, 완전일치)를
   써야 합니다. 그래서 관심종목 등록 후의 일일 시세 조회는 전부
   `likeSrtnCd=<코드>` + `numOfRows=1`을 씁니다.

C) `basDt`(기준일자)를 아예 생략하면 최신 영업일이 있는 데이터부터
   내림차순으로 정렬되어 돌아옵니다 — 그래서 "데이터가 일 1회 지연
   갱신되니 며칠 전 날짜를 추측해서 넣어봐야 한다"는 걱정은 실제로는
   필요 없습니다. `basDt`를 아예 안 쓰고 `numOfRows=1`로 요청하는 것이
   가장 단순하고 안전합니다(영업일/휴일 여부를 이 시나리오가 직접 계산할
   필요가 없어집니다).
```

**등락률(`fltRt`) 필드를 조건 비교에 쓸 때의 함정**: 이 API의 `fltRt`는
`"4.98"`, `"-3.89"` 처럼 부호와 소수점이 있는 문자열입니다. 그런데
2.3절에서 확인했듯 `>`/`>=` 비교는 양변을 **`KUINT`(부호 없는 정수)로
캐스팅**합니다 — 소수점 이하는 자연히 잘리지만(임계값이 정수 %라면
안전), **음수 부호("-")가 붙은 문자열을 그대로 캐스팅하면 언더플로우로
거대한 양수가 되어 비교가 뒤집힙니다.** 그래서 비교 직전에 항상
`함수.부분비교(is_neg,값,-)`로 부호를 확인하고, 음수면
`함수.추출(값_abs,값,1,10)`로 첫 글자(부호)를 잘라낸 뒤에만 임계값과
비교해야 합니다(`처리::KRX.주식감시부호분기` 참고). 이건 KRX API에
국한된 문제가 아니라, **부호 있는 소수 문자열을 `상태::`/`만약에`의
숫자 비교 연산자에 직접 넘기면 항상 발생할 수 있는 일반적 함정**이므로
앞으로 다른 연동(주가/온도/증감률 등 음수가 나올 수 있는 모든 API)에서도
같은 패턴을 재사용해야 합니다.

**관심종목 저장 구조 — 세션 CSV + addr.ini는 "부팅 시드"일 뿐, 런타임
쓰기는 없습니다**: `.sce`에는 `addr.ini`에 새로 값을 "쓰는" 수단이
없습니다(`설정::` INI 클래스는 1.6/2.1절처럼 죽은 코드이고, `함수.*` 22종
어디에도 INI 파일 쓰기가 없습니다). `PUT /notebookflow/config/addr`
자체는 `Flow::applyAddrConfig` → `IniFileReader::IMPORT()`를 통해 실제로
디스크에 파일을 다시 씁니다(1.6절에서 추정했던 것과 달리 이건 진짜
파일 쓰기입니다, `libUtil/dfile/INIFILE.cpp:20-28`) — 하지만
`IniParser::IMPORT()`가 `m_list.clear()` 후 **전달받은 JSON 전체로
완전히 재구성**하므로, 일부 필드만 담아 PUT하면 나머지 모든 섹션
(다른 서비스의 토큰/webhook 전부 포함)이 통째로 사라집니다. 이 위험을
`.sce` 레벨에서 안전하게 처리할 방법이 없어(전체 JSON을 GET해 그대로
들고 있다가 병합해서 다시 PUT하는 왕복을 이 DSL의 배열 처리 능력으로는
안전하게 구현할 수 없음), **이번 기능은 이 경로를 쓰지 않기로
결정했습니다.** 대신:

```
- addr.ini의 [KRX_WATCHLIST] 섹션(종목1~종목5, "이름:코드" 형식)은
  NotebookFlow.exe 부팅 시(처리::FLOW.procRestInit → 주식관심종목초기화)
  딱 한 번만 읽혀 세션 변수(세션.krx_watch_csv, 콤마로 구분된
  "이름:코드,이름:코드,...")에 시드로 들어갑니다.
- 런타임 중 텔레그램 "추가/삭제"는 이 세션 변수만 고쳐 씁니다 -
  addr.ini 파일에는 다시 쓰여지지 않습니다.
- 결과: NotebookFlow.exe가 재시작되면 런타임에 추가/삭제한 내용은
  사라지고 addr.ini의 시드 목록으로 되돌아갑니다. 진짜 영구 저장이
  필요하면 후속 과제로 C++ 쪽에 addr.ini의 한 섹션만 안전하게
  추가/삭제하는 전용 REST 액션을 만들어야 합니다.
```

**⚠️ 새로 실측 확인된 불일치 — addr.ini 파일에서 키를 지워도 핫리로드가
그 삭제를 반영하지 않습니다.** `IniFileReader::inifiletimeout()`(1초
폴링)은 파일이 바뀌면 `m_parser.PARSE()`를 다시 부르는데,
`IniParser::PARSE()`는 시작할 때 `m_list.clear()`를 하지 않습니다(반면
`IMPORT()`는 합니다) — 그래서 파일에서 어떤 `key=value` 줄을 완전히
지워도, 이미 메모리에 올라와 있던 그 키의 값은 절대 사라지지 않고
그대로 남습니다(같은 키에 새 값을 다시 쓰면 그 값으로는 갱신됩니다 -
문제는 오직 "삭제"입니다). 2026-09-26에 이 기능을 검증하다가 직접
재현했습니다: 테스트로 `[KRX_WATCHLIST]`에 `종목1=삼성전자:005930`을
추가했다가 파일에서 그 줄을 지웠는데도, `GET /notebookflow/config/addr`
응답에는 `종목1` 값이 계속 남아있었습니다. **addr.ini에서 뭔가를
완전히 지우고 싶다면(추가/수정이 아니라 삭제라면), 파일만 고치는
걸로는 부족하고 NotebookFlow.exe를 재시작해야 확실합니다** — 이건
`rest.sce`/`addr.ini` 편집 후 "핫리로드로 충분하다"고 안내해 온
이 문서의 기존 습관(1.12절 등)에 대한 중요한 예외이므로, 앞으로 어떤
연동에서든 "값을 지우는" 작업을 할 때는 이 점을 사용자에게 미리
알려야 합니다.

**검증 중 실제로 재현된 또 다른 사실 — `OutboundClient` 스레드 굶주림
(`notebookflow_outbound_client_thread_starvation` 메모리와 동일 이슈,
이번에 실측으로 재확인)**: `이벤트명=시작`을 인위적으로 다시 주입해
`처리::FLOW.procRestInit`를 재실행시켰더니(텔레그램폴/NF봇폴/GitHub
이슈조회 등 여러 개의 동기 외부 HTTP 호출이 한꺼번에 겹치며), 외부
호출용 워커 스레드가 단 2개뿐이라 한동안 `NotebookFlow.exe`의 HTTP
서버 전체가 새 연결조차 못 받는(연결 자체가 실패하는) 상태에 잠깐
빠졌다가 수십 초 뒤 스스로 회복되었습니다. 이 과정에서 환영 메시지와
help 이미지가 사용자의 실제 Telegram으로 다시 발송되는 부작용도
있었습니다. **결론**: `이벤트명=시작`(전체 재부팅 초기화) 같은
무거운 이벤트를 운영 중인 인스턴스에 재현/디버깅 목적으로 주입하는
건 위험합니다 — 여러 서비스의 초기화 로직이 전부 동시에 깨어나
외부 호출을 쏟아내기 때문입니다. 앞으로 이런 검증이 필요하면 더
좁은 범위의 이벤트(예: 이 기능의 `이벤트명=주식감시틱`처럼 단일
타이머 이벤트)만 골라 주입하고, `이벤트명=시작` 전체 재실행은 되도록
피하는 게 안전합니다.

**2단계 — "주식 관심종목 조회" 무응답 버그의 근본 원인과 수정 (2026-09-26)**:
1단계 배포 후 실사용자가 "주식 관심종목 조회"(그리고 구조상 "삭제"도
동일하게)를 보내면 텔레그램 응답이 **완전히 없었습니다**("추가"는
정상 응답). 실측으로 근본 원인을 다음과 같이 확정했습니다.

> ⚠️ **DSL 전역 함정 — `함수.*` 호출 인자에 리터럴 콤마(`,`)를 절대
> 넣지 마세요(따옴표로 감싸도 소용없습니다).** `FUNCPARSER.cpp`의
> 인자 파싱 상태 기계(`m_fnE_PARSE_FUNC_ARG`/`m_fnE_PARSE_FUNC_ARG_SP`,
> 311-394줄)를 직접 읽어 확인한 사실인데, **이 파서는 따옴표/이스케이프
> 개념 자체가 전혀 없습니다.** `함수.쪼개기(krx_watch_list,세션.krx_watch_csv,",")`처럼
> 구분자를 `","`로 "감싸서" 써도, 파서는 그냥 눈에 보이는 모든 콤마를
> 무조건 인자 구분자로 취급합니다. 그 결과 `","`라는 세 글자(`"`,`,`,`"`)가
> **인자 3개("문자열 없음" 상태의 `"` 토큰 2개 + 그 사이의 콤마가
> 구분자로 소비됨)로 쪼개져 버려**, 원래 의도한 "구분자 문자 하나"가
> 아니라 **엉뚱하게 인자 개수 자체가 4개로 늘어납니다.**
>
> 이게 실제로 두 가지 다른 방식으로 문제를 일으켰습니다:
> - **`함수.쪼개기`(SPLIT, `NUMS()!=3` 엄격 검증)**: 인자가 3개가 아닌
>   4개로 잘못 세어지므로 **매번 조용히 `false`를 반환**합니다.
>   `EXEPROC.cpp`의 `ExeProc::EXE`(18-122줄)는 `처리::` 본문 안에서
>   `함수.*`/`전송.*`/`처리.*` 호출 중 **단 하나라도 `false`를 반환하면
>   그 즉시 전체 처리 블록 실행을 중단**합니다(`if(!func.EXE(...))
>   return false;` 패턴이 70/75/88/106줄에 반복). `처리::KRX.주식관심종목순회시작`의
>   `그외` 분기 맨 첫 줄이 바로 이 `함수.쪼개기(krx_watch_list,세션.krx_watch_csv,",")`였기
>   때문에, **이후의 모든 것(`전송.KRX시세조회전송` 발신, 응답 처리,
>   텔레그램 회신)이 통째로 실행되지 않았습니다** — 로그도 안 남고
>   에러도 안 나는 "완전 무응답"이 정확히 이래서 발생합니다. `삭제`
>   플로우(`처리::KRX.주식관심종목삭제명령처리`)도 동일한 호출을 써서
>   똑같이 깨져 있었습니다(사용자가 아직 시도하지 않았을 뿐).
> - **`함수.붙이기`(CAT, 인자 개수 검증 없음)**: 같은 방식으로 `","`가
>   4개 인자로 쪼개지지만 CAT은 그냥 다 이어붙이므로 **크래시 없이
>   조용히 틀린 문자열을 만듭니다** — 의도한 콤마 대신 문자 그대로의
>   따옴표(`"`) 두 개가 값 사이에 끼어듭니다. "추가"가 실제로는 정상
>   응답했던 이유는, 사용자가 딱 **첫 번째** 종목만 추가해봤기
>   때문입니다(`처리::KRX.주식종목검색성공`의 `만약에(krx_watch_csv==없음)`
>   분기는 `:`만 쓰고 콤마를 전혀 안 씀). 두 번째 이후 종목을 추가하는
>   `그외` 분기, `삭제`의 CSV 재조립, `addr.ini`의 `KRX_WATCHLIST`에
>   종목을 2개 이상 시드로 등록하는 경로는 전부 이 콤마 손상 버그를
>   그대로 갖고 있었습니다.
>
> **수정**: KRX 섹션 전체에서 구분자를 콤마(`,`)에서 파이프(`|`)로
> 교체했습니다(`함수.쪼개기(krx_watch_list,세션.krx_watch_csv,|)`처럼
> **따옴표 없이 맨 문자로**) — `|`는 `FUNCPARSER`가 특별 취급하는 문자가
> 아니고(공백/콤마/괄호만 특별함), 같은 파일 안에서 이미
> `함수.붙이기(krx_walk_lines,|,문장.KRX주식시세라인문장)`처럼 맨 문자로
> 안전하게 쓰이고 있던 전례가 있어 그대로 재사용했습니다. 바뀐 지점
> 9곳: `주식시드확인1~5`의 addr.ini 다중 시드 조인, `주식관심종목삭제명령처리`/`주식관심종목순회시작`의
> `함수.쪼개기` 2곳, `주식관심종목삭제보존`의 CSV 재조립, `주식종목검색성공`의
> 두 번째 이후 종목 추가 조인.
>
> **일반 원칙(이 프로젝트 전체에 적용)**: `함수.*` 호출의 인자 값
> 안에 콤마가 필요하면, 따옴표로 감싸려 하지 말고 **콤마가 아닌 다른
> 구분자(`|`, `:`, `;` 등 괄호/공백/콤마가 아닌 아무 문자)를 맨 문자
> 그대로** 쓰세요. 이 DSL의 `FUNCPARSER`는 이스케이프를 지원하지
> 않습니다.
>
> **디버깅 방법론 메모**: 이 버그는 처음엔 "세션 리스트 `[동적인덱스]`/`.SIZE`
> 접근 자체가 깨졌다"는 가설로 추적을 시작했지만, 실제 라이브
> 프로세스에 임시 진단용 `처리::`/`전송::` 블록(`전송메시지.주소 =
> 수신메시지.주소; 전송메시지.RESULT = 0`으로 세션 변수를 HTTP 응답에
> 바로 echo하는 패턴, BSKY의 `게시글전송응답`을 본떠 만듦)을 추가하고
> 필드를 하나씩 늘려가며 이분 탐색한 끝에, 원인이 `.SIZE`가 아니라
> `함수.쪼개기` 호출 자체(콤마 인자)에 있음을 확정했습니다. `GET
> /notebookflow/trace/<NAMESPACE>` SSE와 `PUT /notebookflow/flow`
> 합성 이벤트 주입(`이벤트명=주식감시틱`처럼 안전하고 좁은 이벤트만)을
> 조합하면, 콘솔 로그 없이도 이런 "조용한 조기 반환" 버그를 실측으로
> 확정할 수 있습니다.
>
> **검증**: 수정 후 `이벤트명=주식감시틱`(폴링 모드, `조회`와 완전히
> 동일한 `주식관심종목순회시작`→`순회다음`→`전송.KRX시세조회전송`
> 코드를 공유)을 주입해 `/notebookflow/trace/KRX`로 확인한 결과, 실제
> KRX API 호출(`likeSrtnCd=005930`)이 발신되고 200 응답(삼성전자
> 실시가)이 정상 수신·처리되는 것을 실측으로 확인했습니다. "조회"
> 명령 자체는 실제 텔레그램 메시지로만 트리거되므로(합성 페이로드
> 주입은 `tg_offset` 훼손 위험 때문에 의도적으로 피함, 위 문단의
> 스레드 굶주림 경고와 같은 이유) 사용자가 실제로 "주식 관심종목
> 조회"를 다시 보내 최종 확인하는 절차가 남아 있습니다.

## 1.17 KMA 기상청 단기예보 "날씨 조회+지역 감시 알림" — 1단계 실측 결과와 설계 결정 (2026-09-26)

Telegram "기상청 날씨 [지역]" 명령 + "기상청 지역 추가/삭제 [지역]" 관심지역
등록 + 매일 1회(기본 07시) 등록 지역의 강수확률/비·눈 예보를 확인해 조건
충족 시에만 알리는 기능입니다(`NAMESPACE=KMA`, `telegram_alias=기상청`).
[[1.16절의 KRX 관심종목 감시]]와 완전히 같은 "폴링 → 조건부 알림" 뼈대를
재사용하되, KRX와 달리 **종목 검색 API가 없어 외부 호출 없이 로컬
하드코딩 표만으로 좌표를 해석**한다는 점에서 더 단순합니다. 순수
`rest.sce`/`addr.ini` 작업이며 `Main.cpp` 변경은 없습니다.

**1단계 실측 결과(기상청 단기예보 `getVilageFcst`, 공공데이터포털 실제
호출로 확정, 추측 아님)**:

```
A) 격자 좌표(nx,ny): 아래 10개 주요 도시 전부 실제 호출로 값이 유효함을
   확인했습니다(서로 다른 도시가 서로 다른 합리적인 기온값을 반환 -
   완전 동일 좌표로 겹치지 않음이 확인됨).
     서울 60:127   부산 98:76   대구 89:90   인천 55:124  광주 58:74
     대전 67:100   울산 102:84  세종 66:103  수원 60:121  제주 52:38
   전국 완전 매핑은 후속 과제로 남겨두었습니다(티켓 범위 밖으로 명시됨) -
   미지원 지역명 입력 시 "$$$지역$$$은(는) 아직 지원하지 않는 지역입니다"로
   안내하고 조용히 실패하지 않습니다.

B) 발표시각(base_time) 스케줄은 02/05/08/11/14/17/20/23시(3시간
   간격) 8회이며, 실제 호출로 스케줄 경계를 확정했습니다: 정확히 스케줄에
   없는 시각(예: 1730)이나 먼 미래(4시간 이상 뒤, 또는 다른 날짜)는
   항상 `resultCode:"03"(NO_DATA)`로 명확히 실패합니다.

C) ⚠️ **예상 밖의 실측 발견 - "발표 시각"이 실제로는 조기 게시될 수
   있습니다.** 실제 시각 18:22(KST)에 "아직 발생하지 않은" 20:00
   발표분을 요청했더니 정상적으로 완전한 데이터가 돌아왔습니다(약 98분
   이르게 이미 게시되어 있었음 - 재현 확인됨). 반면 같은 시각에 4시간
   38분 뒤인 23:00분은 `NO_DATA`였습니다. 즉 "발표 이후 지연"이 아니라
   경우에 따라 **"발표 예정 전에 미리 게시되는" 방향의 불확실성**이
   있습니다 - 이 리드타임은 안정적이지 않으므로(가까운 미래는 되고 먼
   미래는 안 되는 정확한 경계를 매번 보장할 근거가 없음), **이 기능은
   미래 슬롯을 절대 시도하지 않고 항상 "현재 시각 기준 가장 최근에 지난
   스케줄 슬롯"만 요청하도록 설계했습니다** - 이 슬롯은 정의상 이미
   과거이므로 항상 안전하게 게시되어 있음이 확인되었습니다(1700을
   82분 지난 시점에 정상 수신 확인).

D) ⚠️ **알려진 한계 - 자정~새벽 2시(00:00~01:59 KST) 사이의 좁은
   구간.** "가장 최근에 지난 슬롯"이 전날 23:00인 경우가 유일하게
   여기 해당하는데, `함수.날짜`는 strftime 포맷팅만 하고 "하루 전"
   같은 날짜 연산 기능이 없어(2부 2.5절 표에 정확히 그렇게 문서화됨)
   전날 날짜를 계산할 방법이 없습니다. 이 구간에서는 부득이하게 당일
   0200을 요청하며(가장 가까운 예정 슬롯, C)의 조기게시 현상 덕분에
   실제로 성공할 수도 있으나 보장되지 않음), 실패 시 KMA API 자체가
   `NO_DATA`를 명확히 반환하므로 조용한 오류가 아니라 티켓 범위상 인지된
   제약으로 남겨둡니다(후속 과제: 날짜 연산이 필요하면 C++쪽에 "어제
   날짜" 헬퍼 추가 검토).

E) 응답 구조: 한 번의 호출이 다일×다시각×다요소 조합의 매우 많은 행을
   반환합니다(전체 조회 시 totalCount 1000행 이상). 이번 기능은
   `numOfRows=12`(정확히 한 발표시각의 다음 예보 1개 시간대분 12개
   요소)만 요청해 GitHub 이슈 순회와 동일한 "배열 통째 저장(함수.객체저장)
   + 대괄호 동적참조 순회 + 재귀" 패턴(1.9/2.6/2.8/3.3절)으로 카테고리별
   값(TMP/POP/SKY/PTY)을 필드명으로 뽑습니다 - CPH의 오디널 함정(2.6절)을
   피하기 위해 필드명 비교(`category == POP`)로 순회하며, 이는 순서
   불변성을 API에 의존하지 않는 안전한 방식입니다.

F) getUltraSrtNcst(초단기실황, "지금 날씨")는 이번 범위에서 쓰지
   않았습니다 - 티켓이 "오늘 예보 요약"(단기예보, getVilageFcst)을
   요구했고, 실황은 후속 과제로 남겨두었습니다.

**PTY(강수형태)/SKY(하늘상태) 코드 번역 - 우선순위 있는 평면 if/elseif
체인으로 구현**(`처리::KMA.KMA요약생성`): PTY가 0이 아니면(비/눈이 예보돼
있으면) SKY보다 우선해서 표시합니다(기상청 공식 안내와 동일한 우선순위) -
PTY 1=비, 2=비또는눈, 3=눈, 4=소나기 / PTY==0일 때만 SKY 1=맑음, 3=구름많음,
4=흐림. 강수확률(POP)은 KRX의 등락률(`fltRt`)과 달리 부호가 없는 0~100
정수 문자열이라, 1.16절의 "음수 부호 캐스팅 함정"이 적용되지 않아
`세션.kma_pop >= 설정.KMA.threshold` 숫자 비교를 그대로 안전하게 씁니다.

**관심지역 저장 구조 - KRX와 동일한 세션 CSV + addr.ini 부팅 시드
원칙(1.16절 그대로 재사용)**: `addr.ini`의 `[KMA_WATCHLIST]`(지역1~5)는
부팅 시 1회만 세션의 `kma_watch_csv`(파이프 구분)에 시드로 들어가고,
런타임 추가/삭제는 세션에만 반영되며 재부팅하면 시드로 되돌아갑니다.
**KRX 사고(1.16절의 콤마 함정)를 이미 알고 시작했으므로, 이번엔 처음부터
구분자를 콤마가 아니라 파이프(`|`)로 설계**했습니다 - `함수.쪼개기`/
`함수.붙이기` 호출 전부 `|`를 따옴표 없이 맨 문자로 씀. KRX와의 차이점:
종목명은 API 검색이 필요해 "이름:코드" 쌍을 저장했지만, KMA 지역명은
로컬 10개 도시 하드코딩 표로 매번 즉시 재해석 가능해 **지역명만
저장**하면 충분합니다(관심지역 추가 시 외부 API 호출 자체가 없음 - KRX의
"검색전송" 단계가 통째로 생략됨).

**검증(2026-09-26, 실측, 세 명령 모두 개별 확인 - KRX 사고의 교훈을
반영해 하나만 테스트하고 넘어가지 않음)**: `PUT /notebookflow/flow`로
`이벤트명=KMA테스트날씨/KMA테스트지역추가/KMA테스트지역삭제/KMA테스트폴링`
같은 임시 진단용 처리::/전송::(KRX 발견 당시와 동일한 방법론, 1.16절
"디버깅 방법론 메모" 참고)를 잠시 추가해 `GET /notebookflow/config/rest`
파싱 확인 → 테스트 실행 → `/notebookflow/trace/TELEGRAM`으로 실제
Telegram `sendMessage` 왕복(진짜 `message_id` 발급, dorafather 실제
채팅방으로 실전 전송)을 확인 → 테스트 코드 완전 제거 → 재파싱 확인,
순서로 진행했습니다(실제 Telegram 클라이언트로 명령을 직접 치지 않고도
tg_offset을 건드리지 않는 안전한 방법 - GitHub 이슈등록 검증 때와 같은
이유로 실제 폴링 오프셋 훼손 위험을 피함). 확인된 결과:
- **"기상청 날씨 부산"**: 실제 KMA API 호출(nx=98,ny=76, base_time=1700) →
  응답 "부산 날씨: 흐림, 기온 21도, 강수확률 30% (오늘 17시 발표 기준)" →
  실제 Telegram `sendMessage` 200 성공(message_id 647 발급 확인).
- **"기상청 지역 추가 대구"**: "대구을(를) 관심지역에 추가했습니다..."
  실제 전송 확인(대구가 `kma_watch_csv`에 추가됨).
- **"기상청 지역 삭제 대구"**: 방금 추가한 대구를 실제로 삭제 -
  "대구을(를) 관심지역에서 삭제했습니다." 확인. 이어서 이미 없는 대구를
  한 번 더 삭제 시도해 "대구은(는) 등록된 관심지역이 아닙니다." 실패
  분기도 별도로 확인(추가/삭제 두 분기 모두 검증).
- **일일 감시 폴링(조건 미충족)**: 관심지역이 서울 하나만 남은 상태에서
  폴링 실행 → 실제 서울 API 호출(맑음, POP 0%) → threshold(60%) 미달로
  알림 생략(로그만, Telegram 미발송) 확인.
- **일일 감시 폴링(조건 충족)**: `threshold`를 임시로 0으로 낮춰 재실행 →
  "[기상청 날씨 알림] 강수확률 0% 이상 또는 비/눈 예보가 있는 관심지역이
  있습니다.\n서울 맑음 강수확률 0%" 실제 Telegram 발송 확인 → threshold를
  즉시 60으로 복구.
- 테스트 종료 후 임시 진단 코드(STATE 5줄 + 처리::/전송:: 6개 블록) 전부
  제거, `GET /notebookflow/config/rest` 응답 크기가 테스트 이전과 정확히
  동일함(201810바이트)을 확인해 원상 복구를 검증했습니다.

**"을(를)" 조사 처리의 사소한 한계**: KRX 문장(`주식관심종목삭제완료문장`
등)과 동일하게 `을(를)`/`은(는)` 병기 트릭을 그대로 재사용했습니다 - "대구"처럼
받침 없는 글자로 끝나는 지역명에도 "대구을(를)"처럼 다소 어색한 조사가
붙지만, 이건 KRX 때부터 있던 기존 트레이드오프(문법적 조사 자동 판별
기능이 이 DSL에 없음)를 그대로 이어받은 것이라 이번 티켓 범위에서
새로 고치지 않았습니다.

**범위 밖으로 남긴 것(티켓에 명시된 대로)**: 전국 완전 지역 매핑(10개
도시 이후 확장), 일 최고/최저기온(TMX/TMN - 특정 발표시각에서만 나타나
이번 MVP의 `numOfRows=12`(다음 한 시간대분)로는 포착되지 않음, 필요하면
후속 과제로 응답 파싱 범위를 넓혀야 함), 초단기실황(getUltraSrtNcst),
미세먼지(에어코리아) 등 다른 공공데이터 API 연동.

**추가: "기상청 지역 조회" 명령 (2026-09-26)**: KRX의 "주식 관심종목
조회"(1.16절)와 완전히 같은 역할로, 등록된 관심지역 전체와 각 지역의
오늘 예보 요약을 한 번에 보여주는 명령을 추가했습니다. 새 `kma_mode`
값 `지역조회`를 기존 `조회단건`/`폴링` 옆에 추가하고, **폴링용으로
이미 있던 관심지역 순회 골격(`처리::KMA.KMA관심지역순회시작/다음/완료`,
`KMA응답분기처리`, `KMA임계값판정`)을 그대로 재사용**했습니다 - 폴링은
임계값 초과 지역만 골라 알림용 라인을 쌓지만, 지역조회는 임계값 판단
없이 등록된 모든 지역에 대해 무조건 `문장.KMA날씨요약문장`(단건조회와
동일한 문구, "OO 날씨: OO, 기온 OO도, 강수확률 OO% (오늘 OO시 발표
기준)")을 쌓는 새 분기(`처리::KMA.KMA지역조회라인추가`)만 추가했습니다.
관심지역이 없으면 KRX와 동일한 패턴(`문장::TELEGRAM.KMA지역조회빈목록문장`)으로
안내합니다. `help.json`에도 명령을 추가했습니다.

**검증(2026-09-26, 실측)**: `PUT /notebookflow/flow`로 `ACTION_EVENT`
필드(수신메시지.이벤트명 조건이 실제로 매칭하는 내부 JSON 키는 "이벤트명"이
아니라 영문 `ACTION_EVENT`임을 이번에 직접 확인했습니다 - `libUtil/flow/FLOW.cpp:515`,
`libUtil/interpretor/EXESTATE.cpp:18` 참고)로 임시 진단 이벤트를 주입해
두 경우 모두 실제 Telegram `sendMessage` 왕복으로 확인했습니다: ①
관심지역이 실제로 1개(서울) 등록된 상태에서 지역조회 실행 → 실제 KMA
API 호출(nx=60,ny=127) → 실제 발송 확인된 텍스트 "서울 날씨: 맑음, 기온
24도, 강수확률 0% (오늘 17시 발표 기준)". ② 관심지역을 임시로 비운 상태에서
지역조회 실행 → "등록된 관심지역이 없습니다..." 실제 발송 확인. 테스트 후
원래 관심지역(서울)으로 복원 확인, 임시 진단 코드(STATE 4줄 + 처리:: 4개
블록) 완전 제거 후 `GET /notebookflow/config/rest` 파싱 크기가 기능
추가 직후 값(205649바이트)과 정확히 일치함을 확인해 원상 복구를
검증했습니다. `PUT /notebookflow/flow` 자체의 HTTP 응답은 매번 504
timeout이었으나(1.16절에 이미 기록된 "curl 종종 504 표시해도 성공 -
trace 로그가 진실"과 동일한 현상 - genericHandler는 같은 연결(idx)로
직접 응답을 돌려받는 요청에만 200을 반환하고, 이번처럼 다른 경로로
비동기 전송만 트리거하는 이벤트는 항상 자체 15초 타임아웃으로
504를 반환합니다), `/notebookflow/trace/TELEGRAM` SSE로 실제 왕복은
전부 정상 확인되었습니다. 추가로 `TraceHub`는 **동시에 하나의 SSE
구독자만 유지**한다는 것도 이번에 코드로 확인했습니다
(`NotebookFlow/TraceHub.cpp:6-17` - 새 `Attach()`가 이전 연결을
`Close()`시킴) - 여러 trace 채널을 동시에 열면 먼저 연 것이 끊기므로,
이후 유사 검증 시 한 번에 하나의 trace만 구독해야 합니다.

---

# 2부 — 정확한 문법 레퍼런스

## 2.1 클래스 7종

소스: `libUtil/parser/CLASSPARSER.h:15-39`, `CLASSPARSER.cpp:6-23`.

| enum | 한글 키워드 | 영문 키워드 | 비고 |
|---|---|---|---|
| `E_DSL_CLASS_SCE` | 시나리오 | SCE | 파일 최상위 1회. `SceParser::NAME()`은 실제로는 내용과 무관하게 항상 `"NotebookFlow"`를 반환하는 사실상 죽은 검사입니다. |
| `E_DSL_CLASS_STATE` | 상태 | STATE | 이벤트 분기 조건 목록(2.2절) |
| `E_DSL_CLASS_PROC` | 처리 | PROC | 조건/함수 실행 블록(2.4절) |
| `E_DSL_CLASS_ACT_SEND` | 전송 | ACT_SEND | 외부 HTTP 요청/응답 오브젝트(2.11절) |
| `E_DSL_CLASS_ACT_TIMER` | 타이머 | ACT_TIMER | 타이머 재무장(2.11절) |
| `E_DSL_CLASS_STMT` | 문장 | STMT | 문자열 템플릿(2.10절) |
| `E_DSL_CLASS_INI` | 설정 | INI | ⚠️ **죽은 코드** — 아래 참고 |

**문법**: `클래스::NAMESPACE.이름 { 본문 }`. `CLASSPARSER.cpp`의 상태
기계는 `::` 다음에 네임스페이스가 비어있는 경로를 허용하지 않으므로
**NAMESPACE는 정의 시 문법적으로 필수**입니다(1.3절). `::`는 오직
정의 헤더에서만 나타나며, 참조/호출 파서들은 전부 `.` 토크나이징만
쓰고 `:`를 전혀 찾지 않습니다.

**본문 중괄호는 중첩되지 않습니다.** `ClassParser::m_fnE_PARSE_BODY`
(`CLASSPARSER.cpp:253-271`)는 본문 안의 `{`를 깊이 카운터 없이 그냥
파싱 에러로 처리하며, 본문은 **첫 번째** `}`에서 끝납니다. 즉 이 DSL에는
중괄호로 스코프를 가지는 블록 구조가 아예 없습니다 — if/그외그외/그외도
줄 순서로만 구분되는 평면 구조입니다(2.4절에서 상세히).

> ⚠️ **불일치 — `설정::`(INI) 클래스는 .sce 안에서 죽은 코드입니다.**
> `설정::` 블록은 `CLASSPARSER` 레벨에서는 정상 파싱되지만,
> `ExeScen::PARSE()`(시나리오 전체 파싱 진입점)는 STATE/PROC/
> ACT_SEND·ACT_TIMER/STMT만 분기 처리하고 `IS_DSL_CLASS_INI`는 아예
> 처리하지 않습니다 — 조용히 버려집니다. 이 값을 실제로 쓰려던
> `ExeCore::PARSESYSTEM()`도 `#if 0`로 완전히 죽어 있는 코드입니다.
> **실제 설정값의 원천은 `.sce` 안의 `설정::` 블록이 아니라, 별도
> 파일인 `addr.ini`를 읽는 `IniFileReader`(`ExeCore::m_ini`)입니다.**
> 실제 `.sce` 파일에도 `설정::` 블록은 0건입니다. `.sce` 안에 `설정::`을
> 쓰는 건 문법적으로는 통과하지만 **아무 효과가 없습니다** — 값을
> 넣고 싶다면 언제나 `addr.ini`를 고치세요.

## 2.2 상태:: 이벤트 분기 문법

```
상태::FLOW.초기
{
  NAMESPACE.수신메시지.필드 == 값    처리.호출할처리이름
  ...
}
```

- 조건 대상은 **오직 `처리`/PROC 클래스만** 허용됩니다(`전송`/`타이머`를
  STATE 조건의 타깃으로 직접 쓸 수 없습니다).
- 맨 앞 `NAMESPACE` 세그먼트는 `ExeCore::isMatchDomain(NAMESPACE, msg)`로
  평가됩니다: `수신메시지.주소.도메인`과 `addr.ini`의 `[NAMESPACE].domain`
  값을 **앞자리(prefix) 일치**로 대조합니다. `FLOW`는 이 대조를 건너뛰고
  항상 통과합니다(하드코딩된 특수 케이스).
- 뒤의 `수신메시지.필드 == 값`은 2.3절의 일반 조건 연산자 규칙을 그대로
  따릅니다.

실제 rest.sce 예:
```
TELEGRAM.수신메시지.ok == 1    처리.텔레그램수신처리
GITHUB.수신메시지.응답코드 == 200    처리.깃허브테스트응답처리
LINEAR.수신메시지.주소.경로 == /graphql    처리.procLinearRsp
```

> ⚠️ **불일치(2026-09-26 신규 발견) — `상태::` 조건은 사실상 `==`(문자열
> 완전일치) 한 가지만 지원합니다. `!=`/`>`/`<`/`===`/`!!!` 등 2.3절에서
> 설명하는 8개 연산자는 `처리::` 안의 `만약에`/`그외그외` 조건(IfParser가
> 처리)에만 적용되며, `상태::` 조건은 완전히 다른 파서(`StateParser`,
> `STATEPARSER.cpp`)가 처리합니다.** `StateParser::m_fnE_PARSE_PARAM_SP`/
> `m_fnE_PARSE_EVENT`(`STATEPARSER.cpp:222-246`)를 보면 `필드`와 `값`
> 사이의 `=`, 공백, 심지어 `==`까지도 전부 그냥 "구분자로 스킵"하는
> 문자로만 취급합니다 — 연산자 기호 자체를 별도로 저장하거나 분기하는
> 코드가 없습니다. 실행 시(`EXESTATE.cpp:40`)도 `item.m_event == pVal.VAL()`
> 로 하드코딩된 **문자열 완전 일치**만 수행합니다. 즉 `상태::` 줄에
> `UPDATE.수신메시지.sha != NULL` 같은 걸 쓰면 파서가 `!`를 "값"의
> 첫 글자로 삼켜버려 그 뒤 토큰화가 전부 어긋납니다(최악의 경우 다음
> 토큰을 처리 클래스 이름으로 잘못 해석해 파싱 에러가 납니다). 실제로
> `rest.sce` 전체의 모든 `상태::` 줄(2026-09-26 기준)이 예외 없이 `==`만
> 씁니다 — 이 발견과 정확히 일치합니다. **새 `상태::` 조건을 쓸 때는
> 항상 `==`만 쓰고, "다르면"/"이상이면" 같은 판단은 그 조건이 가리키는
> `처리::` 블록 본문 안에서 `!=`/`>` 등으로 다시 하세요**(4부 GitHub
> 업데이트 확인 기능의 `처리::UPDATE.업데이트버전확인응답처리`가 이
> 패턴의 실제 예시입니다 — STATE는 `응답코드 == 200`으로만 라우팅하고,
> "버전이 실제로 다른지"는 그 안의 `만약에(...!= ...)`가 판단합니다).

## 2.3 조건 연산자 전체 목록 — ⚠️ 실측: 8개의 기호 연산자만 존재, 한글 이름은 UI 표시용일 뿐

소스: `libUtil/parser/IFPARSER.cpp:6-31`(리터럴 토큰 테이블), `IFPARSER.h:9-22`
(enum), 의미론은 `libUtil/interpretor/EXEIF.cpp`.

> ⚠️ **불일치**: 업무지침 초안이 나열한 "참/거짓/크다/작다/크거나같다/
> 작거나같다/완전일치/불일치/앞자리일치/부분일치"라는 한글 이름은
> **`.sce` 소스에 나타나는 실제 토큰이 아닙니다.** `IfParser::validCompare()`
> (`IFPARSER.cpp:75-82`)는 정확히 아래 표의 **기호 토큰만** 유효한
> 연산자로 인정합니다. 한글 이름들은 프론트엔드(웹 UI)가 사용자에게
> 보여주는 **표시 라벨일 뿐**(`frontend/src/conditionModel.js`의 OPS
> 배열과 대응) — `.sce` 파일에 실제로 저장/실행되는 문자는 항상 기호입니다.

| 기호 토큰 | 의미론 (EXEIF.cpp) |
|---|---|
| `TRUE` (한글: 참) | 좌변과 무관하게 항상 참. 조건 평가 자체를 생략(단축 평가). |
| `FALSE` (한글: 거짓) | 항상 거짓. |
| `>` | 양변을 `KUINT`로 캐스팅해 **숫자 비교**(문자열 비교 아님). 빈 값이면 거짓. |
| `<` | 위와 동일, 부등호만 반대. |
| `>=` | 숫자 비교, 이상. |
| `<=` | 숫자 비교, 이하. |
| `==` | 우변이 `NULL`/`null` 리터럴이면 "좌변이 비어있는지"(`LENGTH()==0`)를 검사(2.9절). 그 외에는 우변을 `/`로 나눠 각 토큰과 **완전 일치를 OR로** 비교(다중값 매치 지원 — `A == B1/B2/B3`처럼 쓸 수 있음). |
| `!=` | `NULL`/`null` 특수 케이스는 `==`와 동일하되 반대(`LENGTH()!=0`). 그 외에는 `/` 분리 없이 **단순 단일 문자열 불일치**만 검사(`==`와 달리 다중값 OR 지원 없음 — 실측으로 확인된 비대칭). |
| `===` | **앞자리(prefix) 일치**. 우변을 `/`로 나눠 각 토큰에 대해 `strncmp(A, tok, tok.길이)==0`. "완전일치"가 아니라 "접두어 일치"라는 점에 주의. |
| `!!!` | **부분(substring) 일치**. 우변을 `/`로 나눠 각 토큰에 대해 `STRSTR(A, tok)`. |

블록 키워드(연산자가 아니라 `처리::` 본문의 구조어)는 다음과 같이
한/영 둘 다 지원됩니다(`IFPARSER.h:24-38`):

```
IF     = 만약에 | IF
ELSEIF = 그외그외 | ELSEIF
ELSE   = 그외 | ELSE
TRUE   = 참 | TRUE
FALSE  = 거짓 | FALSE
AND    = 그리고 | AND
OR     = 또는 | OR
```

> **참고(역사적으로 수정된 버그)**: MSVC 빌드에서 `char`가 부호 있는
> 타입이라 "그리고"/"또는"의 UTF-8 리드 바이트가 `int`로 승격될 때
> 음수가 되어 파싱이 항상 실패하던 버그가 있었습니다(영문 `AND`/`OR`는
> ASCII라 영향 없음). `unsigned char`로 캐스팅해 수정되었습니다
> (`CXIFPARSER.cpp:187-198`). 지금은 한글 연결어도 정상 동작합니다.

`AssignParser`(대입문 `x = y`)와 `BasicParser`(범용 토크나이저)에는
비교 연산자가 전혀 없습니다 — 비교는 오직 `IfParser`/`CxIfParser`
영역의 몫입니다.

## 2.4 if/그외그외/그외 구조 — ⚠️ 진짜 중첩은 불가능하다 (구조적 증거)

`만약에`/`그외그외`/`그외`는 겉보기엔 다른 언어의 if/elseif/else와
비슷해 보이지만, **실제로는 완전히 평면(flat)적인 형제 목록**입니다.
2.1절에서 이미 확인했듯 본문 안에 `{`가 아예 금지되어 있어 애초에
블록을 중첩시킬 문법적 수단 자체가 없습니다.

`ProcParser::PARSE`(`PROCPARSER.cpp:254-339`)는 `만약에`/`그외그외`/
`그외` 키워드로 시작하는 줄을 만날 때마다 **새로운 형제 Procedure를
평면 리스트에 추가**합니다. 들여쓰기나 깊이를 추적하는 코드가 전혀
없습니다.

**만약 사용자가 "겉보기 중첩"을 시도하면 어떻게 되는가** — 파싱 에러가
나지 않고 **조용히 평면화(flatten)됩니다**: 안쪽에 있는 것처럼 보이는
`만약에`/`그외그외`/`그외` 줄은 그냥 새로운 형제 항목으로 리스트에
추가되고, 이후의 문장들은 (바깥 블록이 아니라) 이 새 항목에 계속
붙습니다. 즉 **바깥 조건의 참/거짓과 전혀 무관하게 평가되는 독립된
분기**가 하나 늘어나는 것이며, 저자가 의도한 "바깥이 참일 때만 안쪽을
본다"는 의미는 전혀 구현되지 않습니다 — 겉모습만 문제가 아니라 실제
동작이 틀어지는 함정입니다.

런타임(`ExeProc::EXE`, `EXEPROC.cpp:18-122`)도 이 평면 모델 그대로
동작합니다: 목록을 순서대로 돌며 조건을 평가하고, **처음으로 참이 되는
항목의 본문만 실행한 뒤 즉시 반환**합니다(이후 항목은 아예 평가하지
않음). `만약에`와 `그외그외`는 코드상 완전히 동일하게 취급됩니다(둘 다
그냥 "조건을 다시 평가"). 마지막 `그외`는 **바로 앞 항목의 조건 결과의
부정**만 사용합니다 — 따라서 `그외그외`/`그외`로 체인을 만들지 않고
별개의 `만약에` 블록을 여러 개 나열하면 의도한 분기가 안 될 수 있습니다
(이건 실제로 `업무지침_FileRA_세션구조정정.md`가 "반드시 그외그외/그외로
체인하라"고 경고하는 이유이기도 합니다 — 코드로 정확히 재확인됨).

**여러 조건 AND/OR**(`그리고`/`또는`)은 중첩의 탈출구가 아닙니다.
`CxIfParser`(`CXIFPARSER.cpp/h`)는 **한 헤더 줄 안에서** `IF(a) OR(b)
AND(c)` 형태로 여러 `IfParser` 조건을 좌→우 단축 평가로 묶을 뿐이며,
괄호로 하위 조건을 묶는 기능(`(...)`로 시작하는 재파싱)은 없습니다 —
`IfParser::PARSE`는 `(` 문자로 시작하는 조건 자체를 거부합니다. 즉
조건식도 블록 구조와 마찬가지로 한 단계짜리 평면 구조입니다.

## 2.5 인라인 함수(`함수.*`) 전체 목록

소스: `libUtil/interpretor/EXEFUNC.cpp`(전체 679줄), 디스패치 테이블은
`g_ExeTable`(10-34줄). 키워드 매핑은 `libUtil/parser/FUNCPARSER.cpp:4-28`,
`FUNCPARSER.h:61-82`(전부 한/영 양쪽 리터럴 지원).

호출 문법은 항상 `함수.이름(인자1,인자2,...)`입니다. **인자 개수/타입은
파싱 단계에서 전혀 검증되지 않습니다** — `FUNCPARSER`의 상태 기계는
콤마로 구분된 문자열만 모을 뿐이고, 개수 검사(`NUMS()!=N`)는 전부
`EXEFUNC.cpp`의 각 핸들러 내부, 즉 **런타임**에만 있습니다. 인자 개수를
잘못 쓰면 파싱은 통과하고 실행 시점에만 조용히 실패(`false` 반환)합니다.

| 함수(한/영) | 인자 검증 | 동작 |
|---|---|---|
| `저장` / SET | 없음 | `함수.저장(target, value)` — target = value(세션 변수에 기록) |
| `더하기` / SUM | 없음(2개 이상) | `함수.더하기(target,a,b,...)` — target = a+b+...(숫자합) |
| `빼기` / MINUS | 없음 | `함수.빼기(target,a,b,c)` — target = a-b-c |
| `나누기` / DIV | `NUMS()!=3` | `함수.나누기(target,a,b)` — target=a/b(정수 나눗셈), b==0이면 값 안 씀 |
| `나머지` / MOD | `NUMS()!=3` | `함수.나머지(target,a,b)` — target=a%b, 0으로 나누면 값 안 씀 |
| `붙이기` / CAT | 없음 | `함수.붙이기(target,a,b,...)` — 구분자 없이 전부 이어붙임 |
| `부분비교` / STRSTR | `NUMS()!=3` | `함수.부분비교(target,a,b)` — b가 a의 부분문자열이면 target="1" |
| `앞자리비교` / STRNCMP | `NUMS()!=3` | `함수.앞자리비교(target,a,b)` — a가 b로 시작하면 target="1" |
| `삽입` / INSERT | `NUMS()!=4` | `함수.삽입(target,src,ins,pos)` — pos 위치에 ins 삽입 |
| `삭제` / DELETE | `NUMS()!=4` | `함수.삭제(target,src,pos,len)` — pos부터 len만큼 삭제 |
| `추출` / EXTRACT | `NUMS()!=4` | `함수.추출(target,src,pos,len)` — 부분 문자열 추출 |
| `시간` / TIME | 없음 | `함수.시간(target)` — 현재 epoch 초 |
| `클럭` / CLOCK | 없음 | `함수.클럭(target)` — 현재 밀리초 클럭 |
| `출력` / PRINT | 없음 | `함수.출력(target,fmt,val)` — fmt에 s/d/u 포함 여부로 조잡하게 포맷 판별 |
| `날짜` / DATE | 없음 | `함수.날짜(target,fmt)` — 현재 시각을 fmt로 포맷(strftime류) |
| `컴프리헨션` / CPH | `NUMS()<3` | **2.6절 참고 — 오디널(순서) 기반, 함정 있음** |
| `반복` / LOOP | `NUMS()<2` | **2.7절 참고** |
| `객체저장` / OBJECT | `NUMS()!=2` | `함수.객체저장(name, 수신메시지.경로)` — JSON 오브젝트 통째로 세션에 저장(필드명 유지) |
| `주소저장` / ADDR | `NUMS()!=2` | **`객체저장`과 디스패치 테이블에서 완전히 동일한 별칭**(`EXEFUNC.cpp:30`) — 기능적으로 차이 없음 |
| `쪼개기` / SPLIT | `NUMS()!=3` | `함수.쪼개기(listName,src,trimChars)` — 임의 구분자로 토큰화 |
| `단어분리` / WORDEX | `NUMS()!=2` | `함수.단어분리(listName,src)` — 공백(" ") 고정 구분자로 토큰화 |
| `단어합치기` / WORDSUM | `NUMS()!=3` | `함수.단어합치기(target, 세션.리스트.name, start_pos)` — start_pos부터 공백으로 이어붙임 |

> **`객체저장`/`주소저장` 공통 제약**: 두 번째 인자는 반드시
> `수신메시지`(RCV_MSG) 참조여야 합니다 — 세션 리스트/세션 객체를
> 소스로 넘기면 조용히 실패합니다(`return false`). "지금 막 도착한
> 메시지의 JSON 서브트리 하나를 필드명 그대로 세션에 박제한다"는
> 용도로만 쓰세요.

> ⚠️ **함정 — `함수.붙이기`(CAT)는 대상 세션 변수가 이미 존재하면
> 초기화하지 않고 그 뒤에 그냥 이어붙입니다 (2026-09-25 실측, 사용자가
> 직접 발견).** `ALIST::SET(key)`(`libUtil/data/LIST.cpp:73-84`)는 키가
> 이미 있으면 **기존 값을 그대로 둔 채** 그 참조만 돌려주고, 새 키일
> 때만 빈 값으로 새로 만듭니다. `함수.저장`(`EXE_SET`)은 `sVal = ...`로
> 항상 덮어쓰지만, `함수.붙이기`(`EXE_CAT`)는 그 참조에 `target<<pszTmp`로
> **누적만** 합니다(`EXEFUNC.cpp:184-196`) — 즉 세션 변수 하나를 여러
> 이벤트 사이클에 걸쳐 재사용하며 `함수.붙이기`로 "매번 새로
> 만든다"고 가정하면, 실제로는 사이클마다 이전 값 뒤에 계속 눌어붙어
> 무한정 길어집니다. 실제로 Telegram 사진 업로드 저장 파일명(매 사진마다
> `함수.붙이기`로 새로 만든다고 가정했으나 실제로는 이전 파일명이
> 계속 붙어 나간 사고)과 SSH 텔넷 응답(`세션.telnet_rsp`가 명령마다
> 리셋 없이 계속 누적되던, 아직 아무도 못 알아챈 잠재 버그)에서 둘 다
> 발견·수정했습니다.
>
> **안전한 패턴 두 가지**:
> 1. 매 사이클 "새로 시작"이어야 하면, `함수.붙이기`를 부르기 **직전에**
>    같은 대상을 `함수.저장(target, 첫_값)`으로 먼저 리셋한 뒤, 이어지는
>    `함수.붙이기(target, 나머지_값...)`가 그 위에 이어붙이게 하세요
>    (`텔레그램사진업로드감지2`가 이 패턴 — 주의: `함수.저장`은 두 번째
>    인자 하나만 값으로 쓰고 그 뒤 콤마 인자는 조용히 버리므로, 리셋용
>    `함수.저장` 호출은 반드시 값 인자를 정확히 하나만 넘기세요. 두
>    번째 인자 자리를 아예 비워서 `함수.저장(target,)`처럼 쓰면 파서
>    자체가 깨져 시나리오 전체가 훼손될 수 있습니다 — 2026-09-25 실측
>    확인, `sshRsp`처리에서 최초 시도 시 재현됨. 대신 `함수.저장(target,
>    첫_실제값)` + 그 다음 줄 `함수.붙이기(target, 나머지_값)`처럼
>    "리셋"과 "첫 값 대입"을 한 번에 하세요 — `sshRsp`의 최종 수정본
>    참고).
> 2. 여러 사이클(재귀 호출 등)에 걸쳐 **의도적으로 누적**하고 싶다면,
>    최상위 진입 처리::에서 `함수.저장`으로 딱 한 번 초기 값을 세팅한
>    뒤에만 재귀 루프 안에서 `함수.붙이기(target,세션.target,...)`처럼
>    자기 자신을 명시적으로 다시 참조하는 자기순환 패턴을 쓰세요
>    (GitHub 신규 이슈 순회, 3.3.1절의 `gh_new_issue_titles`/
>    `issue_titles`가 이미 이 패턴대로 안전하게 구현되어 있습니다).

> ⚠️ **함정 — `함수.*` 호출 인자에 리터럴 콤마(`,`)를 절대 넣지 마세요,
> 따옴표로 감싸도 소용없습니다 (2026-09-26 실측, KRX "관심종목 조회"
> 무응답 버그의 근본 원인).** `FUNCPARSER.cpp`의 인자 파싱 상태 기계
> (`m_fnE_PARSE_FUNC_ARG`/`_SP`, 311-394줄)는 따옴표/이스케이프 개념이
> 전혀 없어서, `함수.쪼개기(target,src,",")`처럼 구분자를 따옴표로
> "감싸도" 보이는 모든 콤마를 그냥 인자 구분자로 소비해 버립니다 —
> 의도한 인자 3개가 아니라 **4개**(콤마 앞뒤로 남는 `"` 토큰이 각각
> 별도 인자가 됨)로 잘못 쪼개집니다. `NUMS()` 인자 개수를 엄격히
> 검증하는 함수(쪼개기/나누기/나머지/삽입/삭제/추출 등)는 이 때문에
> **조용히 `false`를 반환**하고, `EXEPROC.cpp`는 `처리::` 본문 안에서
> `함수.*` 호출 하나라도 `false`면 그 즉시 이후 전체를 실행하지 않고
> 중단합니다(로그도 에러도 없이) — "완전 무응답" 버그의 전형적인
> 패턴입니다. 인자 개수 검증이 없는 `함수.붙이기`(CAT) 등은 대신
> **조용히 틀린 문자열**(의도한 콤마 대신 리터럴 따옴표 문자가 끼어듦)을
> 만듭니다. **콤마가 필요하면 `|`/`:`/`;` 등 괄호·공백·콤마가 아닌
> 다른 문자를 따옴표 없이 맨 글자로 쓰세요**(1부 1.16절의 KRX 사례가
> 실제 수정 전후 비교입니다).

## 2.6 컴프리헨션(CPH) — ⚠️ 필드 이름이 아니라 순서(오디널)로 값을 뽑는다

```
함수.컴프리헨션(결과리스트명, 수신메시지.배열경로, 필드인덱스)
```

**핵심 코드(`EXEFUNC.cpp:426-454` 요지)**:

```cpp
RestParam & srcInMsg = _req.GETR((KCSTR)srcKey);   // 수신메시지의 배열
...
for(KUINT ri=0; ri<srcInMsg.NUMS(); ri++)
{
    RestParam & row = srcInMsg[ri];
    if(fieldIdx < row.NUMS())
        valList.ARR().VAL() = (KCSTR)row[(KUINT)fieldIdx].VAL();  // ★ row["필드명"]이 아니라 row[숫자인덱스]
    else
        valList.ARR().VAL() = "null";
}
```

`row[(KUINT)fieldIdx]`는 **JSON 오브젝트의 필드를 이름이 아니라, 그
오브젝트가 파싱되어 저장된 내부 순서상 N번째 위치로** 꺼냅니다.
`(KUINT)"title"`처럼 필드 이름 문자열을 세 번째 인자로 넘기면 숫자로
캐스팅되어 사실상 인덱스 0으로 취급됩니다 — **이름 기반 접근 방법은
존재하지 않습니다.**

**결론**: CPH는 "N번째 필드"를 뽑을 뿐이며, 그 N번째가 실제로 원하는
필드(`title` 등)와 같다는 보장은 **원본 API가 JSON 필드 순서를 안정적으로
보장할 때만** 성립합니다. GitHub API처럼 필드 순서가 공식적으로 보장되지
않는 응답에 이 방식을 쓰는 건 위험합니다 — 실제로 `rest.sce`는 GitHub
이슈 목록에 CPH를 쓰지 않고, 대신 필드명을 보존하는 `함수.객체저장` +
대괄호 동적 참조(2.8절) + 재귀 `처리::` 조합으로 구현되어 있습니다
(3부 3.3절에서 실제 코드와 함께 그 이유를 설명합니다).

CPH의 결과 리스트는 **스칼라 값만** 담습니다(필드 이름 정보는 추출되는
순간 사라짐) — 그래서 나중에 "그 항목의 title을 달라"는 식의 이름 기반
재조회가 불가능합니다. 여러 필드가 필요하면 CPH를 필드 수만큼 여러 번
호출해 "병렬 리스트"(같은 인덱스로 여러 리스트를 나란히 유지)를 만드는
패턴이 필요합니다.

같은 `resultKey`로 CPH를 다시 호출하면 예전 리스트를 먼저 삭제하고
소스에서 완전히 다시 만듭니다(전체 재구성) — File-RA의 "규칙 목록을
추가/삭제할 때마다 전체를 다시 만든다"는 패턴은 이 특성에 안전하게
기댈 수 있습니다.

## 2.7 반복(LOOP) — 함수.반복과 반복횟수

```
함수.반복(리스트명, 반복시_호출할_처리이름)
```

- 리스트는 `CPH`/`쪼개기`/`단어분리` 중 무엇으로 만들었든 상관없습니다
  (셋 다 같은 내부 저장 형식 `{"key":이름,"values":[...]}`을 씀).
- 리스트가 없거나 비어있으면 **에러가 아니라 0회 반복으로 조용히 성공**
  (`return true`).
- 매 반복 전에 `반복횟수`(LOOP_CNT)를 **1부터** 현재 인덱스+1로 세션
  풀에 기록한 뒤, 지정한 `처리::`를 **동기적으로, 같은 콜스택에서** 호출합니다
  (다음 반복은 이전 반복이 완전히 끝난 뒤 시작).
- 몸통 안에서 `상태변경.중단`으로 `LOOP_BREAK` 플래그를 세우면 그 즉시
  반복이 중단됩니다.
- 몸통이 호출한 `처리::` 자체가 실행 실패(false)를 반환하면 LOOP 전체가
  즉시 실패로 중단됩니다.
- 종료 시(정상 종료/중단/실패 모든 경로) `반복횟수`/`LOOP_BREAK` 세션
  값은 삭제되어 다음 실행에 영향을 주지 않습니다.

`반복횟수`는 **조건문에서 값으로 쓸 때는 1-based 그대로**(`반복횟수 > 0`
같은 비교), **대괄호 배열 인덱스로 쓸 때만 자동으로 1을 빼서 0-based로
변환**됩니다(`세션.리스트.이름[반복횟수]`). 이 이중 의미를 헷갈리지
않는 게 중요합니다.

## 2.8 파라미터 참조 문법

소스: `libUtil/interpretor/EXEPARAM.cpp`(전체 396줄).

`ExeParam::PARAM(val1, val2, ...)`가 모든 값 참조의 진입점입니다.
`val1`(맨 앞 세그먼트)로 아래 네 가지 네임스페이스를 인식합니다.

| 네임스페이스 | 뜻 | 예 |
|---|---|---|
| `수신메시지` / RCV_MSG | 지금 처리 중인 이벤트의 JSON | `수신메시지.result[0].message.text` |
| `세션` / SESSION | 세션 변수/객체/리스트(아래 하위 규칙 참고) | `세션.tg_offset`, `세션.객체.gh_issues`, `세션.리스트.titles[0]` |
| `설정` / INI | `addr.ini`의 `[섹션].key` | `설정.TELEGRAM.bot_token` |
| `문장` / STMT | `문장::` 블록 실행 결과(2.10절) | `문장.텔레그램응답경로` |

위 네 가지 중 어디에도 안 걸리면(예: 리터럴 문자열 안에 우연히 `.`이
들어있는 경우, URL 경로 등) **참조로 취급하지 않고 원문 그대로
재조합해 돌려줍니다**(`val1+"."+val2`) — 2026-09-12에 수정된 동작으로,
그 전에는 첫 `.` 이후가 잘려나가는 버그가 있었습니다(리터럴 URL을
`.sce` 우변에 직접 쓰면 잘리던 문제의 원인이었습니다). 지금은 안전합니다.

**`세션.` 하위 규칙**(`EXEPARAM.cpp:137-282`):

- `세션.객체.이름.필드.하위필드...` — `함수.객체저장`으로 저장한 JSON
  오브젝트를 필드명으로 내려찍어 읽습니다.
- `세션.리스트.이름` — `CPH`/`쪼개기`/`단어분리`로 만든 리스트.
  `.SIZE`(길이), `[idx]`(요소 접근, idx는 `반복횟수` 키워드 / 숫자
  리터럴 / 동적 세션변수 참조 모두 가능)를 지원.
- 둘 다 아니면 `반복횟수`(LOOP_CNT) 키워드 특수 처리, 아니면 그냥
  평범한 세션 변수(`_rPool.GET(val2).VAL()`).

**대괄호 동적 참조** (`세션.객체.gh_issues[세션.idx].title`같은 문법):
소스는 `s_fnGetObjBracketVal()`(오브젝트용, 47-118줄)과 리스트용 변형
(206-236줄) 두 곳. 인덱스 해석 우선순위:
1. 토큰이 `반복횟수`/LOOP_CNT 키워드면 → 루프 카운터를 1-based→0-based로 변환해 사용
2. 전부 숫자 리터럴이면 → 그대로 `atoi`
3. 그 외(예: `세션.idx`) → `.`로 토큰화해 `세션.` 접두어를 벗기고, 남은
   이름을 세션 풀 변수로 조회(`GET().VAL()`) → `atoi`

경계를 벗어나면 실패 처리되고, `]` 뒤에 남은 `.필드.하위필드` 경로는
여러 단계까지 계속 내려찍을 수 있습니다(다단계 지원). **"재귀"는 이
C++ 헬퍼 함수 자체가 도는 게 아니라, `.sce` 시나리오 레벨에서 `처리::`가
세션의 인덱스 변수를 1씩 늘리며 스스로를 다시 호출하는 방식으로
구현됩니다**(3부 3.3절 실제 코드 참고).

## 2.9 NULL/빈값 비교의 진짜 의미

`만약에(수신메시지.result[0].update_id != NULL)` 같은 조건을 볼 때
**"이 값이 문자열 `null`이 아니다"라고 읽으면 안 됩니다.**

`EXEIF.cpp`(72-84, 143-160줄)의 실제 로직: `==`/`!=`의 **우변 원문
텍스트 자체가 리터럴로 `NULL`(또는 `null`)일 때만** 특수 케이스가
발동하며, 이때 실제로 검사하는 건 **좌변 값의 길이가 0인지 아닌지**
(`A.LENGTH()==0` / `!=0`)입니다. 즉 `!= NULL`은 정확히 "이 필드가
비어있지 않다(존재하고 값이 채워져 있다)"는 뜻이고, `== NULL`은
"이 필드가 비어있다(없거나 빈 문자열)"는 뜻입니다.

이게 왜 중요한가: `세션.리스트`/`세션.객체`의 대괄호 인덱스 헬퍼들은
범위를 벗어나거나 값을 못 찾았을 때 **반드시 빈 문자열(`""`, 내부
매크로 `KNULL`)을 반환해야 합니다** — 만약 실수로 4글자 문자열
`"null"`을 반환하면 `LENGTH()`가 4로 non-zero가 되어 `!= NULL` 조건이
영원히 참이 되고, 재귀 종료 조건이 걸리지 않아 무한(정확히는 200회
상한까지) 재귀에 빠집니다. 이 정확한 버그가 실제로 2026-09-12(오브젝트
경로)와 2026-09-14(리스트 경로) 두 번 발생했다가 수정된 이력이
있습니다 — 3부 3.3절 GitHub 이슈 순회 패턴이 바로 이 수정 위에서
동작합니다.

## 2.10 문장(STMT) — `$$$...$$$` 치환과 리터럴 중괄호 이스케이프

```
문장::NAMESPACE.이름
{임의의 텍스트 $$$참조식$$$ 더 많은 텍스트}
```

실제 치환 엔진은 `STMTPARSER.cpp` 자신이 아니라 `libUtil/kstring/
STRCONV.cpp/h`입니다(`STMTPARSER`는 `문장::NS.이름{...}` 헤더 파싱만
담당하고, 본문 처리는 `STRCONV`에 위임). 구분자는 정확히 **달러 기호
3개**(`$$$`)이며, `$$$`로 감싸인 부분은 "키워드"로, 그 밖은 "그대로
출력할 텍스트"로 3-상태 기계가 나눕니다. 실행 시(`ExeStmt::EXE`) 각
키워드를 앞서 배운 2.8절 파라미터 참조 규칙으로 해석해 치환합니다 —
못 찾으면(길이 0) **원문 `$$$키워드$$$`를 그대로 되돌려줘** 라운드트립
안전성을 지킵니다.

`문장::`의 본문 자체가 `{`/`}`로 감싸지므로, 본문 안에 리터럴 중괄호를
넣고 싶으면(예: GraphQL 쿼리) 별도 이스케이프가 필요합니다:

```
$$$<$$$  →  리터럴 {
$$$>$$$  →  리터럴 }
```

`EXESTMT.cpp:13-55`에서 `STRCONV`가 뽑아낸 키워드 텍스트가 정확히
한 글자 `<` 또는 `>`일 때 이걸 `{`/`}`로 되돌려 쓰는 후처리를 합니다.
실제 사용 예(`rest.sce`, Linear GraphQL 쿼리):

```
문장::LINEAR.stmtLinearQuery
{$$$<$$$ issues(first: 10) $$$<$$$ nodes $$$<$$$ id title identifier state $$$<$$$ name $$$>$$$ $$$>$$$ $$$>$$$ $$$>$$$}
```

## 2.11 전송::/타이머:: 오브젝트 구조

```
전송::NAMESPACE.이름
{
  전송메시지.메소드 = GET | POST
  전송메시지.주소.도메인 = 설정.NAMESPACE.domain
  전송메시지.주소.경로 = 문장.NAMESPACE.경로문장
  전송메시지.주소.파라미터[N].key = ...
  전송메시지.주소.파라미터[N].val = ...
  전송메시지.헤더[N].key = ...
  전송메시지.헤더[N].val = ...
  전송메시지.임의필드 = ...    (서비스별 바디 필드는 자유롭게 추가)
}
```

**슬래시 인코딩(`^^^` 마커)**: `ActionParser::PARSE()`는 `전송::`/`타이머::`
본문의 **모든 대입문 원문 줄 전체**에 대해 파싱 전에 `serialPath()`를
적용합니다(`ACTIONPARSER.cpp`) — 즉 특정 필드(주소/경로)에 국한되지
않고, 그 블록 안 어떤 값에 쓰인 `/`든 파싱 단계에서 전부 `^^^`(캐럿 3개)
마커로 바뀝니다. 실행 시(`deserialPath`)에 다시 원래 `/`로 복원됩니다.
`$$$`(STMT 구분자)와 겹치지 않도록 의도적으로 다른 기호를 골랐습니다
(`BASICPARSER.cpp:5-41`).

**GET/POST/"응답(echo)" 모드**: 문서에서 자주 오해하는 지점인데,
**"메소드=응답" 같은 특수 문자열 비교는 어디에도 없습니다.** 실제
분기 기준은 **`전송메시지.메소드` 필드가 아예 비어있는지 여부**입니다:

```
메소드 길이 > 0  →  "요청" 모드: 주소 오브젝트를 실제 URL로 조립해
                     OutboundClient로 진짜 외부 HTTP 호출을 보냄
메소드 길이 == 0 →  "응답" 모드: 새 외부 호출 없이, 원래 inbound
                     연결(주소.idx로 식별되는 접속)에 그대로 회신
```

`메소드`를 깜빡 빠뜨리면 조용히 "응답" 모드로 오분류되어 진짜 나가야
할 외부 호출이 사라지는 실제 회귀 사고가 있었습니다(`업무지침_주소
오브젝트_URL조합인코딩_결과보고_2026-09-05.md` 기록) — **전송:: 블록을
새로 만들 때는 `전송메시지.메소드`를 빠뜨리지 않는 게 매우 중요한
습관입니다** (외부 호출 없이 요청을 그대로 되돌려보내는 echo 패턴을
의도적으로 쓸 때만 예외 — 실제 rest.sce의 `전송::TELEGRAM.텔레그램알람직접응답`
등이 `전송메시지.주소 = 수신메시지.주소` + `전송메시지.RESULT = 0`만
쓰고 메소드를 의도적으로 생략하는 정확히 이 패턴입니다).

**주소 오브젝트 → 실제 URL 조립은 `OutboundClient`가 아니라 `Main.cpp`
쪽 책임**입니다. `OutboundClient`는 이미 완성된 URL 문자열만 받습니다.
`Main.cpp`의 `s_fnBuildAddrObjUrl()`이 도메인+경로를 이어붙이고,
`주소.파라미터[N].{key,val}` 각각을 `deserialPath` 후 RFC3986 기준으로
퍼센트 인코딩해 `?key=val&key2=val2...`로 조립합니다.

**Kor→Eng 최상위 키 변환**: 실제로 외부에 나가기 직전, `s_fnTranslateSndMsgKorToEng()`가
정해진 화이트리스트(`주소`→ADDR, `타입`→ACTION_EVENT, `서비스_타입`→SVC_TYPE,
`시간`→TIME, `리스트`→LIST, `메소드`→METHOD)만 영문 키로 바꿉니다.
사용자가 정의한 필드(예: `content`, `text`, `instruction` 등)는 건드리지
않습니다.

## 2.12 multipart/form-data (`전송메시지.문서`)

파일 업로드(Telegram `sendDocument`/`sendPhoto` 등)는 `전송::` 블록에
최상위 `문서` 필드 그룹을 추가하는 것으로 트리거됩니다. 구현은 전부
`Main.cpp` 쪽에 있으며(`OutboundClient`는 이 존재 자체를 모릅니다),
바이트 조립은 vendored `httplib.h`의 기성 헬퍼(`make_multipart_data_
boundary`/`serialize_multipart_formdata`/`serialize_multipart_formdata_
get_content_type`)를 그대로 재사용합니다.

```
전송메시지.문서.경로 = 세션.file_path        (로컬 파일 경로 — C++가 직접 읽음)
전송메시지.문서.내용 = 세션.telnet_rsp        (경로 대신 내용을 바로 문자열로, 둘 중 하나만)
전송메시지.문서.파일명 = ...
전송메시지.문서.필드명 = document             (생략 시 기본값 "document")
전송메시지.문서.타입 = application/octet-stream (생략 시 기본값 "text/plain")
전송메시지.문서.캡션 = ...                     (선택, Telegram 캡션)
```

`내용`이 있으면 우선 사용, 없으면 `경로`의 파일을 읽습니다. `OutboundClient`의
WinHTTP 경로(`s_fnHttpsRequest`, https 외부 API가 실제로 타는 경로)는
호출자가 이미 `Content-Type`을 넘겼으면 `application/json`을 강제로
덮어쓰지 않도록 수정되어 있어, multipart의 boundary 헤더가 충돌하지
않습니다.

## 2.13 `설정.NAMESPACE.key` — addr.ini 참조

`ExeCore::GETINI(category, key, ret)`(`EXECORE.cpp:143-146`)가
`addr.ini`의 `[category]` 섹션에서 `key` 값을 찾습니다. `IniParser`는
allowlist가 전혀 없는 순수 key=value 리더이므로, `.sce`를 고치지 않고
`addr.ini`에 새 섹션/새 키를 추가하는 것만으로 `설정.NEWSECTION.newkey`를
즉시 참조할 수 있습니다(1.6절에서 다룬 카탈로그 메타 필드들이 정확히
이 방식으로 늘어났습니다 — 단, 그 필드들을 실제로 "동작"으로 연결하는
코드는 별도로 작성해야 합니다. `addr.ini`에 값을 넣는 것 자체는 아무
동작도 만들지 않습니다).

---

# 3부 — 실제 동작 예시 (rest.sce 원문 발췌)

이 절의 모든 코드는 `NotebookFlow/rest.sce`(2026-09-18 기준 최신본,
769줄 전체 확인) **원문 그대로**입니다. 가상의 예시는 없습니다.

## 3.1 알람 4종 직접 트리거 패턴 (Slack/Discord/ntfy + Telegram)

네 서비스 모두 완전히 동일한 3단 구조를 반복합니다 — **처리(세션에
메시지 저장 + echo 응답 + 실제 전송) / 전송(echo 응답) / 전송(실제
발신)**:

```
처리::DISCORD.디스코드알람직접트리거
{
  만약에(참)
    함수.저장(alert_message,수신메시지.message)
    전송.디스코드알람직접응답
    전송.디스코드알람직전송
}
전송::DISCORD.디스코드알람직접응답
{
  전송메시지.주소 = 수신메시지.주소
  전송메시지.RESULT = 0
}
전송::DISCORD.디스코드알람직접전송
{
  전송메시지.메소드 = POST
  전송메시지.주소.도메인 = 설정.DISCORD.webhook_url
  전송메시지.content = 세션.alert_message
}
```

ntfy는 바디 필드가 `topic`+`message`로 다르고, Slack은 `text` 하나뿐,
Discord는 `content` 하나뿐 — **서비스마다 바디 필드 이름이 제각각인
것 자체가 정상**입니다(2부 2.11절의 "통신 계층은 내용을 판단하지
않는다" 원칙 그대로, 필드 이름 매핑은 전적으로 `.sce`의 몫입니다).

## 3.2 Bluesky — 로그인→포스팅 2단계 순차 실행 패턴

Bluesky는 매 포스팅마다 먼저 세션 토큰을 발급받아야 하므로, 1개
이벤트가 **전송 두 번을 순차로 거칩니다** — 첫 전송의 응답이 상태::로
돌아와 두 번째 전송을 트리거하는 전형적인 비동기 체인입니다:

```
처리::BSKY.게시글전송트리거
{
  만약에(참)
    함수.저장(post_message,수신메시지.message)
    전송.게시글전송응답
    전송.sndBskyAccToken          ← 1단계: 로그인(토큰 발급) 먼저
}
처리::BSKY.procBskyTokRcv          ← (상태::에서 BSKY.수신메시지.handle==... 로 라우팅됨)
{
  만약에(참)
    함수.저장(bsky_access,수신메시지.accessJwt)
    함수.저장(bsky_did,수신메시지.did)
    함수.날짜(bsky_now,%Y-%m-%dT%H:%M:%S)
    전송.sndUpText                 ← 2단계: 토큰을 받은 뒤에야 실제 포스팅
}
전송::BSKY.sndBskyAccToken
{
  전송메시지.메소드 = POST
  전송메시지.주소.도메인 = 설정.BSKY.domain
  전송메시지.주소.경로 = 문장.actk
  전송메시지.identifier = 설정.BSKY.my_id
  전송메시지.password = 설정.BSKY.app_pass
}
전송::BSKY.sndUpText
{
  전송메시지.메소드 = POST
  전송메시지.주소.도메인 = 설정.BSKY.domain
  전송메시지.주소.경로 = 문장.bskyPostPath
  전송메시지.헤더[0].key = Authorization
  전송메시지.헤더[0].val = 문장.bskyAuthHeader
  전송메시지.repo = 세션.bsky_did
  전송메시지.collection = 문장.bskyCollection
  전송메시지.record.$type = 문장.bskyCollection
  전송메시지.record.text = 세션.post_message
  전송메시지.record.createdAt = 문장.bskyCreatedAt
}
문장::BSKY.bskyAuthHeader
{Bearer $$$세션.bsky_access$$$}
문장::BSKY.bskyCreatedAt
{$$$세션.bsky_now$$$+09:00}
```

**핵심 패턴**: 1단계(로그인) 응답의 필드(`accessJwt`, `did`)를 세션에
저장해뒀다가, 2단계 전송의 헤더/바디 조립에서 `세션.bsky_access` 등으로
다시 꺼내 씁니다. 이게 "두 서비스 조합" 템플릿의 원형입니다(4부 4.3절).

## 3.3 GitHub 이슈 조회 — ⚠️ 실제로는 CPH/LOOP가 아니라 "재귀 처리:: + 대괄호 동적참조"

`업무지침_GitHub이슈처리_CPH_LOOP_재작성.md`는 `함수.컴프리헨션`+`함수.반복`
조합으로 재작성할 것을 제안했지만, **실제 rest.sce의 GitHub 이슈 순회는
CPH도 LOOP도 쓰지 않습니다.** 그 제안 문서 스스로가 "CPH가 정말
안전한지 0단계에서 재확인하고, 위험하면 억지로 쓰지 말라"고 명시했는데,
2부 2.6절에서 확인했듯 **CPH는 실제로 필드 이름이 아니라 오디널
위치로만 값을 뽑는** 것으로 재확인되었고, GitHub API가 필드 순서를
보장한다는 근거도 없습니다. 그래서 실제로 채택된 해법은 CPH를 쓰지
않는 대신 **① 필드명을 보존하는 `함수.객체저장`으로 JSON 배열 전체를
세션에 통째로 저장**하고 **② `처리::`가 인덱스를 하나씩 늘려가며 자기
자신을 재귀 호출**하고 **③ 그 인덱스를 대괄호 동적 참조로 오브젝트에
꽂아** 순회하는, 엔진에 새로 추가된 기능(2부 2.8절 "대괄호 동적 참조")
조합입니다:

```
처리::GITHUB.깃허브테스트응답처리
{
  만약에(세션.pending_reply_chat_id != NULL)
    함수.객체저장(gh_issues,수신메시지.결과)      ← 배열 전체를 필드명 보존한 채 세션에
    함수.저장(issue_titles,이슈,목록:)
    함수.저장(issue_count,0)
    함수.저장(idx,0)
    처리.깃허브이슈순회
  그외
    로그.출력(github 이슈 조회 완료)
}
처리::GITHUB.깃허브이슈순회
{
  만약에(세션.객체.gh_issues[세션.idx].title != NULL)   ← 대괄호 동적참조(세션 변수로 인덱싱)
    함수.더하기(issue_count,세션.issue_count,1)
    함수.붙이기(issue_titles,세션.객체.gh_issues[세션.idx].title,|)
    함수.더하기(idx,세션.idx,1)
    처리.깃허브이슈순회                               ← 재귀 호출(다음 인덱스로)
  그외
    처리.깃허브이슈텔레그램회신                        ← 종료 조건: title이 NULL(=빈값)이면 배열 끝
}
```

이 패턴은 2부 1.9절의 **재귀 상한 200회**, 2부 2.9절의 **NULL이
`LENGTH()==0`을 뜻한다는 정확한 의미(빈 값이면 배열이 끝났다고 판단)**에
정확히 기대고 있습니다 — 이 두 가지를 실측으로 확정한 뒤에야 이
재귀 패턴이 안전하다고 판단할 수 있었던 것입니다. 이슈가 0개일 때도
`title != NULL` 조건이 첫 인덱스(0)부터 바로 거짓이 되어 안전하게
"GitHub 이슈가 없습니다" 분기로 빠집니다.

```
처리::GITHUB.깃허브이슈텔레그램회신
{
  만약에(세션.issue_count == 0)
    함수.저장(reply_text,GitHub,이슈가,없습니다)
    전송.깃허브이슈텔레그램회신전송
  그외
    함수.저장(reply_text,세션.issue_titles)
    전송.깃허브이슈텔레그램회신전송
}
```

## 3.3.1 GitHub 이슈 등록(POST) + 관리자 일일 폴링 감시 — 2026-09-19 신규,
실측 검증 완료 (업무지침_GitHub이슈_커뮤니티소통_관리자감독_사용자등록.md)

이 절은 "AX 소통"(dorafather님이 2026-09-19에 밝힌 다짐 — AI와 사람이
함께 자동화로 소통하는 앱 생태계) 철학을 실제 기능으로 옮긴 첫 사례입니다.
축이 두 개입니다: **관리자(dorafather) 감독**(하루 1회 이슈 폴링 → 새
이슈 발견 시 Telegram 알림 + agent-flow를 통한 Claude Code 검토·요약)과
**사용자 등록**(누구나 Claude Code에게 "GitHub에 이슈로 등록해줘"라고
말하면 실제로 이슈가 생성됨)입니다.

### 재사용 vs 신규 — 세 갈래로 나뉘는 `깃허브테스트응답처리`

3.3절의 GitHub 이슈 조회(GET)는 그대로 재사용하되, 이 하나의 응답
디스패처(`GITHUB.수신메시지.응답코드 == 200`으로 들어오는 유일한 착지점)를
**세 가지 호출 맥락**이 공유하도록 확장했습니다: ① Telegram "깃허브 이슈"
명령(`세션.pending_reply_chat_id`), ② 부팅 시 1회 기준선 설정
(`세션.gh_poll_mode == 기준선`), ③ 하루 1회 폴링(`세션.gh_poll_mode ==
폴링`). 세 조건 중 **기준선/폴링을 `pending_reply_chat_id` 앞에** 배치한
것이 중요한 정정입니다 — `pending_reply_chat_id`는 한 번 세팅되면 이
세션이 끝날 때까지 지워지지 않는(=Telegram "이슈" 명령을 단 한 번이라도
쓰면 영원히 truthy) 기존 변수이기 때문에, 원래 순서(`pending_reply_chat_id`
가 맨 앞)를 그대로 두고 새 분기를 뒤에 추가했다면 폴링 응답이 전부
Telegram 조회 분기로 잘못 흡수되어 매일 옛날 채팅으로 오답이 갔을
것입니다(그 갈래는 처음 정의된 그대로 한 글자도 건드리지 않았습니다 —
`그외그외`끼리는 완전히 동등하게 취급된다는 2.4절 규칙 덕분에 순서만
바꿔도 안전합니다).

### 최초로 실전 검증된 `그리고`(AND) 다중조건 체이닝

2.4절이 소개한 `IF(a) AND(b)` 형태의 다중조건은 이 프로젝트 어디에도
실사용 예가 없었습니다. 새 이슈 감지 재귀에 처음으로 썼습니다 —
**중요: 조건마다 자기 자신의 괄호가 필요합니다** (`그리고(조건)`이지
`그리고 조건`처럼 괄호 없이 이어붙이면 안 됨. `CxIfParser::m_fnE_PARSE_COND_SP`
는 AND/OR 키워드 다음 문자부터 새 `IfParser`를 처음부터 다시 시작하므로,
그 새 조건도 자기 괄호로 닫혀야 완결됩니다):

```
처리::GITHUB.깃허브새이슈순회
{
  만약에(세션.객체.gh_poll_issues[세션.gh_poll_idx].number != NULL) 그리고(세션.객체.gh_poll_issues[세션.gh_poll_idx].number > 세션.gh_last_seen_issue) 그리고(세션.gh_new_issue_count == 0)
    함수.저장(gh_new_issue_titles,문장.깃허브새이슈항목문장)
    ...
    처리.깃허브새이슈순회
  그외그외(...NULL 아님...) 그리고(...최고번호 아님...)
    함수.붙이기(gh_new_issue_titles,세션.gh_new_issue_titles,|,문장.깃허브새이슈항목문장)
    ...
  그외
    처리.깃허브새이슈완료처리
}
```

실측(2026-09-19, `NotebookFlow.exe`를 직접 재기동해 `/notebookflow/config/rest`
로 파싱 성공 확인 + 폴링 이벤트를 수동 트리거해 실제 판정 결과까지 확인)으로
이 문법이 정상 파싱·정상 평가됨을 확인했습니다 — 이 문서가 지금까지
검증한 것 중 **처음으로 실전에 배치된 `그리고` 체이닝 사례**입니다.

이슈 번호 오프셋 비교(File RA의 offset 패턴과 동일 원리)는 세션 변수
`gh_last_seen_issue`에 저장됩니다. **NonProto 세션은 재기동 시 초기화되므로
(1.7절) 이 값도 재기동마다 사라집니다** — 그래서 `처리::FLOW.procRestInit`이
부팅 직후 "기준선 설정"(알림 없이 현재 최고 이슈 번호만 조용히 기록)을
한 번 거친 뒤에야 24시간 폴링 타이머를 무장합니다. 이 순서를 지키지
않으면 재기동 직후 첫 폴링에서 기존 이슈 전체가 "새 이슈"로 오인되어
알림이 쏟아집니다.

### 새로 발견한 함정 — 트리거용 전송::은 반드시 "즉시 응답"이 있어야 한다

`GET/PUT/POST /notebookflow/flow`로 걸려온 원본 HTTP 커넥션은
`ConnRegistry`에 등록된 채 **누군가 그 `idx`로 회신할 때까지 계속
대기**합니다. 3.1절의 `...알람직접트리거` 계열이 전부 `전송.OOO직접응답`
(`전송메시지.주소 = 수신메시지.주소; 전송메시지.RESULT = 0`)을 실제
작업과 별개로 즉시 한 번 더 호출하는 이유가 바로 이것입니다. 처음
`전송::GITHUB.깃허브이슈등록트리거`를 이 응답 없이 만들었더니 실측에서
매번 정확히 이 타임아웃(`{"RESULT":"1","REASON":"timeout"}`)이
재현되었습니다 — 실제 GitHub 등록 자체(`전송::GITHUB.깃허브이슈등록전송`)는
완전히 정상 동작했는데도, 원본 커넥션에 대한 응답이 없다는 이유만으로
호출자 입장에서는 "실패"로 보였습니다. **새 이벤트 기반 트리거를
`상태::FLOW.초기`에 추가할 때는 항상 `...직접응답` 패턴을 먼저 넣고
시작하세요.**

### GitHub API 오류 응답도 조용히 버려지지 않게

`GITHUB.수신메시지.응답코드`가 200/201이 아닌 값(403/404/422 등)으로
돌아오면 STATE 조건에 매치되는 게 없어 원래는 그냥 버려집니다. 실측
중 실제로 **403("Resource not accessible by personal access token")**을
만났습니다 — `addr.ini`의 기존 `[GITHUB].token`이 조회(읽기) 전용으로
발급된 Fine-grained PAT라서 이슈 생성(쓰기) 권한이 없었던 것입니다(코드
결함이 아니라 토큰 스코프 문제 — 등록을 실제로 쓰려면 `Issues: Read and
write` 권한으로 토큰을 재발급해야 합니다). 이 실패가 아무 피드백 없이
사라지지 않도록 403/404/422 세 코드를 `처리.깃허브이슈등록실패처리`로
받아 Telegram으로 실패 사유를 알리는 안전망을 추가했습니다 — "AX 소통"
철학상 실패도 침묵하지 않아야 한다는 판단입니다.

### agent-flow에게 "검토해줘"만 시키면 승인 대기에 걸린다 — 필요한 내용은 지시문에 직접 담아야 한다

실측 중 실제로 막힌 사례: 처음엔 검토 지시문(`문장::GITHUB.깃허브새이슈검토지시문장`)에
이슈 **제목만** 담아 "이 이슈를 검토하고 핵심 내용을 요약해줘"라고
보냈더니, `agent-flow`가 띄운 `claude -p` 세션이 "핵심 내용"을 알아내려고
스스로 GitHub API(이슈 본문 조회)를 호출하려 시도했고, 그 호출에 도구
승인이 필요해서 그대로 멈춰버렸습니다(`claude -p`는 비대화형 실행이라
승인해줄 사람이 없음 — 검토 결과 대신 "승인이 필요합니다"라는 텍스트가
Telegram으로 그대로 전달됐습니다). 3.6절의 agent-flow 패턴은 "지시만
던지면 알아서 도구를 써서 조사한다"는 전제였는데, **조사에 필요한 도구
호출 자체가 승인 없이는 실행될 수 없다는 사실이 실측으로 처음
드러난 지점**입니다.

해법은 간단합니다 — Claude Code가 스스로 뭔가를 "조회"하게 만들지 말고,
**필요한 데이터를 처리:: 단계에서 미리 세션에 담아 지시문 안에 통째로
박아 넣는 것**입니다:

```
처리::GITHUB.깃허브새이슈순회
{
  ...
    함수.저장(gh_new_issue_bodies,문장.깃허브새이슈본문문장)   ← 이슈 본문을 미리 세션에 저장
  ...
}
문장::GITHUB.깃허브새이슈검토지시문장
{아래는 ... 이슈의 본문 전문이다. GitHub API를 호출하거나 이슈를
직접 조회하지 말고, 아래 제공된 내용만 보고 요약해줘. ...
$$$세션.gh_new_issue_bodies$$$}
```

지시문에 "GitHub API를 호출하거나 직접 조회하지 말라"는 문장을 명시적으로
넣은 것도 중요합니다 — 필요한 데이터가 이미 다 있는데도 습관적으로
도구를 쓰려 할 가능성을 아예 차단합니다. **agent-flow에게 어떤 조사·요약
작업이든 새로 시킬 때는, "가서 찾아봐"가 아니라 "여기 다 줄 테니 이것만
보고 답해"로 지시문을 설계하세요** — 승인 대기 없는 완전 비대화형 실행을
보장하는 유일한 방법입니다.

### 여러 줄에 걸친 함수 호출은 시나리오 전체를 파괴한다 — 반드시 한 줄로

실측 중 실제로 겪은 사고: `함수.붙이기(target,a,b,c)` 같은 함수 호출의
인자 목록 중간에 실수로 줄바꿈을 넣었더니(가독성을 위해 구분자 인자에
여러 줄짜리 텍스트를 직접 썼음), `ExeScen::PARSE()`가 최상위에서
실패하면서 **`m_listState`/`m_listProc`/`m_listAct`/`m_listStmt`를 이미
비운 뒤라 시나리오 전체가 빈 상태로 남았습니다**(2.1절 "본문 중괄호는
중첩되지 않는다"와 같은 뿌리 — 이 DSL의 파서는 애초에 한 줄 단위로
동작하도록 설계되어 있습니다). 더 심각한 건, 이게 파싱 실패로 곱게
끝나지 않고 **프로세스 자체를 크래시시켜 SLEE가 자동 재기동**했다는
점입니다 — 재기동 순간 디스크에 아직 깨진 내용이 남아있었기 때문에
`ExeCore::ADD("rest",...)`의 최초 `INIT()`이 실패해 `ExeScen` 객체가
통째로 버려졌고(`ExeCore::ADD` 55-64줄 — `INIT()` 실패 시 `delete
pNew; return;`로 `m_listSce`에 아예 등록되지 않음), 이후 파일을 다시
정상으로 고쳐도 **1초 단위 핫리로드 타이머 자체가 존재하지 않는 상태라
저절로 복구되지 않았습니다**(`Gpolling::setTimer`는 `ExeScen::INIT()`의
최초 `PARSE()` 성공 뒤에만 걸립니다, `EXESCEN.cpp:38-53`). 복구하려면
**프로세스를 완전히 재기동**해야 했습니다(`GET /notebookflow/config/rest`가
`{}`를 반환하면 이 상태를 의심할 것 — `FINDSCEN("rest")`가 `NULL`이라는
뜻입니다).

**교훈**: `처리::`/`전송::`/`타이머::` 본문 안의 모든 문(대입문, 함수
호출, 조건문 헤더)은 반드시 **한 줄**로 써야 합니다. 문자열 인자 안에
여러 줄짜리 텍스트가 필요하면(이번처럼 GitHub 이슈 본문 전체를 담아야
하는 경우 등) 함수 호출 인자로 직접 쓰지 말고, **`문장::`(STMT) 블록을
따로 만들어 그 안에 여러 줄로 텍스트를 담고, 함수/전송:: 쪽에서는
`문장.이름`으로 참조만** 하세요(`문장::`의 본문 자체는 여러 줄을
지원합니다 — 3.9절 help 안내문구가 실제 사례). rest.sce를 고칠 때는
저장 직후 반드시 `GET /notebookflow/config/rest`로 응답 크기가 정상
범위인지(빈 `{}`가 아닌지) 확인하는 습관을 들이세요.

## 3.3.2 GitHub 이슈 등록의 Telegram 명령 통합 — 2026-09-19 신규

3.3.1절이 만든 `처리::GITHUB.깃허브이슈등록트리거`(내부 `ACTION_EVENT=
깃허브이슈등록` 전용, agent-flow/Claude Code가 HTTP로 직접 호출하는
경로)는 처음엔 Telegram 명령으로는 연결되어 있지 않았습니다. 이 절은
그 게이트웨이를 Telegram 통합 명령 파싱 체계(3.5절) 안으로 정식
편입시킨 기록입니다 — **트리거/전송 로직 자체(`전송::GITHUB.
깃허브이슈등록전송`)는 그대로 재사용**하고, Telegram 쪽에 새 입력
경로 하나만 추가했습니다.

핵심은 기존 `깃허브` 서비스 2단계 단어분리(3.5절)에 **분기 하나를
더 끼워 넣는 것**입니다 — "이슈등록"이 "이슈"의 접두어이므로(둘 다
"이슈"로 시작), **더 긴 키워드를 먼저 검사**해야 "깃허브 이슈등록
..."이 기존 "이슈" 조회 분기로 잘못 흡수되지 않습니다:

```
처리::TELEGRAM.텔레그램깃허브명령처리
{
  만약에(참)
    함수.단어분리(cmd_word_list,수신메시지.result[0].message.text)
    함수.단어합치기(cmd_rest,세션.리스트.cmd_word_list,1)
    함수.앞자리비교(cmd_이슈등록,세션.cmd_rest,이슈등록)   ← "이슈"보다 먼저 검사
    함수.앞자리비교(cmd_타입,세션.cmd_rest,이슈)
    처리.텔레그램깃허브명령분기
}
처리::TELEGRAM.텔레그램깃허브명령분기
{
  만약에(세션.cmd_이슈등록 == 1)                          ← 더 긴 키워드를 먼저 분기
    함수.저장(pending_reply_chat_id,수신메시지.result[0].message.chat.id)
    처리.텔레그램깃허브이슈등록명령처리
  그외그외(세션.cmd_타입 == 1)
    함수.저장(pending_reply_chat_id,수신메시지.result[0].message.chat.id)
    처리.깃허브테스트처리
  그외
    전송.델레그램응답전송
}
처리::TELEGRAM.텔레그램깃허브이슈등록명령처리
{
  만약에(참)
    함수.단어분리(gh_reg_word_list,세션.cmd_rest)          ← "이슈등록"을 뗀 나머지 = 제목
    함수.단어합치기(issue_reg_title,세션.리스트.gh_reg_word_list,1)
    함수.저장(issue_reg_body,문장.깃허브텔레그램이슈등록본문문장)
    함수.저장(gh_token,설정.GITHUB.token)
    전송.깃허브이슈등록전송                                 ← 3.3.1절 기존 전송:: 재사용
}
```

**"깃허브 이슈등록 [제목]"처럼 제목에 공백이 들어가야 하므로**, 2부
2.5절이 경고한 `함수.저장(target,a,b,c,...)`의 함정(EXE_SET은 실제로
`_params[0]`/`[1]`만 쓰고 나머지 콤마 인자는 조용히 버립니다 —
`EXEFUNC.cpp:135-143`, `깃허브이슈텔레그램회신`의 기존
`함수.저장(reply_text,GitHub,이슈가,없습니다)`가 실제로는 `reply_text`에
"GitHub" 한 단어만 남기고 나머지를 버리는 동일한 함정에 걸려 있었다는
사실도 이번에 확인했습니다 - 기존 동작이라 이번 티켓 범위에서 손대지
않았지만, 새 코드에서 같은 함정을 반복하지 않도록 `함수.저장` 대신
**`함수.단어분리`+`함수.단어합치기(...,1)`**(3.5절 2단계 단어분리 패턴)로
제목을 재조합했습니다. 반대로 등록 본문(`issue_reg_body`)처럼 고정
문구는 여러 단어라도 리터럴 콤마 나열이 아니라 **`문장::`(STMT) 블록으로
빼서 `함수.저장(target,문장.이름)`(단일 값 참조 2인자)로 채우면
안전합니다.

**대상 저장소는 그대로 `dorafather/NotebookFlow`로 고정**입니다(1.10절) —
Telegram으로 등록한 이슈도 3.3.1절의 기존 `전송::GITHUB.깃허브이슈등록전송`
(경로 `/repos/dorafather/NotebookFlow/issues` 하드코딩)을 그대로 타므로,
누가 이 명령을 쓰든 항상 이 저장소로 이슈가 생성됩니다. 다른 저장소를
쓰고 싶다면 1.10절 안내대로 Claude Code에게 새로 요청하세요.

## 3.4 Telegram ↔ GitHub 조합 — 세션을 통한 컨텍스트 유지

Telegram에서 시작된 요청이 GitHub 서비스로 위임됐다가, 다시 Telegram으로
회신되는 전형적인 "서비스 조합" 사례입니다. 핵심은 **1.5절**에서 설명한
세션의 NAMESPACE 예외입니다:

```
처리::TELEGRAM.텔레그램깃허브명령분기
{
  만약에(세션.cmd_타입 == 1)
    함수.저장(pending_reply_chat_id,수신메시지.result[0].message.chat.id)  ← TELEGRAM이 세션에 적음
    처리.깃허브테스트처리                                                   ← GITHUB 네임스페이스로 위임(자유 호출)
  그외
    전송.델레그램응답전송
}
```

(2026-09-19 기준 `텔레그램깃허브명령분기`는 3.3.2절에서 설명한 "이슈등록"
분기가 맨 앞에 하나 더 추가된 상태입니다 - 이 스니펫은 "TELEGRAM이 세션에
적고 GITHUB가 읽는다"는 패턴 자체를 보여주는 것이 목적이라 여기서는
단순화된 옛 모습 그대로 두었습니다. 실제 최신 코드는 3.3.2절을 참고하세요.)

GitHub 조회가 끝나면(3.3절), `세션.pending_reply_chat_id`를 GITHUB
네임스페이스 코드가 그대로 읽어 Telegram으로 회신합니다:

```
전송::GITHUB.깃허브이슈텔레그램회신전송
{
  전송메시지.메소드 = GET
  전송메시지.주소.도메인 = 설정.TELEGRAM.domain
  전송메시지.주소.경로 = 문장.텔레그램응답경로
  전송메시지.주소.파라미터[0].key = chat_id
  전송메시지.주소.파라미터[0].val = 세션.pending_reply_chat_id   ← TELEGRAM이 남긴 값을 GITHUB가 읽음
  전송메시지.주소.파라미터[1].key = text
  전송메시지.주소.파라미터[1].val = 세션.reply_text
}
```

## 3.5 Telegram 명령 분기 구조 — ⚠️ 실측: addr.ini 동적 순회가 아니라 서비스별 하드코딩

`업무지침_Telegram_통합명령파싱_help_주석분리.md`는 "`{연동노드} {타입}
{나머지}}` 문법을 `addr.ini`의 `telegram_alias`/`category` 필드를 순회해
동적으로 인식하고, 새 서비스 추가 시 `.sce` 코드는 손대지 않는다"는
설계를 제안했습니다.

> ⚠️ **불일치**: 실제 `rest.sce`는 이 설계를 구현하지 않았습니다. 메모리
> 기록(`project_notebookflow_telegram_unified_cmd_blocked`)에도 "토큰분리+
> INI 동적조회 함수 부재로 중단"이라고 남아있어, 이 티켓 자체가 완료되지
> 못했다는 사실과 일치합니다. **실제 구현은 서비스 이름마다 `함수.앞자리비교`를
> 한 줄씩 나열하는 하드코딩**입니다:

```
처리::TELEGRAM.텔레그램에코응답처리
{
  만약에(수신메시지.result[0].message.from.id == 설정.TELEGRAM.my_chat_id)
    함수.더하기(tg_offset,수신메시지.result[0].update_id,1)
    함수.앞자리비교(cmd_help,수신메시지.result[0].message.text,help)
    함수.앞자리비교(cmd_깃허브,수신메시지.result[0].message.text,깃허브)
    함수.앞자리비교(cmd_슬랙,수신메시지.result[0].message.text,슬랙)
    함수.앞자리비교(cmd_디스코드,수신메시지.result[0].message.text,디스코드)
    함수.앞자리비교(cmd_노티파이,수신메시지.result[0].message.text,노티파이)
    함수.앞자리비교(cmd_블루스카이,수신메시지.result[0].message.text,블루스카이)
    함수.앞자리비교(cmd_클루드코드,수신메시지.result[0].message.text,클루드코드)
    함수.앞자리비교(cmd_텔넷,수신메시지.result[0].message.text,텔넷)
    함수.앞자리비교(cmd_파일,수신메시지.result[0].message.text,파일)
    함수.앞자리비교(cmd_사진,수신메시지.result[0].message.text,사진)
    처리.텔레그램수신메시지분기
  그외
    ...
}
처리::TELEGRAM.텔레그램명령분기
{
  만약에(세션.cmd_help == 1)
    전송.텔레그램help이미지생성
  그외그외(세션.cmd_깃허브 == 1)
    처리.텔레그램깃허브명령처리
  그외그외(세션.cmd_슬랙 == 1)
    처리.텔레그램슬랙명령처리
  ... (서비스 수만큼 그외그외 나열)
  그외
    전송.델레그램응답전송
}
```

각 서비스별 처리(`텔레그램슬랙명령처리` 등)는 **2단계 단어분리** 패턴을
씁니다 — 먼저 첫 단어(서비스명)를 떼어 "나머지"를 얻고, 그 "나머지"의
앞자리를 다시 검사해 두 번째 키워드("알람" 등)를 떼어 "진짜 나머지"를
얻습니다:

```
처리::TELEGRAM.텔레그램슬랙명령처리
{
  만약에(참)
    함수.단어분리(cmd_word_list,수신메시지.result[0].message.text)
    함수.단어합치기(cmd_rest,세션.리스트.cmd_word_list,1)     ← 첫 단어("슬랙") 제외한 나머지
    함수.앞자리비교(cmd_타입,세션.cmd_rest,알람)
    처리.텔레그램슬랙명령분기
}
처리::TELEGRAM.텔레그램슬랙명령분기
{
  만약에(세션.cmd_타입 == 1)
    함수.단어분리(type_word_list,세션.cmd_rest)
    함수.단어합치기(alert_message,세션.리스트.type_word_list,1)   ← "알람"까지 제외한 진짜 문구
    전송.슬랙알람직접전송
  그외
    전송.델레그램응답전송
}
```

**정리**: `addr.ini`의 `telegram_alias`/`category` 필드는 실제로
존재하고 값도 채워져 있지만(1.6절), 이 Telegram 명령 분기 코드
어디에서도 읽히지 않습니다. **새 서비스를 Telegram 명령으로 추가하려면
`addr.ini`에 한 줄 추가하는 것으로는 부족하고, 위 세 군데(에코응답처리의
`함수.앞자리비교` 목록 / 명령분기의 `그외그외` 목록 / 전용
처리+전용 분기 처리 한 쌍)를 전부 새로 작성해야 합니다.** 4부 4.5절에
템플릿을 정리해 두었습니다.

## 3.6 agent-flow — Telegram → Claude Code 역방향 연동

```
처리::TELEGRAM.텔레그램클루드코드명령처리
{
  만약에(참)
    함수.단어분리(cmd_word_list,수신메시지.result[0].message.text)
    함수.단어합치기(cmd_rest,세션.리스트.cmd_word_list,1)
    로그.출력(클루드 명령 실행 세션.cmd_rest)
    전송.클루드명령에코
    전송.클루드코드명령전송
}
전송::TELEGRAM.클루드코드명령전송
{
  전송메시지.메소드 = POST
  전송메시지.주소.도메인 = 설정.AGENT.domain
  전송메시지.주소.경로 = 설정.AGENT.cmd_path
  전송메시지.instruction = 세션.cmd_rest
  전송메시지.callback_chat_id = 수신메시지.result[0].message.chat.id
  전송메시지.session_key = claude_persistent
  전송메시지.agent_name = 설정.AGENT.agent_name
}
```

`AGENT`(`agent-flow`, `agent_flow.py`)는 이 요청을 **즉시 접수만 하고**
(`{"RESULT":"0","job_id":...}`를 수십 ms 안에 응답 — `claude -p`가 몇
초~몇 분 걸릴 수 있어 동기 대기하면 `OutboundClient`의 10초 read
timeout에 걸리기 때문) 백그라운드로 `claude -p`를 돌린 뒤, 완료되면
**스스로 NotebookFlow.exe에 다시 fire-and-forget PUT**을 보내
`ACTION_EVENT=클루드코드완료` 이벤트를 되돌립니다:

### "클루드코드"는 지속 세션이다 (2026-09-25 도입)

"클루드코드"는 더 이상 매번 완전히 새로 시작하는 1회성 호출이 아닙니다.
`session_key=claude_persistent`를 항상 실어 보내므로, `agent_flow.py`가
`claude -p --resume <고정 UUID>`로 **이전 대화를 계속 이어갑니다**
(`agent_flow_sessions.json`에 session_key→UUID 매핑을 영구 저장 —
최초 호출은 그 세션이 아직 없으므로 `--resume`이 아니라 `--session-id`로
새로 만듭니다. 존재하지 않는 세션 ID에 `--resume`을 쓰면 "No conversation
found"로 그냥 실패한다는 걸 실측으로 확인했으므로 반드시 이 순서를
지켜야 합니다).

`전송메시지.agent_name = 설정.AGENT.agent_name`은 `--agent <이름>`으로
연결됩니다 — `addr.ini`의 `[AGENT].agent_name`이 비어있으면(기본값)
`agent_name` 필드 자체가 빈 문자열로 나가고, `agent_flow.py`는 그걸
`None`으로 취급해 `--agent` 플래그 자체를 생략합니다(평범한 Claude로
동작). 값이 채워져 있으면 그 이름의 커스텀 에이전트 정의(전역
`~/.claude/agents/<이름>.md`)로 실행됩니다 — 설치 마법사의 "전용 Claude
Code 에이전트 생성" 항목을 선택하면 `notebookflow-agent`로 자동
채워집니다(1.14절 참고). ⚠️ **`--agent`에 존재하지 않는 이름을 넘기면
호출 전체가 그냥 실패합니다**(실측 확인, 폴백 없음) — `agent_name`을
비워두는 것과 "아무 이름이나 넣어도 무해하게 무시됨"은 전혀 다릅니다.

### 대화 횟수 기반 리셋 확인 + "클루드초기화"

`agent_flow.py`는 session_key별 누적 대화 횟수를 세어서, 기본
**20회**(`AGENT_RESET_TURN_THRESHOLD` 환경변수로 조정 가능)의 배수에
도달할 때마다 평소 응답을 보낸 직후 "이 세션이 N회 대화를 이어왔습니다.
초기화할까요? (예/아니오)"를 한 번 더 보냅니다(같은 `report_done()` 콜백을
두 번 부르는 것뿐, 새 DSL 배선 없이 동작). 그 확인 대기 중에 다음
"클루드코드 [텍스트]"가 오면, 그 텍스트를 지시문이 아니라 예/아니오
응답으로 해석합니다(`claude -p`를 아예 실행하지 않으므로 비용 없음) —
"예"류(예/네/넵/응/ok 등, 첫 어절 완전 일치만 인정)면 세션을 완전히
버리고 다음 호출부터 새로 시작하고, 애매한 응답이면 확인 대기 상태를
유지한 채 재질문합니다.

"클루드초기화" 명령은 이 확인 절차 없이 즉시 `PUT /notebookflow/agent/reset`을
호출해 session_key를 리셋합니다:

```
처리::TELEGRAM.텔레그램클루드초기화명령처리
{
  만약에(참)
    전송.클루드초기화명령전송
}
전송::TELEGRAM.클루드초기화명령전송
{
  전송메시지.메소드 = POST
  전송메시지.주소.도메인 = 설정.AGENT.domain
  전송메시지.주소.경로 = 설정.AGENT.reset_path
  전송메시지.session_key = claude_persistent
  전송메시지.callback_chat_id = 수신메시지.result[0].message.chat.id
}
```

```
상태::FLOW.초기
{
  ...
  FLOW.수신메시지.이벤트명 == 클루드코드완료    처리.클루드코드완료처리
  ...
}
처리::FLOW.클루드코드완료처리
{
  만약에(참)
    함수.저장(agent_chat_id,수신메시지.chat_id)
    함수.저장(agent_result,수신메시지.result)
    전송.클루드코드완료텔레그램전송
}
```

**이 패턴의 일반형**: "긴 작업을 트리거하고, 완료되면 콜백으로 결과를
받는" 모든 외부 RA(File-RA 등)가 이 구조를 재사용합니다. `agent_flow.py`
자체는 `claude -p` 서브프로세스 실행을 `asyncio.Lock()`으로 직렬화하여
동시에 여러 명령이 파일을 건드리는 경쟁을 피합니다.

### agent-flow가 요구하는 최소 Claude CLI 버전 (2026-09-23 조사)

`agent_flow.py`의 `build_command()`(`agent-flow/agent_flow.py:96-110`)가
실제로 조립하는 `claude` 헤드리스 호출 플래그:

```
claude -p <instruction> --permission-mode acceptEdits --output-format json
       --no-session-persistence [--max-budget-usd <금액>]  (마지막 플래그는
       AGENT_MAX_BUDGET_USD 환경변수가 설정된 경우에만 조건부로 추가됨)
```

**최소 버전: Claude CLI 2.1.258 이상을 권장합니다.** 이 버전은 이
프로젝트에서 agent-flow 전체 파이프라인(즉시 접수 → 백그라운드 실행 →
완료 콜백 → 실제 Telegram 발신)이 정상 동작함을 실측으로 확인한 버전입니다
(`rest_devnotes.txt` 참고). 2026-09-23 기준 개발 환경에는 2.1.274가
설치돼 있으며, `claude -p --help` 출력으로 위 4개 플래그가 모두 그대로
지원됨을 재확인했습니다. 공식 CLI changelog
(`https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md`)
기준으로는 `--max-budget-usd`가 버전 **2.0.28**에서 추가되었고,
`--output-format`(스트리밍이 아닌 기본 text/json 모드)은 그보다 훨씬
이전(스트리밍 변형이 추가된 0.2.66 시점에 이미 존재)부터 있었습니다 —
즉 `--max-budget-usd`를 쓰지 않는 한(옵션이며 기본은 미사용) **2.0.28보다
훨씬 오래된 버전에서도 이론적으로는 동작할 수 있지만, 실제로 이 정확한
조합을 검증한 것은 2.1.258부터**이므로 그 이전 버전은 보증하지 않습니다.

**구버전 CLI에서 플래그를 모르면 어떻게 되는지(실측)**: 이 세션에서 존재하지
않는 플래그(`--totally-fake-flag-xyz`)로 직접 재현한 결과, `claude` CLI는
`stdout`을 전혀 출력하지 않고 `stderr`에 `error: unknown option
'--플래그이름'` 한 줄만 남긴 채 종료 코드 1로 즉시 종료합니다(아무 API
호출도 발생하지 않음 — 과금 없이 안전하게 실패). `agent_flow.py`의
`run_claude()`는 "exit code가 0이 아니어도 먼저 stdout을 JSON으로 파싱
시도하고, 실패하면 stderr 기반 메시지로 폴백"하도록 구현돼 있으므로
(`agent_flow.py:141-152`), 구버전 CLI를 쓰는 사용자는 Telegram
완료 콜백으로 **"claude 종료 코드 1: error: unknown option
'--output-format'"**(또는 누락된 플래그 이름) 같은 명확한 에러 메시지를
그대로 받게 됩니다 — 무한 대기나 조용한 실패가 아니라 원인이 드러나는
형태의 실패입니다.

## 3.7 ssh-flow — 원격 명령 실행

```
처리::SSH.sshRsp
{
  만약에(참)
    함수.붙이기(telnet_rsp,수신메시지.stderr,수신메시지.stdout)
    처리.sshRsp길이분기
}
처리::SSH.sshRsp길이분기
{
  만약에(세션.telnet_rsp.길이 > 4096)    ← .길이(.LENGTH) 접미사로 조건 분기
    전송.텔넷회신문서전송                 ← 길면 문서(파일)로 전송
  그외
    전송.텔넷회신                        ← 짧으면 텍스트로 바로 회신
}
```

`ssh-flow`(`ssh_flow.py`, FastAPI + `paramiko`)는 호스트/포트/계정/명령을
그대로 받아 SSH로 실행하고 stdout/stderr/exit_code를 돌려주는 단순한
브릿지입니다. 응답이 길면(4096자 초과) 2부 2.12절의 `문서` multipart
필드로 파일 전송으로 전환하는 게 이 예시의 핵심 응용입니다(3부 3.8절과
동일한 패턴). 요청을 어느 서버로 보낼지 고르는 라우팅은 3.7.1절 참고.

## 3.7.1 다중 SSH 대상 분기("응답형" 라우팅 모델) — 전송객체명으로 서버를 고르는 구조 (2026-09-19 신규)

원래 `전송::SSH.텔넷명령어전송`은 host/port/username/password가 addr.ini가
아니라 **이 전송:: 블록 안에 직접 하드코딩**되어 있어서 SSH 대상이 항상
하나였습니다. 이 절은 "텔넷 {전송객체명} {명령어...}" 문법으로 **여러
SSH 대상 중 하나를 골라 라우팅**하도록 확장한 기록이며, 3.5절의 Telegram
통합 명령 파싱(`{연동노드} {타입} {나머지}`)과 완전히 같은 원리를 한 단계
더 적용한 것입니다 — 다만 여기서는 "타입"이 아니라 **"이 요청을 어느
전송 대상으로 보낼지"**를 고르는 용도로 쓰입니다.

**① addr.ini — 대상마다 별도 섹션**. `[SSH]`는 여전히 `ssh-flow` 자체의
REST 엔드포인트(domain/cmd_path)만 담당하고, 실제 원격 서버 자격증명은
대상 이름을 붙인 새 섹션에 하나씩 둡니다:

```ini
[SSH]
domain=http://127.0.0.1:8450
cmd_path=/notebookflow/ssh/execute

[SSH_기본]
host=203.0.113.10
port=10022
username=deploy_user
password=your-ssh-password

[SSH_회사서버]
host=203.0.113.20
port=10022
username=deploy_user
password=your-ssh-password

[SSH_홈서버]
host=203.0.113.30
port=10022
username=deploy_user
password=your-ssh-password
```

`[SSH_기본]`은 전송객체명을 생략한 옛 문법("텔넷 [명령어]")의 하위호환
경로입니다 — 원래 `전송::SSH.텔넷명령어전송`에 박혀 있던 실제 운영 서버
정보를 그대로 옮겨온 것이라, 기존 사용자의 "텔넷 df" 같은 명령은 한
글자도 바뀌지 않고 계속 이 대상으로 갑니다.

**② rest.sce — 두 단계로 "대상 이름"을 떼어낸 뒤 전용 `전송::`으로
분기**. 1.6절 원칙대로 addr.ini 섹션을 자동 순회하는 코드는 없으므로,
대상 이름마다 `함수.앞자리비교`를 하드코딩하고(3.5절과 동일한 패턴),
매치되면 그 이름 전용 `전송::`을 호출합니다:

```
처리::TELEGRAM.텔레그램텔넷명령처리
{
  만약에(참)
    함수.단어분리(cmd_word_list,수신메시지.result[0].message.text)
    함수.단어합치기(cmd_rest,세션.리스트.cmd_word_list,1)   ← "텔넷"을 뗀 나머지("회사서버 df" 또는 "df")
    함수.앞자리비교(cmd_ssh_회사서버,세션.cmd_rest,회사서버)
    함수.앞자리비교(cmd_ssh_홈서버,세션.cmd_rest,홈서버)
    처리.텔레그램텔넷명령분기
}
처리::TELEGRAM.텔레그램텔넷명령분기
{
  만약에(세션.cmd_ssh_회사서버 == 1)
    함수.저장(ssh_target,회사서버)
    함수.단어분리(ssh_cmd_word_list,세션.cmd_rest)
    함수.단어합치기(cmd_rest,세션.리스트.ssh_cmd_word_list,1)  ← 대상 이름을 한 번 더 떼어 진짜 명령어만 남김
    처리.텔레그램텔넷명령실행
  그외그외(세션.cmd_ssh_홈서버 == 1)
    함수.저장(ssh_target,홈서버)
    함수.단어분리(ssh_cmd_word_list,세션.cmd_rest)
    함수.단어합치기(cmd_rest,세션.리스트.ssh_cmd_word_list,1)
    처리.텔레그램텔넷명령실행
  그외                                                        ← 대상 이름이 안 걸리면 옛 문법("텔넷 [명령어]")
    함수.저장(ssh_target,기본)
    처리.텔레그램텔넷명령실행
}
처리::TELEGRAM.텔레그램텔넷명령실행
{
  만약에(세션.ssh_target == 회사서버)
    전송.텔넷명령어전송_회사서버
  그외그외(세션.ssh_target == 홈서버)
    전송.텔넷명령어전송_홈서버
  그외
    전송.텔넷명령어전송                                        ← [SSH_기본]으로
}
전송::SSH.텔넷명령어전송_회사서버
{
  전송메시지.메소드 = POST
  전송메시지.주소.도메인 = 설정.SSH.domain
  전송메시지.주소.경로 = 설정.SSH.cmd_path
  전송메시지.host = 설정.SSH_회사서버.host
  전송메시지.port = 설정.SSH_회사서버.port
  전송메시지.username = 설정.SSH_회사서버.username
  전송메시지.password = 설정.SSH_회사서버.password
  전송메시지.command = 세션.cmd_rest
}
```

`전송::SSH.텔넷명령어전송_홈서버`/`전송::SSH.텔넷명령어전송`(기본)도
`설정.SSH_홈서버.*`/`설정.SSH_기본.*`만 바꿔 완전히 같은 모양으로
반복됩니다. **응답 처리(`처리::SSH.sshRsp`/`sshRsp길이분기`, 3.7절)는
대상이 몇 개든 손댈 필요가 없습니다** — `ssh-flow`는 어느 대상으로
보냈든 같은 모양의 stdout/stderr JSON을 돌려주므로, 응답 쪽 STATE 매치
지점(`SSH.수신메시지.응답코드 == 200`)과 회신 로직은 3.5절의 "서비스별
전용 처리 한 쌍"보다 더 재사용하기 좋은 지점입니다.

**새 원격 대상을 추가하려면**(예: `모니터링서버`):

```
1. addr.ini에 [SSH_모니터링서버] 섹션 추가 (host/port/username/password)
2. 처리::TELEGRAM.텔레그램텔넷명령처리에 한 줄 추가:
     함수.앞자리비교(cmd_ssh_모니터링서버,세션.cmd_rest,모니터링서버)
3. 처리::TELEGRAM.텔레그램텔넷명령분기에 그외그외 분기 추가(다른 대상보다
   먼저 검사해야 하는 이름 충돌이 없는지 확인 - 지금처럼 대상 이름들이
   서로의 접두어가 아니면 순서는 상관없습니다)
4. 처리::TELEGRAM.텔레그램텔넷명령실행에 그외그외 분기 + 전송::SSH.
   텔넷명령어전송_모니터링서버 한 쌍 추가(기존 것 복제, 대상 이름만 교체)
```

이 패턴은 3.5절 "Telegram 명령 분기"와 완전히 같은 하드코딩 철학(1.6절 —
addr.ini 필드 확장만으로는 DSL 엔진이 자동으로 반응하지 않는다)을
공유합니다 - 다른 점은 여기서는 "서비스 종류"가 아니라 "같은 서비스
안의 여러 대상"을 고른다는 것뿐입니다.

## 3.8 File/사진 관련 — 로컬 파일시스템 조회, multipart 업로드/다운로드

```
전송::TELEGRAM.텔레그램폴더조회
{
  전송메시지.주소.도메인 = 설정.TELEGRAM.domain
  전송메시지.폴더.경로 = 세션.dir_path
}
```

`전송메시지.폴더` 최상위 키는 1부 1.7절에서 설명했듯 **외부 HTTP 호출
없이 `Main.cpp`가 로컬 `FindFirstFileA`로 직접 디렉토리를 읽어 합성
응답을 만드는** 특수 경로입니다(메소드조차 지정하지 않는 것에 주목 —
이 경로엔 진짜 요청이 없기 때문입니다). 파일 전송은 2.12절의 `문서`
필드 그룹을 그대로 씁니다:

```
전송::TELEGRAM.텔레그램파일전송
{
  전송메시지.메소드 = POST
  전송메시지.주소.도메인 = 설정.TELEGRAM.domain
  전송메시지.주소.경로 = 문장.텔레그램문서전송경로
  전송메시지.주소.파라미터[0].key = chat_id
  전송메시지.주소.파라미터[0].val = 설정.TELEGRAM.my_chat_id
  전송메시지.문서.경로 = 세션.file_path
  전송메시지.문서.파일명 = 세션.file_path
  전송메시지.문서.필드명 = document
  전송메시지.문서.타입 = application/octet-stream
  전송메시지.문서.캡션 = 요청하신 파일입니다
}
```

### 3.8.1 반대 방향 — Telegram에 문서/사진을 올리면 자동 저장(업무지침_텔레그램_파일수신_스트림저장.md, 2026-09-17)

위 예시가 "NotebookFlow → Telegram"(꺼내오기) 방향이라면, 이건 그 반대
방향입니다 - **사용자가 Telegram 채팅창에 문서나 사진을 올리면, 별도
명령 없이 자동으로 NotebookFlow가 실행되는 컴퓨터의 `downloads\` 폴더에
저장**됩니다(폴더는 exe와 같은 폴더 기준 - `s_fnDownloadBaseDir()`,
`Main.cpp`). 흐름은 3부 전체가 공유하는 "폴링 → 분기 → 다운로드" 3단계
그대로입니다:

```
처리::TELEGRAM.텔레그램수신메시지분기
{
  만약에(수신메시지.result[0].message.document.file_id != NULL)
    처리.텔레그램파일업로드감지         ← 문서(파일) 업로드 감지
  그외그외(수신메시지.result[0].message.photo[0].file_id != NULL)
    처리.텔레그램사진업로드감지         ← 사진 업로드 감지(해상도별 배열 중 가장 큰 것 선택)
  그외
    처리.텔레그램명령분기
}
처리::TELEGRAM.텔레그램파일업로드감지
{
  만약에(참)
    함수.저장(download_file_id,수신메시지.result[0].message.document.file_id)
    함수.저장(download_file_name,수신메시지.result[0].message.document.file_name)
    전송.텔레그램파일메타조회            ← Telegram getFile API로 실제 file_path를 얻음
}
처리::TELEGRAM.텔레그램파일다운로드
{
  만약에(참)
    함수.저장(download_file_path,수신메시지.result.file_path)
    전송.텔레그램파일실제다운로드
}
전송::TELEGRAM.텔레그램파일실제다운로드
{
  전송메시지.메소드 = GET
  전송메시지.주소.도메인 = 설정.TELEGRAM.domain
  전송메시지.주소.경로 = 문장.텔레그램파일다운로드경로
  전송메시지.저장.파일명 = 세션.download_file_name   ← "저장.파일명"이 있으면 응답 바디를 JSON 대신 파일로 스트림 저장
}
```

`전송메시지.저장.파일명`은 2부 2.11절/1.7절이 설명한 "통신 계층이 판단하지
않는 라우팅 메타" 계열의 또 다른 예시입니다 - `Main.cpp`의 `App::ACTION()`이
이 필드를 보면 응답 바디를 JSON으로 파싱하지 않고 그대로 디스크에
스트림 저장합니다. 파일명은 Telegram이 보낸 원본 파일명(신뢰할 수 없는
외부 입력)을 그대로 믿지 않고, `s_fnBuildSafeDownloadPath()`가 `/`,`\`,
`:`,`..` 같은 경로 탈출 요소를 제거해 항상 고정된 `downloads\` 폴더
**안에만** 쓰도록 강제합니다("저장.경로"처럼 임의 전체 경로를 받는
필드가 아니라 "저장.파일명"만 받는 이유). 저장이 끝나면
`TELEGRAM.수신메시지.저장모드 == 1` STATE 매치로
`처리::TELEGRAM.텔레그램파일저장완료처리`가 실행되어 성공/실패를
Telegram으로 회신합니다.

## 3.9 help 명령어 — ⚠️ 실측: 정적 STMT 텍스트, addr.ini 동적 생성 아님

`업무지침_Telegram_통합명령파싱_help_주석분리.md`는 "`addr.ini`를 순회해
`telegram_alias`가 있는 모든 섹션을 동적으로 모아 help 목록을 만든다,
새 서비스 추가 시 자동 반영된다"는 설계를 제안했습니다.

> ⚠️ **불일치**: 실제 구현은 **완전히 정적인 `문장::` 텍스트 블록**입니다
> — addr.ini를 순회하는 코드는 어디에도 없습니다. 새 서비스를 추가해도
> 이 문장을 손으로 고치지 않으면 help 목록에 나타나지 않습니다.

```
문장::TELEGRAM.텔레그램help안내문구
{
[사용 가능한 명령어]
 깃허브    이슈 - GitHub 이슈 목록 조회
 슬랙     알람 [문구] - Slack 알림 전송
 디스코드  알람 [문구] - Discord 알림 전송
 노티파이  알람 [문구] - ntfy 알림 전송
 블루스카이 알람 [문구] - Bluesky 포스팅
 클루드코드 [지시]    - Claude Code에게 작업 지시
 텔넷     [명령어]   - 원격 서버에서 셸 명령 실행
 파일 얻기 [경로]     - 로컬 파일을 전송받기
 파일 폴더 [경로]     - 로컬 폴더 목록 조회(최대 20개)
 사진 얻기 [경로]     - 로컬 파일을 전송받기
}
```

또한 실제로 텍스트로 회신하는 `help`가 아니라, 최신 rest.sce는 `cmd_help`
분기에서 `전송.텔레그램help이미지생성`(GDI+로 이미지를 만들어 사진으로
전송)을 호출하도록 진화해 있습니다 — 이 역시 3.8절과 같은 "외부 호출
없는 로컬 합성 응답" 패턴(`전송메시지.헬프이미지 = 1`)입니다.

> ⚠️ **불일치 정정 (2026-09-25)**: 이 절은 한동안 "실제 사용자가 보는
> help는 `Main.cpp`의 `s_fnGenerateHelpImage()`가 그리는 하드코딩된
> `s_rows` 배열"이라고 서술했지만, **실측 결과 그 반대입니다.**
> `s_fnGenerateHelpImage()`는 `#if 1`로 `HelpFileReader`(exe와 같은
> 폴더의 `help.json`을 1초 간격으로 폴링해 읽는 경로)를 실제로 쓰고
> 있고, `s_rows` 하드코딩 배열은 `#else` 쪽에 **죽은 코드**로만
> 남아있습니다(2026-09-24 실측 확인). 즉 **`help.json`이 진짜 소스이고,
> `rest.sce`/`addr.ini`와 똑같이 1초 핫리로드로 반영됩니다**(재시작
> 불필요) — 파일 위치는 `NotebookFlow/help.json`(개발 트리) /
> `bin\notebookflow\help.json`(배포판), 형식은 `{"rows":[{"cmd":...,
> "desc":...}]}` 단순 배열입니다. 2026-09-25 기준 실제 목록:

```
{"cmd": "help", "desc": "도움말 보기"}
{"cmd": "깃허브 이슈", "desc": "NotebookFlow 저장소 이슈 목록 조회"}
{"cmd": "깃허브 이슈등록 [제목]", "desc": "NotebookFlow 저장소에 이슈 등록"}
{"cmd": "슬랙 알람 [문구]", "desc": "Slack 알림 전송"}
{"cmd": "디스코드 알람 [문구]", "desc": "Discord 알림 전송"}
{"cmd": "노티파이 알람 [문구]", "desc": "ntfy 알림 전송"}
{"cmd": "블루스카이 알람 [문구]", "desc": "Bluesky 포스팅"}
{"cmd": "클루드코드 [지시]", "desc": "Claude Code에게 작업 지시 (이전 대화 맥락을 이어가는 지속 세션)"}
{"cmd": "클루드초기화", "desc": "클루드코드 세션을 즉시 초기화 (확인 없이 바로 리셋)"}
{"cmd": "텔넷 [전송객체명] [명령어]", "desc": "원격 서버 셸 명령 실행 (대상 생략 시 기본 서버)"}
{"cmd": "파일 얻기 [경로]", "desc": "로컬 파일 전송받기"}
{"cmd": "파일 폴더 [경로]", "desc": "로컬 폴더 목록 조회 (최대 20개)"}
{"cmd": "사진 얻기 [경로]", "desc": "로컬 이미지 전송받기 (사진으로)"}
{"cmd": "고객문의 [문의내용]", "desc": "NotebookFLOW 커뮤니티 채널로 문의 전달"}
{"cmd": "문서,사진을 채팅창에 올리기", "desc": "downloads 폴더(exe와 같은 폴더 기준)에 자동 저장"}
```

**rest.sce에 새 명령 분기를 추가했다면 반드시 `help.json`에도 대응하는
행을 같이 추가**하세요 — 자동 반영은 안 됩니다(`addr.ini`를 순회해서
동적으로 만드는 게 아니라 여전히 정적 목록입니다), 다만 파일만 저장하면
재시작 없이 바로 반영된다는 점은 다른 hot-reload 파일들과 동일합니다.

## 3.10 Linear GraphQL 연동 — STMT 리터럴 중괄호 이스케이프 실전 예시

```
전송::LINEAR.sndLinearIssue
{
  전송메시지.메소드 = POST
  전송메시지.주소.도메인 = 설정.LINEAR.domain
  전송메시지.주소.경로 = /graphql
  전송메시지.헤더[0].key = Authorization
  전송메시지.헤더[0].val = 설정.LINEAR.api_key
  전송메시지.query = 문장.stmtLinearQuery
}
문장::LINEAR.stmtLinearQuery
{$$$<$$$ issues(first: 10) $$$<$$$ nodes $$$<$$$ id title identifier state $$$<$$$ name $$$>$$$ $$$>$$$ $$$>$$$ $$$>$$$}
```

GraphQL 쿼리 문자열 자체가 중괄호투성이라, 2.10절의 `$$$<$$$`/`$$$>$$$`
이스케이프가 실전에서 왜 필요한지를 가장 잘 보여주는 예시입니다.

---

# 4부 — 자연어 요청을 DSL로 옮기는 가이드

## 4.1 절차 — 이 문서 어디를 언제 참고할지

```
1. 사용자의 자연어 요청을 "이벤트 → 조건 → 행동" 3단으로 쪼갠다.
   (예: "슬랙으로 도라아빠 천재라고 알려줘" → 이벤트=지금 즉시,
    조건=없음, 행동=Slack에 POST)

2. 기존에 비슷한 서비스가 있는지 3부에서 먼저 찾는다.
   → 있으면 그 블록을 복사해 이름/필드만 바꾸는 게 가장 안전
     (이 문서가 "가상 예시 금지"인 이유와 같음 — 실제로 동작이 검증된
     코드를 변형하는 게 새로 짜는 것보다 항상 안전하다)

3. 새 외부 서비스라면 addr.ini에 섹션을 먼저 추가한다(2.13절).
   도메인/토큰 등 인증 정보 + (선택) category/telegram_alias 등
   메타 필드. ⚠️ 메타 필드는 DSL 엔진이 자동으로 읽어주지 않는다
   (1.6절) — 실제 동작은 .sce에 반드시 별도로 작성해야 한다.

4. 처리::/전송::/(필요시 타이머::/문장::) 블록을 NAMESPACE를 새로
   정하여 작성한다(2.1절 문법, 정의엔 NAMESPACE 필수/호출엔 생략).

5. 상태:: 블록에 새 이벤트 분기 조건을 추가한다(2.2절).

6. 배열/반복이 필요하면 CPH보다 "재귀 처리:: + 대괄호 동적참조"를
   기본으로 고려한다(2.6절, 3.3절 이유 참고) — 원본 API가 필드 순서를
   보장한다고 확신할 수 있을 때만 CPH를 검토.

7. 조건 분기가 3개 이상이면 반드시 그외그외/그외 체인으로 쓴다
   (2.4절 — 별개의 만약에를 나열하면 안 된다).
```

## 4.2 템플릿 — 새 알림(알람) 서비스 추가

3부 3.1절 Discord/Slack/ntfy 패턴을 그대로 복제합니다. 필요한 것:

1. `addr.ini`에 `[새서비스]` 섹션: `domain`(또는 `webhook_url`) + 인증정보
2. `상태::FLOW.초기`에 두 줄 추가:
   ```
   FLOW.수신메시지.이벤트명 == 새서비스알람전송    처리.새서비스알람직접트리거
   새서비스.수신메시지.응답코드 == 200(또는 서비스가 실제로 돌려주는 성공 신호)    처리.새서비스응답처리
   ```
3. `처리::새서비스.새서비스알람직접트리거` (3.1절 Discord 패턴 그대로 복제, 서비스명만 교체)
4. `전송::새서비스.새서비스알람직접응답` (echo, 메소드 생략)
5. `전송::새서비스.새서비스알람직접전송` (진짜 발신, 메소드=POST 필수 — 빠뜨리면 echo로 오분류됨, 2.11절)
6. `처리::새서비스.새서비스응답처리` (응답 로깅만 해도 충분, 3.1절 참고)

## 4.3 템플릿 — 두 서비스 조합(로그인 필요 서비스, 또는 A→B 위임)

3부 3.2절(Bluesky 로그인→포스팅) 또는 3부 3.4절(Telegram→GitHub→Telegram)을
기반으로:

```
A) 로그인이 필요한 단일 서비스 조합 (3.2절 패턴)
  처리::가 1단계 전송(로그인) 호출
  1단계 응답을 상태::가 감지해 처리::(토큰수신처리)로 라우팅
  그 처리::가 함수.저장으로 토큰을 세션에 남기고 2단계 전송(진짜 동작) 호출

B) 서로 다른 두 서비스 위임(3.4절 패턴)
  서비스A 처리::가 함수.저장으로 "회신 대상"을 세션에 남김
  서비스B 네임스페이스의 처리::를 자유롭게 호출(NAMESPACE는 접근제어가 아님, 1.4절)
  서비스B 작업 완료 후, 세션에 남겨둔 값을 읽어 서비스A로 회신
```

**주의**: 세션 변수 이름은 네임스페이스로 격리되지 않으므로(1.5절),
`pending_reply_chat_id`처럼 이미 쓰이고 있는 이름과 겹치지 않게
새 변수명을 고르세요(예: 기능별 접두어를 붙이는 관례를 권장).

## 4.4 템플릿 — 새 조회 기능(배열 응답) 추가

3부 3.3절 GitHub 패턴을 그대로 복제:

```
1. 응답을 처음 받는 처리::에서 함수.객체저장(리스트명, 수신메시지.배열경로)
   (필드명을 보존해야 나중에 이름으로 꺼낼 수 있음 — 2.6절 CPH의 함정을
   피하는 이유가 바로 이것)
2. 인덱스 변수(idx)를 함수.저장(idx,0)으로 0부터 시작
3. 재귀 처리:: 하나를 만들어:
   만약에(세션.객체.리스트명[세션.idx].원하는필드 != NULL)
     ...(이번 항목 처리, 예: 함수.붙이기로 결과 문자열에 이어붙이기)
     함수.더하기(idx,세션.idx,1)
     처리.자기자신                    ← 재귀
   그외
     처리.종료처리                    ← 배열이 끝났을 때(빈 값 감지)
4. 이슈 개수가 매우 많을 수 있는 API라면(200개 근접) 1부 1.9절의 재귀
   상한을 염두에 두고 상한 처리를 추가로 고려.
```

## 4.5 템플릿 — Telegram에 새 명령어 추가 (현재 하드코딩 패턴 기준)

3부 3.5절에서 확인했듯, **`addr.ini`만 고쳐서는 새 Telegram 명령이
생기지 않습니다.** 아래 세 곳을 함께 고쳐야 합니다(실제 코드 기준):

```
1. 처리::TELEGRAM.텔레그램에코응답처리에 새 줄 추가:
     함수.앞자리비교(cmd_새명령,수신메시지.result[0].message.text,새명령키워드)

2. 처리::TELEGRAM.텔레그램명령분기에 그외그외 분기 추가:
     그외그외(세션.cmd_새명령 == 1)
       처리.텔레그램새명령처리

3. 전용 처리 한 쌍 작성 (3.5절 슬랙 패턴 복제):
   처리::TELEGRAM.텔레그램새명령처리 — 함수.단어분리+함수.단어합치기(...,1)로 첫 단어 제거
   처리::TELEGRAM.텔레그램새명령분기 — 필요하면 2단계로 한 번 더 단어분리

4. (선택, 사용자 안내용) 문장::TELEGRAM.텔레그램help안내문구에 한 줄 추가
   — 자동 반영 안 됨(3.9절), 손으로 추가해야 help에 나타남
```

## 4.6 흔한 함정 체크리스트

```
[ ] 만약에/그외그외/그외를 쓸 때 겉보기로만 "중첩"시키지 않았는가?
    → 실제로는 무조건 평면 형제 목록. 조건을 합치려면 그리고/또는을
      한 헤더 줄 안에서만 쓸 것(2.4절).

[ ] == 의 우변에 NULL/null을 리터럴로 쓸 때, 이게 "빈 값 검사"라는
    걸 인지하고 있는가? != NULL은 "값이 존재하고 비어있지 않다"는 뜻.

[ ] 배열 순회에 함수.컴프리헨션을 쓰려 한다면, 세 번째 인자가 필드
    "이름"이 아니라 필드 "순번(오디널)"이라는 걸 확인했는가? 원본 API가
    필드 순서를 보장하지 않는다면 3.3절의 재귀+대괄호 패턴을 대신 쓸 것.

[ ] 전송:: 블록에 전송메시지.메소드를 빠뜨리지 않았는가? 빠뜨리면
    조용히 "응답(echo)" 모드로 오분류되어 실제 외부 호출이 사라진다.

[ ] 처리::/전송::/타이머::/문장:: 정의는 반드시 NAMESPACE를 붙이지만,
    호출부(처리.이름/전송.이름/타이머.이름/문장.이름)에는 NAMESPACE를
    붙이지 않는다 — 붙이면 오히려 찾지 못한다(1.3절).

[ ] 이름은 시나리오 파일 전체에서 클래스 종류와 무관하게 유일해야
    한다(전역 평면 이름공간) — 다른 네임스페이스라도 이름이 같으면
    충돌한다.

[ ] 세션 변수 이름이 다른 기능이 이미 쓰고 있는 이름과 겹치지 않는가?
    (세션은 NAMESPACE로 격리되지 않는다, 1.5절)

[ ] 문장:: 본문에 리터럴 중괄호가 필요한가?(GraphQL 등) $$$<$$$/$$$>$$$
    이스케이프를 썼는가(2.10절)?

[ ] 전송::/타이머:: 블록의 리터럴 값에 우연히 "/"가 들어 있다면,
    이게 파싱 시 자동으로 ^^^ 마커로 치환됐다가 실행 시 복원된다는 걸
    인지하고 있는가(2.11절) — 디버그 로그 등에서 ^^^가 보이면 이 때문.

[ ] addr.ini에 카탈로그 메타 필드(category/telegram_alias 등)를
    추가하는 것만으로 뭔가 자동으로 동작할 거라 기대하지 않았는가?
    (1.6절 — 이 필드들을 실제로 읽어 분기하는 코드는 mcp-flow에만
    있고, rest.sce 자체에는 없다. rest.sce 안에서 새 동작을 만들려면
    반드시 처리::/전송:: 코드를 직접 작성해야 한다.)

[ ] 재귀로 배열을 순회하는 처리::를 새로 만든다면, 종료 조건이 정확히
    "값이 없을 때"(!=NULL의 반대)로 걸려 있는가? 종료 조건을 잘못
    걸면 200회 상한(1.9절)까지 도는 조용한 실패가 난다.

[ ] `함수.붙이기`(CAT)로 "이번 이벤트마다 새로 문자열을 만든다"고
    가정하지 않았는가? 대상 세션 변수가 이전 사이클에 이미 값을 가진
    적이 있으면 초기화 없이 그 뒤에 계속 이어붙는다(2부 2.5절 붙이기
    함정 참고). 매번 새로 시작해야 하면 `함수.붙이기` 직전에
    `함수.저장(target,첫_실제값)`으로 리셋 + 첫 값 대입을 겸하고, 절대
    `함수.저장(target,)`처럼 값 인자를 비우지 말 것(파서가 깨진다).

[ ] 함수 호출 인자 개수를 틀리지 않았는가? 파싱은 인자 개수를 검증하지
    않으므로, 개수가 틀려도 로드는 성공하고 실행 시점에야 조용히
    실패한다(2.5절).

[ ] 그리고/또는(AND/OR)으로 조건을 여러 개 이을 때, 두 번째 조건부터도
    각자 자기 괄호로 감쌌는가? (`그리고(조건)`이지 `그리고 조건`이
    아님 — 3.3.1절에서 처음 실전 검증됨)

[ ] GET/PUT/POST /notebookflow/flow로 새 이벤트 트리거(상태::FLOW.초기의
    ACTION_EVENT 매치)를 추가했다면, 실제 작업과 별개로
    `전송메시지.주소 = 수신메시지.주소; 전송메시지.RESULT = 0` 형태의
    즉시 응답을 반드시 먼저 보냈는가? 안 보내면 원본 커넥션이
    ConnRegistry에서 타임아웃날 때까지 붙잡혀 있는다(3.3.1절에서 실측
    재현·확정).

[ ] 외부 API가 2xx가 아닌 응답(4xx/5xx)을 돌려줄 가능성이 있는 새
    전송::을 추가했다면, 그 응답코드에 대한 STATE 분기도 최소한 하나는
    만들어 실패를 조용히 버리지 않게 했는가?(3.3.1절 — GitHub 403이
    실제로 재현된 사례)

[ ] agent-flow(Claude Code)에게 조사/검토/요약을 시키는 지시문을 만들
    때, "가서 찾아봐/조회해봐"식으로 쓰지 않았는가? 필요한 데이터를
    처리:: 단계에서 미리 세션에 담아 지시문 안에 통째로 넣고, "이미
    제공된 내용만 보고 답해, 직접 조회하지 마"라고 명시했는가? 안
    그러면 claude -p가 도구 호출 승인 대기에 걸려 영원히 멈춘다
    (3.3.1절에서 실측 재현·확정 — 비대화형 세션엔 승인해줄 사람이 없다).

[ ] 처리::/전송::/타이머:: 본문 안의 문장(대입문, 함수 호출, 조건문
    헤더)을 여러 줄에 걸쳐 쓰지 않았는가? 반드시 한 줄이어야 한다 —
    어기면 파싱 실패 정도가 아니라 시나리오 전체가 빈 상태로 날아가고
    프로세스가 크래시할 수 있다(3.3.1절 — 실측으로 확인, 복구하려면
    프로세스 재기동이 필요했다). 여러 줄 텍스트가 꼭 필요하면 함수
    인자로 직접 쓰지 말고 별도 문장::(STMT) 블록으로 빼서 참조할 것.

[ ] 함수 호출 인자 값 안에 콤마(,)가 들어가야 하는가? 따옴표로
    감싸도 소용없다 — 이 DSL의 함수 인자 파서는 따옴표/이스케이프
    개념이 전혀 없어서 보이는 콤마를 전부 인자 구분자로 먹어버린다
    (2.5절, 2026-09-26 KRX "관심종목 조회" 무응답 버그의 근본 원인).
    콤마 대신 |/:/; 등 다른 문자를 따옴표 없이 맨 글자로 쓸 것.

[ ] "추가/삭제/조회"처럼 하위 명령이 여러 개인 기능을 만들었다면,
    그중 하나(보통 제일 단순한 "추가")만 실측하고 "구현 완료"로
    보고하지 않았는가? 이번 KRX 기능이 정확히 이 실수로 v1.4.0에
    버그가 든 채 릴리스됐다(2026-09-26) — "조회"/"삭제"는 실제로
    한 번도 눌러보지 않은 채 배포됐고, 둘 다 같은 파서 함정으로
    완전 무응답이었다. **하위 명령이 N개면 최소 N번, 각 명령을
    최소 한 번씩은 실제로(가능하면 실제 Telegram으로) 호출해서
    응답이 오는 것까지 확인한 뒤에만 "검증 완료"로 보고할 것** —
    "비슷한 패턴이니 될 것이다"라는 추정은 검증이 아니다.
```

---

# 부록 A — 발견된 불일치 전체 목록

| # | 항목 | 기존 설계 문서 주장 | 실제 코드/rest.sce 확인 결과 |
|---|---|---|---|
| 1 | NAMESPACE 호출부 표기 | `설계원칙_처리전송타이머문장_NAMESPACE확장.md`: 정의/호출 모두 `NAMESPACE.이름` 동일 문자열 사용 필수 | 정의만 NAMESPACE 필수, 호출은 이름만(접두사 없음). FINDPROC 등 조회 함수는 네임스페이스 인자 자체가 없음. rest.sce 전체에서 네임스페이스-포함 호출 0건 |
| 2 | `카탈로그::` 클래스 | `설계원칙_Default시나리오_카탈로그선언.md`: 새 `.sce` 클래스로 서비스 자기소개 | 그런 클래스 없음(CLASSPARSER에 미존재, .sce 0건). 실제로는 기존 `설정::`(addr.ini)의 자유 key=value로 동일 필드 구현, 소비자는 DSL 엔진이 아니라 별도 프로세스 mcp-flow |
| 3 | `설정::`(INI) 클래스 | 문법상 존재, 설정값을 담는 용도로 오인 가능 | `.sce` 안 `설정::` 블록은 `ExeScen::PARSE()`가 처리하지 않는 죽은 코드. 실제 설정 원천은 별도 파일 `addr.ini` |
| 4 | Telegram 통합 명령 파싱 | `업무지침_Telegram_통합명령파싱_help_주석분리.md`: addr.ini `telegram_alias`/`category` 동적 순회, 하드코딩 없음 | 실제로는 서비스별 `함수.앞자리비교` 하드코딩 + 서비스별 전용 처리:: 쌍. addr.ini 필드는 존재하나 rest.sce에서 읽히지 않음(티켓 자체가 3/4/5 항목 미완료로 중단됨) |
| 5 | help 명령어 | 같은 문서: addr.ini 동적 순회로 하드코딩 없이 목록 생성 | 정적 `문장::` 텍스트 블록(또는 GDI+ 이미지 생성). 새 서비스 추가 시 자동 반영 안 됨 |
| 6 | GitHub 이슈 목록 처리 | `업무지침_GitHub이슈처리_CPH_LOOP_재작성.md`: `함수.컴프리헨션`+`함수.반복`로 재작성 제안 | 실제로는 CPH/LOOP를 채택하지 않고, 신규 기능(대괄호 동적참조, EXEPARAM.cpp)을 만들어 재귀 `처리::` + `함수.객체저장`으로 구현. 제안 문서 자신이 우려한 "CPH 오디널 취약성"이 실측으로 확인되어 회피됨 |
| 7 | CPH(함수.컴프리헨션) 인자 | 필드를 지정하는 방식이 이름일 수도 있다는 가능성 열어둠 | 세 번째 인자는 항상 숫자 오디널 위치. 이름 기반 접근 경로 없음(`row[fieldIdx]`, 이름 조회 아님) |
| 8 | 조건 연산자 한글 이름 | 업무지침 초안: 크다/작다/크거나같다/작거나같다/완전일치/불일치/앞자리일치/부분일치 | `.sce` 실제 토큰은 8개 기호(`TRUE,FALSE,>,<,>=,<=,==,!=,===,!!!`)뿐. 한글 이름은 웹 UI 표시 라벨일 뿐, `.sce`에 쓰이는 문자가 아님 |
| 9 | if/그외그외/그외 중첩 | (업무지침이 스스로 질문으로 남겨둠) | 진짜 중첩 불가 — 본문 내 중괄호 자체가 금지되어 있어 겉보기 중첩은 조용히 평면 형제 목록으로 흩어지고, 바깥 조건과 무관하게 독립 평가됨(파싱 레벨 증거로 확정) |
| 10 | STATE 도메인 대조 방식 | `설계원칙_NAMESPACE표기법_STATE조건.md`: 정확 일치처럼 서술, Discord 웹훅 URL 케이스를 위험 요인으로 우려하며 대안 보류 | 실제로는 앞자리(prefix) 일치(`STRNCMP`) — 그 우려를 오히려 완화하는 방향으로 이미 구현됨. FLOW 네임스페이스는 도메인 대조 자체를 생략(항상 통과)하도록 하드코딩 — 문서가 "구현 시 확정"으로 남겼던 질문의 답 |
| 11 | 통신 계층 "메소드=응답" 특수값 | 문서상 표현으로 "메소드=응답" 같은 특수 모드가 있는 것처럼 서술될 수 있음 | 그런 리터럴 비교는 코드 어디에도 없음. 실제 트리거는 `전송메시지.메소드`가 **비어있는지 여부**. 값 자체를 비교하지 않음 |
| 12 | `함수.주소저장`과 `함수.객체저장` | 서로 다른 용도로 암시됨(주소 vs 객체) | 디스패치 테이블에서 완전히 동일한 함수(`EXE_OBJ`)를 가리키는 순수 별칭. 기능 차이 0 |
| 13 | 슬래시 인코딩(`^^^`) 적용 범위 | `업무지침_HTTP통신부_슬래시치환.md`: deserialPath 호출 지점이 정확히 1곳이라고 명시 | 2026-09-17~18 이후 문서/폴더 관련 필드 추가로 최소 7곳 이상으로 늘어남(시간 경과에 따른 자연스러운 노후화, 코드 자체의 오류는 아님) |
| 14 | 통신 계층 JSON 불간섭 원칙 | `설계원칙_통신계층_JSON내용물불간섭.md`: 내용물로 분기하는 코드가 생기면 위반 신호 | 2026-09-17~18에 추가된 `폴더`/`헬프이미지` 로컬 전용 분기가 이 원칙과 정확히 같은 모양(최상위 키 유무로 조기 분기)이나, 외부 호출이 없다는 점에서 기존 SSE 위반 사례와는 성격이 다름 — 명확한 위반 판정은 아니지만 원칙이 아직 이 케이스까지 명문화되지 않은 긴장 지점으로 기록 |
| 15 | `Telegram_sendDocument_구현.md` 계획 범위 | 계획: Main.cpp+OutboundClient.cpp+EXEACTION.cpp 3개 파일 변경 | 실제 구현은 Main.cpp+OutboundClient.cpp 2개 파일만 변경, EXEACTION.cpp는 무변경(범용 dotted-path 대입 메커니즘으로 충분했음) |

---

# 부록 B — 참고 소스 파일 / 설계 문서 목록

## 직접 읽고 검증한 소스 파일

```
libUtil/parser/CLASSPARSER.h, CLASSPARSER.cpp
libUtil/parser/SCEPARSER.h, SCEPARSER.cpp
libUtil/parser/STATEPARSER.h, STATEPARSER.cpp
libUtil/parser/PROCPARSER.h, PROCPARSER.cpp
libUtil/parser/ACTIONPARSER.h, ACTIONPARSER.cpp
libUtil/parser/STMTPARSER.h, STMTPARSER.cpp
libUtil/parser/IFPARSER.h, IFPARSER.cpp (649줄 전체)
libUtil/parser/CXIFPARSER.h, CXIFPARSER.cpp
libUtil/parser/ASSIGNPARSER.h, ASSIGNPARSER.cpp
libUtil/parser/BASICPARSER.h, BASICPARSER.cpp
libUtil/parser/FUNCPARSER.h, FUNCPARSER.cpp
libUtil/parser/INIPARSER.h, INIPARSER.cpp
libUtil/dfile/INIFILE.h
libUtil/interpretor/EXESCEN.h, EXESCEN.cpp
libUtil/interpretor/EXESTATE.h, EXESTATE.cpp
libUtil/interpretor/EXEPROC.h, EXEPROC.cpp
libUtil/interpretor/EXEIF.h, EXEIF.cpp
libUtil/interpretor/EXECXIF.h, EXECXIF.cpp
libUtil/interpretor/EXEACTION.h, EXEACTION.cpp (288줄 전체)
libUtil/interpretor/EXEFUNC.h, EXEFUNC.cpp (679줄 전체)
libUtil/interpretor/EXEPARAM.h, EXEPARAM.cpp (396줄 전체)
libUtil/interpretor/EXESTMT.h, EXESTMT.cpp
libUtil/interpretor/EXECORE.h, EXECORE.cpp (전체)
libUtil/flow/FLOW.h, FLOW.cpp (전체)
libUtil/flow/MAINP.h, MAINP.cpp
libUtil/kstring/STRCONV.h, STRCONV.cpp
NotebookFlow/Main.cpp (주요 구간 전체 확인)
NotebookFlow/Main.h
NotebookFlow/OutboundClient.h, OutboundClient.cpp (전체)
NotebookFlow/rest.sce (769줄 전체)
NotebookFlow/addr.ini (전체)
agent-flow/agent_flow.py (전체)
ssh-flow/ssh_flow.py (전체)
mcp-flow/mcp_flow.py (관련 구간)
```

## 대조한 기존 설계 문서 (`NotebookFlow_업무/`)

```
설계원칙_통신계층_JSON내용물불간섭.md
설계원칙_NAMESPACE표기법_STATE조건.md
설계원칙_처리전송타이머문장_NAMESPACE확장.md
설계원칙_Default시나리오_카탈로그선언.md
설계노트_배치문법vs세션FSM.md
업무지침_GitHub이슈처리_CPH_LOOP_재작성.md
업무지침_Telegram_통합명령파싱_help_주석분리.md
업무지침_sendDocument_기술검토_multipart지원.md / _결과보고_2026-09-17.md
업무지침_Telegram_sendDocument_구현.md
업무지침_주소오브젝트_URL조합인코딩.md / _결과보고_2026-09-05.md
업무지침_HTTP통신부_슬래시치환.md / _결과보고_2026-09-06.md
업무지침_FileRA_세션구조정정.md
업무지침_DSL완전레퍼런스_소스분석기반.md (이 문서를 만들라고 지시한 업무지침 자신)
설계노트_NotebookFlow_커뮤니티비전.md
업무지침_1차실행지시_커뮤니티비전_실무착수.md
```

---

*이 문서는 2026-09-18~19에 작성되었습니다. NotebookFlow는 계속
개발 중인 프로젝트이므로, 이 문서가 다루는 소스(특히 `rest.sce`)가
이후 바뀌면 이 문서의 3부 예시와 부록 A의 일부 항목은 다시 검증이
필요할 수 있습니다. 이 문서 자체가 "설계 문서가 코드와 어긋날 수
있다"는 교훈 위에서 쓰였다는 걸 기억해 주세요 — 다음에 이 문서를
갱신할 사람도, 여기 적힌 내용을 맹신하지 말고 소스를 다시 열어
대조하시길 권합니다.
